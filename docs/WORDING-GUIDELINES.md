# 文言ガイドライン（部員向けの言葉づかい統一）

最終更新: 2026-07-30 / Claude

> 目的: アプリ内の日本語文言を、非エンジニアの部員が迷わず読める言葉に統一する。
> 「同期・反映・取込・書き戻し・正・上書き」などの**開発都合の用語が画面に出ている**問題を解消するための規約。
> UI・操作の規約本体は `docs/UI-UNIFICATION.md`、UX方針は `docs/UX-ISSUES-2026-06.md`。本書は**言葉づかいだけ**を扱う。

---

## 0. 大原則

1. **部員は「アプリ・スプレッドシート・Google」しか知らない**前提で書く。DB・同期方向・列・行・トークンなどの内部概念は画面に出さない。
2. **敬体（です・ます）で統一**。常体（〜だ／〜する）や体言止めの説明文は使わない。
3. **質問形（「どちらに記録する？」等）は使わない**。アプリの文言としてくだけすぎる。ラベルは名詞、操作は動詞、案内は「〜します／〜してください」。
4. **短く。1文1情報**。1つの吹き出し・注釈に情報を詰め込まない。
5. **不安をあおる語を避ける**。「上書き」「失敗」「エラー」「消えます」は、必要なときだけ・必ず対処法とセットで。

---

## 1. 文体の型（どこに何を書くか）

| 要素 | 文体 | 例 |
|---|---|---|
| 入力欄のラベル | **体言止め（名詞）** | 「日付」「場所」「メニュー」 |
| ボタン | **動詞・言い切り** | 「記録する」「保存する」「予定に登録」 |
| 補助説明（欄の下の注釈） | **敬体・1文** | 「選ぶと、練習記録が1時間ごとに自動で同期されます」 |
| プレースホルダ | **「例: 〜」または「〜を入力」** | 「例: 100m 11.2 (+1.5)」「場所名を入力」 |
| 成功メッセージ | **敬体・完了を伝える** | 「保存しました」「すべて既読にしました」 |
| エラー・失敗 | **§3の型**（何が起きた＋どうすれば） | 「保存できませんでした。もう一度お試しください」 |
| 確認ダイアログ | タイトル＝結果／本文＝敬体の補足 | 「ログアウトしますか？」「次に使うときは再度ログインが必要になります」 |

---

## 2. 用語の言い換え表（開発用語 → 部員向け）

**画面に出す文言では、左の語を使わず右へ置き換える。** コメント・変数名・DB列名・ログは対象外（そのままでよい）。

| 開発用語（禁止） | 部員向け（推奨） | 補足 |
|---|---|---|
| 反映（する／される） | **書き込む／表示される／登録する** | 「どこに」何が起きるかで動詞を選ぶ。スプシへ=書き込む、アプリ画面に=表示される、予定として=登録する |
| 同期 | **自動で書き込み／自動で取り込み**、または「連携」 | ボタン名など短くしたい所は「連携」可。「同期」単独は残さない |
| 取込・取り込み | **取り込み**（ひらがな送り）／「アプリに読み込む」 | |
| 書き戻し | **スプレッドシートに書き込む** | |
| 正（になる／が正） | 使わない。**動作で説明**する | 「アプリで記録すると、スプレッドシートにも自動で書き込まれます」 |
| 上書き（されます） | 原則出さない。必要時のみ「**新しい内容に置き換わります**」 | 「上書き」は不安語。挙動を淡々と説明する |
| 反映に失敗 | **書き込めませんでした** | |
| 発行（する） | **作る／作成する** | 「シートを発行」→「シートを作る」 |
| プルダウン | **選択欄／メニュー** | 「対象ブロックのプルダウンを自動設定」→「対象ブロックの選択欄をあらかじめ用意」 |
| 正常行／エラー行 | **問題のない行／エラーのある行** | |
| 未出力 | **未書き込み** | 管理画面のラベル |
| 競合スキップ | **重複のため見送り** | |
| 入力元 | **記録の保存先** | 「記録の入力元」→「記録の保存先」 |
| リコンサイル／切替前の同期 | 使わない。**「設定の変更」**でまとめる | |
| write-through / LWW 等の英語 | 画面に出さない | コード内コメントのみ |

> 迷ったら: **「主語をシステムにしない。部員の操作か、アプリ/スプシの動作で書く」**。
> 例）×「スプシが正になります」→ ○「アプリで記録すると、スプレッドシートにも自動で書き込まれます」。

---

## 3. エラー・失敗メッセージの型

**必ず「①何が起きたか ②どうすればいいか」の2部構成。**原因が内部的なものは部員に見せない。

- ○「保存できませんでした。もう一度お試しください」
- ○「スプレッドシートに『補強』の欄がなかったので、その項目だけ書き込めませんでした」
- ×「スプレッドシートへの反映に失敗しました（自動で再試行されます）」← 「反映」「再試行」が固い
- ×「HTTP 500」「同期の進捗情報がありません」← 内部エラーは「うまくいきませんでした。時間をおいてお試しください」に丸める

**部員本人に非がないエラー（同期の連続失敗など）は、対処できる運営者にだけ出す。**一般部員に「3回連続で失敗しています」とだけ見せない。

---

## 4. 運営者向け画面の扱い

運営者（権限者）だけが見る同期・シート発行・システム状態の画面も、**同じ用語表に従う**。「運営者は分かるだろう」で開発用語を残さない（運営者も多くは非エンジニア）。
ただし、集計値（取込 X / 更新 Y など）は「新しく取り込み X件 / 更新 Y件」のように**平易語＋件数**で残してよい。

---

## 5. 適用チェックリスト（この規約で直す既知の箇所）

2026-07-23 時点で洗い出し済み。上から優先度順（部員が見る頻度）。**下記は 2026-07-23 に一括適用済み（Claude）。**

- [x] `ProfileEditForm.tsx` … 「記録の入力元」→「記録の保存先」、説明文の「正／上書き／書き戻し」を動作説明に、切替失敗文言を平易化、「同期」→「連携」
- [x] `RecordForm.tsx` … 「反映に失敗」→「書き込めませんでした／あとで自動で試します」、「列が見つからず反映できません」→「欄がなかったので書き込めませんでした」
- [x] `NotificationsList.tsx` … 「同期が3回連続で失敗しています」→「スプレッドシートとの連携でエラーが続いています。設定を確認してください」
- [x] `safe-update.ts` … 「更新できませんでした。ページを再読み込み…」→「保存できませんでした。画面を開き直して…」
- [x] `SheetSyncButton.tsx` … 「取込／書き戻し／競合スキップ／失敗」→「取り込み／書き込み／重複のため見送り／うまくいかなかった人」、ボタン「同期」→「連携」
- [x] `SystemSyncStatus.tsx` … 「未出力／前回失敗／取込／出力」→「未書き込み／前回エラー／取り込み／書き込み」、「同期は正常」→「連携は正常」
- [x] `ScheduleSheetsManager.tsx` … 「発行」→「作る」、「プルダウンを自動設定」→「選択欄をあらかじめ用意」、「〜へ反映」→「予定に登録」、「正常行」→「問題のない行」
- [x] `MenuSheetImportManager.tsx` … 「反映」→「登録」、「正常行」→「問題のない行」

> 残す語（意図的）: 「連携」（同期の短い言い換えとして採用）、「取り込み／書き込み」＋件数（運営者向け集計値）、「スプレッドシート」。
> 未着手の周辺: `ScheduleCard.tsx`「スプレッドシートから最新表示」・`CommentSection.tsx`「スプレッドシートからの返信」は現状維持（意味が通るため）。

> 直したら各行を `[x]` にし、AGENTS.md の作業ログに1行残す。
> **文言だけの変更でも、実機（iOS PWA）で該当画面の表示崩れ（幅・改行）がないか確認する**（ガクつき禁止ルール／`docs/UX-ISSUES-2026-06.md`）。

---

## 6. 同じものは同じ語で（2026-07-30 追加・全画面棚卸しの結果）

§2 が「開発用語を平易語に」なのに対し、本節は **「同じ操作・同じ物を、画面ごとに別の言葉で呼ばない」** ための規約。
2026-07-30 に全1,477件の日本語リテラルを棚卸しし、12系統のゆれを解消したときの確定ルール。

### 6-1. ボタンの動詞

| 場面 | 語 | 例 |
|---|---|---|
| タイムラインに出るものを新規作成 | **投稿する** | 練習記録・つぶやき・大会・記録会の結果 |
| タイムラインに出ないものを新規作成 | **作成する** | 予定・お知らせ・練習メニュー・フォルダ・記事 |
| 既存を編集して保存 | **更新する** | 全フォーム共通 |
| 設定・一覧の保存 | **保存する** | 「この設定で保存」「このフォームを保存」など対象を繰り返さない |
| 進行中 | **{動詞}中…** | 保存中… / 作成中… / 削除中… / 送信中… / 公開中… |

- 「記録する」「追加する」「保存」「保存しています…」は使わない。
- 進行中の三点リーダは **`…`（U+2026）だけ**。`...`（半角ピリオド3つ）は禁止。
- FormModalFooter の主ボタンはスピナー（`LoaderCircle` + `animate-spin`）を必ず添える。

### 6-2. モーダルのタイトル

- **新規作成 = 対象の名詞のみ**（「予定」「お知らせ」「つぶやき」「フォルダ」「記事」「スレッド」）。
  動作はフッターのボタンが担うので、タイトルで繰り返さない。
- **編集 = `{対象}を編集`**。
- FAB の速攻メニューの項目名と、開くモーダルのタイトルは **完全に同じ語**にする。

### 6-3. 空状態

- 初回の空 … **`まだ{対象}はありません`**（助詞は「は」。「が」は使わない）
- 絞り込み結果ゼロ … **`条件に合う{対象}はありません`** ＋ 説明 `条件を変えてみてください。`
- 「条件に一致する」「該当する」「〜はまだ登録されていません」は使わない。
- `EmptyState` の `description` は**句点あり**。

### 6-4. エラー

- 見出しは §3 のとおり **`〜できませんでした`**。**`〜に失敗しました`は全廃**（API・lib の throw も含む）。
- **トースト**（自動で消える）は1文だけ。「もう一度お試しください」を付けない。
- **フォーム内エラー**（消えない）は `〜できませんでした。もう一度お試しください`（**末尾句点なし**）。
- フォーム送信の失敗は、対象を問わず **`保存できませんでした。もう一度お試しください`** に統一。

### 6-5. プレースホルダ

- **`例: 〜`（半角コロン＋半角スペース）** か **`〜を入力`** のどちらか。全角の `例：` は禁止。
- `〜を書く`・裸の名詞句（`集合方法、持ち物、連絡事項など`）・末尾の `…` は使わない。
- 選択欄だけ `〜を選択` を許可。
- 検索欄は **`検索`** の一語。何を検索するかは `aria-label` に書く。
- 例外（意図的・これ以外は増やさない）:
  - 練習記録の感想欄 `今日の練習を記録`（オーナー確定 2026-07-30）
  - URLの見本そのもの（`https://maps…`）
  - 数値欄の見本 `0`
  - 記録フォーム編集の**プレビュー**（入力できない見本）の `入力欄`

### 6-6. 語彙の統一

| 使う | 使わない |
|---|---|
| 絞り込み | しぼり込み |
| フォルダ | ノートフォルダ / フォルダ設定 |
| スプレッドシート | スプシ |
| 見出し | ヘッダー |
| 記録の保存先 | 記録の入力元 |
| 連携（できませんでした） | 同期（に失敗しました） |

### 6-7. 数と記号

- 人は **人**、それ以外は **件**。（`うまくいかなかった人 3件` のような取り違えを作らない）
- コロンは **半角 `: `**。全角 `：` は使わない。
- 数字と単位の間に空白を入れない（`3件`。ただし `3 / 12 件` のような比率表示は例外）。
