# 統一感の棚卸しと改善（2026-06-21）

実機レビューで「あちこち統一されていない」と指摘。横断的に直す。
規約の正は `docs/UI-UNIFICATION.md`。本ファイルは今回の是正リストと担当。

## 担当
- **Claude（横断UI）**: 送信ボタン統一・フィルタ共通化・学年フィルタ刷新・マイページ簡易表示。
- **Codex（予定/スプシ領域）**: 下記「Codex 是正ルール」。

---

## 確認できた不統一と是正

| # | 不統一 | 是正 | 担当 |
|---|--------|------|------|
| 1 | つぶやきの送信ボタンだけ `size="sm"`（他は `lg`） | 全フォームの主送信ボタンを **`size="lg"`** に統一 | Claude |
| 2 | 簡易表示がタイムライン/ホームのみ、マイページに無い | マイページの投稿一覧にも簡易表示トグル | Claude |
| 3 | メンバー一覧とタイムラインでフィルタの作りが違う | 学年フィルタを**同一部品**に統一。ブロック＝セグメントで共通 | Claude |
| 4 | 学年フィルタが独自プルダウンで使いにくい・不安定 | **シート式の複数選択**に刷新（規約「選ぶ＝シート」に準拠）。適用中は固定幅チップ | Claude |
| 5 | 通常入力とスプシ入力でタブ/項目が不統一 | 下記ルールで統一 | **Codex** |
| 6 | 予定の「既存を編集」が選択前提 | 下記ルール | **Codex** |

---

## Codex 是正ルール（明記）

### ルールC-1: 通常入力とスプレッドシート入力のタブ・項目を統一
- 予定まわりの「通常フォーム入力」と「スプレッドシート取込」で、**タブ名・項目名・並び順・用語を揃える**。
  - 種別の呼び方（練習/大会・記録会/記録会 等）を一語に統一し、両方で同じラベルにする。
  - 入力項目（日付・時刻・場所・対象ブロック・詳細 等）の**名称と順序を一致**させる。
- 「通常」と「スプシ」は**同じ役割の2手段**として、入口（タブ/ボタン）の見た目と文言を統一する。
- 対象: `src/components/post/ScheduleForm.tsx`, `src/components/features/ScheduleSheetsManager.tsx`, 取込プレビュー。

### ルールC-2: 予定の「既存を編集」は全選択スタート
- 既存予定から編集用シートを発行する導線は、**初期状態で対象を全選択**にする。
- 「個別に選ぶ意味が薄い」ため、可能なら**選択UI自体を簡略化**（既定＝全件対象、必要な人だけ絞る）。
- 対象: `src/components/features/ScheduleSheetsManager.tsx`。

---

## ガラパゴス化バックログ（2026-06-21・Codex監査を反映）

> 順序: 「仕様書を現状へ合わせ（済）→ 更新処理（済）→ エラー表示 → 入力部品」。
> **#1・#2 は Claude 対応済み。残り（#3〜#13）は Codex 担当。**

### ✅ 対応済み（Claude）
- **#1 仕様書と実装の食い違い**: `UI-UNIFICATION.md` §B/§4 を「⋯ ActionMenu＝現行標準」に修正。
  スワイプ案は「未採用の将来案」と明記。`UI-AUDIT.md` §3 の文脈FABを「実装済み」と明記。
- **#2 更新の無言失敗対策を共通化**: `src/lib/safe-update.ts`（`safeUpdate` / `safeUpdateMessage`）を新設。
  お知らせ/目標/プロフィール/つぶやき/練習記録の編集に適用済み。
  予定は既存インライン実装、結果/ロール/会場は `.single()` で0件検出済み。

### 残り（Codex 担当）

**高優先**
- **#4 更新後の再描画方式を統一**: `router.refresh()` / 楽観的更新 / ローカル即時 が混在。
  方針を1つ決めて明文化し寄せる（推奨: 既定は `router.refresh()`、件数表示など軽い反映のみローカル）。
- **#10 エラー通知を共通化**: `alert()`（例 `VenueManager.tsx:30`, `NoticeActions.tsx:20`）/ フォーム内 / 無表示 が混在。
  → **共通トースト**を新設し `alert()` を全廃。フォーム内バリデーションは現状維持で可。
  - 更新失敗時は `src/lib/safe-update.ts` の `safeUpdate`＋`safeUpdateMessage` を使えば文言も揃う（#2の基盤を流用）。

**中優先（入力部品の共通化）**
- **#5 トグル統一**: 共通 `src/components/ui/toggle.tsx` に寄せる（独自スイッチ＝`ResultForm.tsx:105`, `RoleManager.tsx`）。
- **#6 `<select>` 共通部品**: 生 `<select>`（予定 `ScheduleForm` / 管理 / スプシ `ScheduleSheetsManager`）を共通 `Select` 部品に。
- **#7 空表示を `EmptyState` に統一**: カード直書き（`home/page.tsx:107` 付近, `mypage/page.tsx:162` 付近）を置換。
- **#8 簡易表示・展開状態の一元化**: ホーム(`HomeFeed`)/タイムライン(`TimelineView`,cookie)/`ActivityFeed`(local) の
  状態管理・永続化方式がバラバラ。共通フック/部品に寄せる。
- **#9 フォーム下部ボタン**: 本文末尾配置を `FormModal` の固定フッターに寄せる（長尺・キーボード時の操作性）。

**低優先**
- **#11 `backLabel` 撤去**: `SubHeader` は表示しないのに呼び出し側がまだ渡している。各ページから削除。
- **#12 Linkify 拡大**: 会場アクセス情報・プロフィール目標も `Linkify`。
- **#13 未使用部品の確認削除**: `ScheduleComposer` / `NoticeComposer` が未使用なら削除。
  ※ `GradeFilter` / `ActivityFeed` は**現行部品**（削除しない）。

---

## 共通の原則（再掲・全員）
- 「書く＝全画面フォーム（主ボタンは `lg`）／選ぶ＝シート」。
- フィルタの適用中は**固定幅チップ**で可視化（`GradeChips`）。
- ガクつき禁止（`tabular-nums`＋固定幅＋0でも非表示にしない）。
- 同じ役割のUIは同じ部品を使う（フィルタ・カード・メニュー）。
