# UI・操作の統一ガイド（実装指示）

複数人・複数端末で積み増した結果、画面ごとに UI/操作がバラバラ（ガラパゴス化）している。
これを解消するための**確定方針**と**やることリスト**。今後はこの規約に沿って実装する。

> 運用ルール: `origin/master` が正。**force-push 禁止**。作業前に `git pull`。
> マイグレーションは新しいタイムスタンプで、**冪等**（`IF NOT EXISTS` / `DROP POLICY IF EXISTS` 等）に書く。

---

## 1. 確定した統一方針

### A. モーダルは「選ぶ＝シート / 書く＝全画面」で統一
- **ボトムシート（下から）= 軽い“選ぶ”操作だけ**
  - 例: FABの投稿種別選び、`⋯` の編集/削除メニュー、フィルター選択（学年など）、出欠者一覧の表示。
- **全画面モーダル = ちゃんと入力する“書く”フォーム**
  - 例: 練習記録 / つぶやき / 予定 / お知らせ / 大会結果(PB) / 会場 / 目標 / 練習メニュー の入力。
- 理由: フォームをシートに入れると高さ伸縮でガクつく・キーボードで隠れる・窮屈。全画面は高さ固定で安定。
- 既に `ScheduleForm` は全画面化済み（`src/components/ui/fullscreen.tsx` を使用）。これに他フォームも寄せる。

### B. 編集・削除は「⋯ ActionMenu」に統一（現行標準・2026-06-21 現状反映）
- すべてのコンテンツ（投稿・つぶやき・コメント・予定・大会結果・会場・お知らせ・ロール・ノート記事）で、
  右上などの **`⋯`（`ActionMenu`）→「編集」「削除」** に統一する。これが**現在の実装標準**。
- 「編集」で該当の**全画面フォーム**が開く。「削除」は **`ConfirmDialog` で確認**してから実行。
- 破壊的操作には必ず確認を挟む（誤操作防止）。

> 【未採用】「左スワイプ＋長押し＋Undoスナックバー」案が2026-06に検討されたが、
> **現状は未実装**。共通基盤（Undo）が無いため、当面は上の `ActionMenu` を正とする。
> 採用する場合は全画面横断で一括置換し、中途半端に混在させないこと。

### C. 進め方
1. まず**共通部品**と本ガイド（規約）を整える。
2. 以後は**必ず共通部品で作る**。
3. 既存画面は**主要画面から順に**共通部品へ寄せる（いきなり全部はやらない）。

---

## 2. 共通部品（`src/components/ui` に集約）

### 既存（流用）
`Button` / `Card` / `Input` / `Textarea` / `Badge` / `Sheet`(=選ぶ) / `FullScreen`+`FullScreenContent`(=書く) / `SegmentedControl` / `Toggle`

### 追加・標準化する部品
- **`FormModal`**: 全画面フォームの共通ガワ。`FullScreenContent` をベースに、
  ヘッダー（×＋タイトル）／スクロール本文／（必要なら）下部固定の保存ボタン。**全フォームはこれを使う**。
- **`ActionMenu`**: `⋯` を押すと出るシート。`編集` / `削除`（破壊的は赤＋確認）を共通化。
  props 例: `onEdit?`, `onDelete?`, `editLabel`, `deleteLabel`。
- **`Disclosure`**: `▾` で開閉する共通の折りたたみ（予定のアクセス、グラフ詳細などで使う）。
- **`KeyValue`**: 「ラベル：値」の項目表示（結果・補強・感想・参加費など）。本文は `Linkify` 経由＋`whitespace-pre-wrap`。
- **`EmptyState`** / **`Skeleton`**: 空状態・読み込み中の共通表現。
- 入力系の見た目（余白・角丸・高さ）は `Input`/`Textarea`/`Card` の基準に必ず合わせる。
- 日付/時刻入力は `globals.css` で iOS のはみ出し対策済み。`type="date/time"` をそのまま使う。

---

## 3. （2026-09-13 削除）当時のTODO一覧

2026-06 時点の小修正リストはすべて完了したので削除した（下ナビ5タブ化、ホームの挨拶撤去、
マイページのリンク整理など）。**このファイルにTODOを置かない**。やることは AGENTS.md の
実装バックログへ書く。ここは「今どうなっているか」の規約だけを書く。

---

## 4. 各画面の寄せ方チェックリスト（2026-06-21 現状反映）

> 主要部は実装済み。以下は到達状況。

### フォーム＝全画面（FormModal）… 実装済み
- 記録/つぶやき/結果/お知らせ/ノート記事は FAB から `FormModal`（全画面）で開く。予定も全画面。
- メニュー・目標・会場の各フォームも全画面/モーダルで統一。

### 編集・削除＝`⋯ ActionMenu` … 実装済み（現行標準。§B 参照）
- 投稿/つぶやき/コメント/お知らせ/予定/大会結果/会場/ロール/ノート記事で `ActionMenu` を使用。
- 削除は `ConfirmDialog` で確認。

### フィルター/タブ … 実装済み
- ブロックは `SegmentedControl`、学年は `GradeFilter`（シート式・共通部品）。
- タイムラインの学年絞り込みは撤去済み（§6）。`GradeChips` は現存しない。
- 予定の種別タブ・スプシの種別タブは `SCHEDULE_TYPE_OPTIONS` で統一。

### 残課題（共通化バックログ）
空表示（`EmptyState` を使わず直に書いている箇所が数件）、`<select>` の共通部品化など。
当時の一覧だった `docs/CONSISTENCY-AUDIT.md` は内容が古くなったため2026-09-13に削除した。
残りは AGENTS.md の実装バックログと `docs/ARCHITECTURE-REFACTOR-PLAN.md` Phase 3 を参照。

---

## 4.5 システム/アーキテクチャの統一（UIだけでなく仕組みも揃える）

画面ごとに取得・更新・キャッシュのやり方がバラついているので、以下を標準とする。

### データ取得
- 画面の**初期データは Server Component + `src/lib/queries/` の関数**で取得する。画面に直接 supabase クエリを書かない。
- **新しいクエリは必ず `lib/queries/` のドメイン別ファイルへ**（重複・流儀の分散を防ぐ）。
  入口は `@/lib/queries` のまま。2026-09-13に1ファイル1183行から分割した（`docs/ARCHITECTURE-REFACTOR-PLAN.md`）。
- クライアントでの追加取得（操作起点のもの：会場リスト、コメント、出欠など）はブラウザ用クライアントで。だが定義は使い回せる形に。

### DBへの往復を直列に積まない（2026-09-13 追加・重要）
バックエンドが所有者PCにある間、1回の往復は数百ミリ秒かかる。**直列に待った回数が
そのまま表示の遅さになる。**

- **自分のIDだけで引ける問い合わせは、プロフィールの取得を待たない。**
  `getCurrentUserId()`（`lib/supabase/auth.ts`）はcookieを読むだけでDBを使わない。

  ```ts
  const userId = await getCurrentUserId();
  const [profile, feed] = await Promise.all([getCurrentProfile(), getFeed(userId)]);
  ```

- 互いに依存しない取得は必ず `Promise.all`。前の結果が要るのは「本当にその値を使うとき」だけ。
- 新しい画面を足したら「最初の表示までにDBへ何回**直列で**行くか」を数える。2回を超えるなら設計を見直す。

### キャッシュ / 表示速度（“重いページの標準形”）
- **`experimental.staleTimes` と `cacheComponents` は使わない**（`next.config.ts` のコメント参照）。
  2026-07-12のiOS PWA全面フリーズの原因。速度はこの節の他の手段で担保する。
- **動的ルートには必ず `loading.tsx` を置く。** これがあると `<Link>` の先読みが
  「共通レイアウト＋スケルトンだけ」になり、ページの取得処理＝DBへの問い合わせは走らない。
  無いルートは先読みそのものが行われず、タップしてから何も起きない時間が長くなる。
- 主要な遷移リンク（下タブ、管理メニュー等）は**先読みを切らない**（`prefetch={false}` を付けない）。
  一覧の中の大量のリンク（記事・部員など）は資源の無駄なので `prefetch={false}` のままでよい。
- 先読みが間に合わないときのために、下タブは `useLinkStatus` で**押した瞬間に選択状態**にする。
- 重いセクションは **`Suspense` でストリーミング**し、ガワ（見出し等）を即表示。
- → 「重い一覧を待ってからページ全体を出す」をやめ、**軽い所を先に出して重い所を流し込む**を全画面で標準化。

### 更新（ミューテーション）
- **保存・編集・削除の成功後は `router.refresh()` を既定**とし、Server Component の表示を正しい状態へ戻す。
- いいね・リアクション・コメント件数・展開状態など、現在の画面だけで完結する軽い反映はローカル state で即時更新する。
- 一覧の追加・削除をローカルで即時反映する場合も、他セクションや権限表示へ影響する操作は最後に `router.refresh()` する。
- セッション維持などユーザー操作以外を起点とした `router.refresh()` は禁止。`SessionKeepAlive` は SIGNED_OUT のみ refresh する。
- 破壊的操作は確認を挟む。更新失敗は `safeUpdateMessage`、その他の操作失敗は共通トーストで通知する。

### 認証 / セッション
- 検証・更新は **proxy(`getUser`)**、ページは **`getCurrentProfile`(`getSession`、ネット往復なし)**。
- `SessionKeepAlive` は自動更新（startAutoRefresh）は維持しつつ、**TOKEN_REFRESHED では refresh しない**（SIGNED_OUT のみ）。
- データのアクセス制御は最終的に **RLS** が担保（UIで緩めても安全）。

### 権限
- サーバー側の出し分けは **`permissionsOf(profile.roles)`**、DB側は **`can_*()` 関数で RLS**。
- **UIガードとRLSの両方**で必ず守る（片方だけにしない）。

### その他の仕組み
- 新バージョン検知（`VersionWatcher` → 更新バナー）を全体で維持。
- Realtime は**お知らせベルの未読数だけ**で使う（`NotificationBell.tsx`）。他へ広げるときは別途方針を決める。
- エラー/空/読み込みの表現は `EmptyState` / `Skeleton` に共通化（UIセクションと対）。

---

## 5. 用語メモ
- 「モーダル」= 入力時に上に出るパネル（シート or 全画面）。
- 「シート」= 下からせり上がる小窓（`Sheet`）。
- 「全画面」= 画面全体を覆う入力画面（`FullScreen`）。

---

## 6. 投稿の高さと絞り込み（2026-08-29 改訂）

### 投稿の高さは投稿自身が決める（「簡易表示」は廃止）
かつて「詳細表示／簡易表示」の切替トグルがあったが、2026-08-29に**廃止した**。
同じ名前で中身が画面ごとに3種類あり、初期値もばらばら、切り替えると開いていた投稿が
全部閉じてしまっていた。主要SNSはどこも密度の切替を持たず「1投稿の高さに上限＋続きを読む」で
一覧性を作っているため、その形に寄せた。

- `src/components/common/ExpandableSection.tsx` が本文に高さの上限を与え、
  下端をぼかして「続きを読む」を出す。**収まる投稿にはボタンを出さない。**
- 練習記録の詳細は150px、つぶやき本文は140px。
- `compact` prop・`CompactFeedRow`・`useFeedDisplay` などの密度切替の仕組みは削除済み。復活させない。
- タイムラインの新着位置は `NewPostMarkers.tsx`（「新着の投稿 N件」と「以前の投稿」）。

### 絞り込み
- ブロック（全体・中長距離・短距離）は**常時表示の `SegmentedControl`**。
- 人物を選ぶ場面の絞り込みは共通部品（名前検索＋複数ブロック＋複数学年）。
  学年は**在籍者がいる学年だけ**出す（0人の学年は出さない）。
- **タイムラインの学年絞り込みは2026-08-29に撤去した**（利用頻度が低く、場所を「一覧」表示ボタンに譲った）。
  学年で絞るのは人物選択とメンバー一覧。使う部品は `src/components/features/GradeFilter.tsx`。

---

## 7. 確定ルール追補（2026-06-21 策定・現在も有効）

### ガクつき禁止（全コンポーネント共通の鉄則）
選択や数値で要素の幅/高さが変わるのは禁止（安っぽい）。出欠・チップ・トグル・カウンタ全部に適用。
1. 数字は `font-variant-numeric: tabular-nums`。
2. 最長文言ぶんを `min-width` で確保（「出席/欠席」等の切替で伸縮させない）。
3. 0でも要素を消さない（`visibility:hidden` で場所だけ残す。`display:none` 禁止）。
4. 状態差は**色だけ**で表す（幅を変えない）。
5. `contain: layout` で波及を隔離。
6. 読み込み中はスケルトン。

### フィルタの「適用中」表示＝固定幅チップ
- 適用中の学年等は外せる**固定幅チップ**（`B1` `M1`…）。上のガクつき禁止に準拠（tabular-nums＋min-width）。
- **学年フィルタは在籍者がいる学年だけ動的表示**（0人は出さない）。

### メンバー一覧・ロール管理は学年でグループ分け
- B1/B2/…/M1/M2/D1… の見出しで区切る。名前検索も追加。

### 練習記録フォームはブロック別（2系統）
- 中長距離＝現行 / 短距離・跳躍・投擲＝共通（メニュー・目的/意識・タイム・感想）。
- 出し分けの初期値は**プロフィールの専門種目（所属ブロック）**。プロフィール編集に専門種目設定を追加。

### スプレッドシート取込のプレビューは編集可能に
- 取込前プレビュー＝**その場で直せる表**（行単位エラー表示→修正→そのまま取込）。元シートへの往復をなくす。

### 横断
- 二重送信防止（保存中は無効化＋「保存中…」）。無言update対策を全更新へ横展開。
- 破壊的操作の確認は `ConfirmDialog` に統一。タップ領域は最低44px。

---

## 8. 押したときの見え方（2026-09-13 統一）

押下フィードバックは**2種類だけ**。ここから増やさない。

| 対象 | 付けるもの |
|---|---|
| 面（カード・一覧の行・リンク行） | `active:bg-bg` で背景を変える |
| アイコンだけ・文字だけのボタン | `pressable`（`globals.css`） |

- `pressable` は 0.12秒のトランジションと `opacity: 0.78` を持つ。
  共通の `Button` もこれを使うので、**濃さを変えたいときは globals.css の1か所だけ**を直す。
- **`active:opacity-◯◯` を新しく書かない。** 2026-09-13以前は 50 / 60 / 70 / 80 / [0.78] が
  混在していて、同じ種類のボタンでも画面ごとに押した感じが違っていた（57箇所を `pressable` へ統一）。
- 拡大縮小（`active:scale-*`）は使わない。2026-07-23に全廃した（安っぽく見え、ガクつきの原因になる）。
- `prefers-reduced-motion: reduce` ではトランジションを切る（`globals.css` で対応済み）。
