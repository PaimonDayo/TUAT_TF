# 大会表示・アーカイブ・エントリー準備

> **2026-09-25更新（この文書の下の「本番反映禁止」より優先）**: オーナー指示で、アーカイブ（27大戦は9/25 08:00 JSTでアーカイブ済み）と大会ページの整理、OB戦の入口統一を本番反映した（be5d873）。関東新人の番組取込は取りやめ、コードとmigrationはリポジトリから退避した。

追記: この後、OB戦だけは「今回はシステムロール限定」と本番反映の指示があり、
競技エントリーの限定公開を実施した。詳細は [OB戦の限定公開](OB-ENTRIES-2026-09-24.md)。
以下の27大戦・関東新人の準備変更は引き続き未反映。

## 本番反映禁止（オーナー指示）

2026-09-24の作業中に「まだ本番実装はしないで」と指示された。
この変更はローカル準備のみ。commit/push、migration適用、DB書き込み、デプロイ、
明朝に本番を変更する自動処理の登録は行っていない。
改めて本番反映の指示があるまで実行しない。

## ローカル変更

- 大会ページの同じプログラムへの重複リンクを除去し、「プログラム」に統一。
  本人が登録したPB記録は別データなので「部員が登録した記録」と明示して保持。
- `competitions.archive_at` を追加するmigrationと、日本時間の日時指定フォームを準備。
  指定後もプログラム・目標・PB記録は保持し、大会一覧のアーカイブから閲覧できる。
  ホームとcron自動取得からは対象を外す。管理者の明示的な再取得は可能。
- 27大戦は2026-09-25 08:00 JSTをmigrationに設定。**未適用なので自動では切り替わらない**。
  現在のホーム選択大会がアーカイブになったときだけ、開催中・次回大会を日程順に選ぶ。
  全ホーム選択を明示解除した場合は非表示を維持。
- 関東新人の大会設定migrationと、関東学連の競技者索引→農工大→競技ページの取込を準備。
  取得先を公式ドメイン・パスへ限定。人数、期間、表形式、既存競技の欠落を検査し、
  失敗時は保存済みプログラムを置換しない。資格記録は結果にしない。

公式: https://www.kgrr.org/competition/?id=145
番組: https://www.kgrr.org/event/event/2026/37Departure_beyond/kyougi.html

2026-09-24に直接取得した公式HTMLで農工大3人・3種目を解析確認。
9/25 10:00女子800m予選3組、9/25 14:40男子110mH予選6組、9/27 10:00女子ハンマー投決勝。
実際の結果表への切替は大会前のため未確認。未知の形式は失敗として既存情報を保持する。

## OB戦

指定された回答CSVは読み取りのみ。本人が同意した「最新回答＋末尾の追記」で集計。
空白・全半角を正規化して同名の最新日時を選択。学部・修士の在学生表記で現役候補を抽出。
部員名簿との照合は未実施。氏名・学年が不明な回答を推測で補完しない。

再実行用スクリプトは `ops/prepare-ob-entries.py`。
個人データと確認用HTMLはGit対象外の `.contingency/ob-entries-2026/` に保存。
公開リポジトリへ回答データを追加しない。懇親会だけの回答を競技参加者に数えない。
アプリDBへの取込と本番掲載は未実施。競技未回答者も、確認用一覧では未回答と明示して残す。
作業中のオーナー補正はGit対象外の `corrections.json` に記録し、CSVのSHA256が一致する場合だけ適用する。
確認用には懇親会の最新回答・追記も集計。`application-entries.json` は氏名・学年・種目だけで、
競技未回答者と懇親会情報・自由記述を含めない。

## 検証と残事項

- アーカイブの日本時間境界、次大会選択、公式番組解析、資格記録の誤取込防止、欠落時の拒否をテスト。
- 全テスト、対象ESLint、TypeScript、PC本番設定を使ったローカルbuildを実施。
- OB戦確認用HTMLをローカルブラウザで確認。個人データは本番へ転送していない。
- DB migrationの適用・権限確認と、本番/実機の大会UI確認は未実施。
  本番反映が指示された場合も、先にバックアップ・migrationの隔離検証・実データ件数照合を行う。
