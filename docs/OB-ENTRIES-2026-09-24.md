# OB戦エントリーの限定公開

## 対象部員の訂正（同日追加・最新）

所有者の意図は学年制限のみの解除だったため、補助員表・CSVからエントリー未確認の部員を再度除外。B3以上も含め、競技回答がある現役部員を対象とする。未照合の回答は本人照合後に担当登録できる。既存担当は削除しない。対象9テスト・eslint・TypeScript付き本番設定buildで確認。

## PCの横スクロール・現役全員を候補に（同日追加）

共通CSSでスクロールバーの幅/高さが0だったため、補助員表に限り12pxのスクロールバーを表示。所有者指定で左右移動ボタンは設けない。氏名列の固定と枠からの担当編集は維持。追加指示によりB3以上・エントリー未確認も含む現役部員全員を候補に戻した。OB・OG回答は対象外。migration 20260924110000をPC本番適用し、学年/エントリー制限を解除、在籍とシステム権限の検証は維持。既存担当を保持し、試験書き込みはrollbackした。

433テスト（修正した表示順期待値は対象再実行）、対象eslint・TypeScript付き本番設定build成功。合成UIでスクロールバー表示・横移動・移動ボタン撤去・全学年/未エントリー部員の候補表示を確認。

## 同時刻の補助員担当を種目別に分離（同日追加）

所有者の指示でジャベリックスロー/立ち五段跳び、100m/砲丸投げ、やり投げ/走り幅跳びを別枠に分離。12種目それぞれの担当を登録・編集・解除でき、CSVも種目名付きの27列とした。同時刻の他種目への出場は引き続き表示し、担当割り当て時に確認できる。

`20260924100000_ob_duty_events.sql` を暗号化バックアップ後にPC本番へ適用。担当の主キーとRPCに種目を追加し、種目/開始時刻の対応を検証する。既存1500mの担当5件は内容・revisionを保持。種目が判別できない旧合同枠にデータがあれば移行を停止する。DBで同時刻の2種目への登録・片方だけの解除・別種目保持・権限・競合・既存データ保持をrollback検証した。全433テスト・eslint・TypeScript付き本番設定build成功。合成UIで12枠表示、砲丸投げの入力と同時刻100m出場の表示、保存完了を確認。システム限定を維持。

## 補助員の対象とタップ領域（同日追加）

所有者の指定に合わせ、補助員表とCSVは競技エントリーがあるB1・B2のみとし、B3以上とエントリー未確認の部員を除外。未照合の競技回答は本人照合後に担当登録する。各枠の全体をボタンにし、空き枠を直接タップして追加、担当のある枠から編集できる。

`20260924090000_ob_helper_eligibility.sql` をPC本番に適用し、保存時にも学年と競技回答の存在を検証する。所有者指定の懇親会回答1件の区分を「OB・OG等」に訂正し、出欠など他の列は保持。氏名を含む訂正スクリプトはGit対象外。暗号化バックアップ・rollback付きDB検証後に適用。全432テスト・対象eslint・TypeScript付き本番設定build成功。320px合成UIで対象行の絞り込み、枠全体のタップ領域、空き枠からの追加保存、横はみ出しなしを確認。

## 補助員の登録・編集（同日追加）

補助員表の「回答なし」を「エントリー未確認」に変更。本人に紐付いた競技回答がない状態と、その時間帯に「出場登録なし」の状態を区別する。未照合の回答は別行で表示する。

システムロールは部員・開始時刻ごとの担当欄から補助員の追加、編集、解除ができる。共通FormModalを使用し、同時刻の出場登録を表示、未保存の破棄と解除を確認する。未照合の回答行は本人照合後に登録する。CSVは各時間帯の出場予定と保存済み担当を含む21列。

`20260924080000_ob_duties.sql` を暗号化バックアップ後にPC本番へ適用。担当は独立テーブルに保存し、システム限定のRPCで入力・在籍・更新競合を検証する。解除後もrevisionを保持し、古い画面からの上書きを拒否する。実DBで追加・編集・解除・同時更新競合・他時間帯保持・匿名/一般部員の操作拒否を検証し、試験変更をロールバック。既存の競技回答・懇親会回答は全列不変。

全431テスト、対象eslint、TypeScript付き本番設定build成功。320pxの合成UIで編集保存・解除確認・一覧復帰・横はみ出しなしを確認し、ダウンロードCSVの21列と担当内容も確認。iOS実機は未確認。公開範囲は引き続きシステムロール限定。

## OB・OGのプログラム追加（同日追加）

所有者の依頼で、元CSVの最新回答からOB・OGの競技回答21人64種目を追加。資格記録28欄（空欄を含む）を対応する種目に保持し、出場区分は既存DBトリガーで保存。名前欄の明示的な出欠変更を統合済みの回答行を用い、懇親会だけの参加者や競技未回答者は競技登録しない。アプリには「OB・OG」の区分で表示し、現役と同じ開始時刻/種目に並べる。

全体85人238種目、既存64人の競技回答・資格記録・本人紐付け61件は全列不変。懇親会21件は同じ回答の競技IDへ関連付け、出欠や氏名を変更しない。個人データと取込スクリプトはGit対象外。再取込は既存行を上書きしない。
OB・OGを現役名簿の本人照合や補助員検討表へ混ぜないようにした。システムロール限定を維持し、一般部員・匿名の非公開も実DB確認。暗号化バックアップ、rollback付き取込検証後に適用。全427テスト・対象eslint・TypeScript付き本番設定build成功。既存のプログラム行コンポーネントを利用し、レイアウト変更なし。


## 予定・懇親会・新規回答・補助員検討表（同日追加）

所有者が指定した09:00受付〜16:00閉会の13項目を時刻順に掲載。11種目を既存の種目名へ対応付け、各競技を開くと男女別の出場者・資格記録を確認できる。15:30の4×300mリレーは当日エントリーと明記し、事前登録種目には追加しない。予定は提示された時刻をそのまま使い、終了時刻や大会日付は推測していない。

懇親会は19:00〜、ミライザカ 府中並木通り店、3,500円。最新回答＋追記を適用し、氏名欄に明示された不参加への変更1件を元の回答と統合。参加64人、不参加27人、未回答1人、別に氏名未確認1件を取込。確認待ちを参加人数に含めない。元CSVと集計・取込データはGit対象外。
`20260924070000_ob_party.sql` をPC本番へ適用。懇親会は別テーブルでシステム限定RLS・RPCを使用し、既存競技64人の回答IDへ紐付けた。既存競技データの全列を保持。懇親会だけの回答、OB/OGの回答も一覧・検索・出欠編集に対応。氏名未確認行の編集は保留。

新規フォームでは在籍部員を選択し、競技と懇親会を一括保存する。懇親会だけでも登録でき、後から競技追加時に出場区分を選べる。既存の懇親会回答が氏名一致する場合は確認文を表示し、保存時に対応付ける。既存回答と別人の関連付け・二重登録はDBで拒否。競技と懇親会のrevisionを検証し、競合時に片方だけ保存されないトランザクション構成。Googleフォームへの書き戻しや一般部員への公開は行わない。

補助員表は在籍部員×9開始時刻の出場予定。氏名列・時刻行を固定し表内でスクロール、氏名/学年検索、CSV出力を追加。CSVには担当記入欄と注意書きを含む。未照合の回答は別行で残すため、照合前の行数は人数ではない。「出場登録なし」「回答なし」「当日確認」を区別し、アップ・移動・終了時刻が不明なため担当可能とは断定しない。今回のアプリは検討表までで、担当割当の保存は未実装。

検証：全426テスト・対象eslint・TypeScript付き本番設定build成功。暗号化バックアップ後、二重migration・取込件数・競技全列保持・出欠/競技の原子的保存・古いrevision拒否・重複拒否・保留回答拒否・懇親会のみ新規作成後の競技追加・匿名/一般部員拒否を実DBで確認。試験変更はrollback。390px/320px合成UIで予定順、懇親会一覧/編集、新規フォーム、既存出欠の候補表示、補助員表内スクロール・ページ横はみ出しなし、実CSVダウンロードと13列/UTF-8 BOMを確認。iOS実機は未確認。


## 27大戦に合わせたUI再構成・出場区分の固定（同日追加）

種目別一覧を27大戦のプログラムと同じ、トラック/フィールドの見出し、区切り線付きカード、種目をタップして出場者と資格記録を開く構成へ変更。検索中は該当種目を展開する。切替は共通SegmentedControl、編集は共通の三点メニュー、入力はFormModalに統一した。
編集は種目のチェックと直下の資格記録入力を同じ一覧にまとめた。保存と件数は下部固定、未保存の破棄・種目の取り消し・入力中の区分変更は確認を出す。履歴の画面は引き続き設けない。

出場区分は名前やアカウントから推測しない。既存回答の種目から男子/女子を引き継ぎ、本人照合でも区分を表示して確認する。初回登録は区分を未選択から選び、該当する11種目だけ表示する。保存後は区分を固定し、全種目を取り消しても保持する。
`20260924060000_ob_entry_division.sql` をPC本番へ適用。DBトリガーでも区分変更・男女の種目混在を拒否し、既存の保存RPCと権限を維持。取消済みの回答は直近の非空の登録履歴から区分を復元する。既存に区分混在があれば移行を中断する。区分そのものの訂正が必要な場合は個別確認が必要で、本人のアカウントを付け替えて区分を変える仕様ではない。

64人全員の区分を確定でき、本人紐付け61人を含む元の全列は不変。暗号化バックアップ後、移行の二重適用・全既存回答の逆区分拒否・取り消し/再登録・新規登録・直接変更拒否・一般部員/匿名の権限を実DBで検証。試験変更はすべてロールバックし、区分列と制約のみ反映。
全418テスト、TypeScript、対象eslint、本番設定build成功。合成データの実コンポーネントで390px/320px、種目展開・共通編集メニュー・資格記録入力・種目選択・未保存確認・新規区分選択を確認。320pxで横はみ出しなし。iOS実機は未確認。公開範囲はシステムロール限定。


## 種目別一覧と編集UIの整理（同日追加）

所有者の指示で初期表示を「種目別」に変更。各種目の氏名・学年・資格記録をコンパクトな表にまとめ、その行から編集できる。種目絞り込みと検索に対応し、「自分」「本人照合」は独立したタブに分離した。取り消し済みの回答は「自分」または「本人照合」から再エントリーできる。

編集フォームの種目プルダウンと追加ボタンを廃止し、男女切替＋種目の複数選択ボタンに変更。タップで選択/解除し、選択した種目の資格記録をまとめて入力する。選択件数と保存ボタンは下部固定。既存の入力保持、未保存確認、取り消し確認、保存時の競合検知は維持。

変更履歴の画面・呼び出し・Server Actionを撤去した。過去のDBデータや保存処理は今回変更していない。公開範囲はシステムロール限定のまま。
全417テスト、TypeScript、対象eslint、本番設定build成功。合成UIを390px/320px幅で確認し、種目別の比較・検索・行からの編集・ワンタップ追加・固定保存ボタン・横はみ出しなしを確認。iOS実機は未確認。

## エントリー編集（同日追加）

所有者の依頼で、追加エントリー、種目の追加・一部/全種目取り消し、資格記録編集、再エントリー、変更履歴をシステムロール限定で追加。
全画面フォームで最後に保存し、種目の取り消し・未保存で閉じる操作は確認を出す。資格記録は種目ごとの任意の自由記述（1000文字以内）。
追加エントリーでは在籍部員を選択し、既に紐付いた回答がある場合はその回答を編集する。正規化した氏名に一致する既存回答も重複追加せず、先に本人照合する。

`20260924050000_ob_entry_edit.sql` をPC本番に適用済み。通常のテーブル直接更新は引き続き本人IDとrevisionだけで、競技内容はシステム限定RPCから変更する。DBでも22種目の許可リスト・記録の長さ/種目との対応・重複・在籍部員・revisionを検証する。種目0件を取り消し済みとして保持し、本人IDや回答のIDを削除しない。
追加・編集と同じトランザクションで、変更者・日時・変更前後を追記履歴へ保存する。履歴の直接追加/変更/削除は一般ログインに許可せず、閲覧もシステム限定。画面では最新20件を表示する。履歴は編集機能の導入後からで、取込前のフォーム履歴は含まない。

変更はアプリ内のみで、Googleフォームや大会主催者への送信は行わない。今後CSVを再取込する場合はアプリ編集と突き合わせ、フォームの旧回答で上書きしない。
本番適用前の暗号化バックアップと、ロールバック付き実DB検証で追加・編集・取り消し・再エントリー・競合・重複・履歴内容・権限を確認。検証用変更は回収し、既存回答の全列不変・履歴0件を確認して適用した。
全417テスト、TypeScript、対象eslint、本番設定build成功。320px幅の合成UIで全画面編集、複数行入力、未保存確認、取り消し確認と保存後の終了を確認。iOS実機は未確認。

## 資格記録・照合の再利用（同日追加）

所有者の追加依頼で、初期表示を「自分のエントリー」に変更し、紐付いた本人の出場種目と資格記録を表で表示する。「全員・本人照合」で従来の管理一覧へ切り替える。
資格記録は最新回答の原文を保持し、83件の申告記録・29件の未回答・62種目の記録欄なしを区別する。未選択種目の記録から出場種目を増やしたり、資格記録を大会結果に転用したりしない。

`20260924040000_ob_entry_qualification_marks.sql` をPC本番へ適用済み。取込は名前・学年・種目の一致を確認し、既存の61人の本人IDとrevisionを含む元の全列を保持した。閲覧・更新の権限はシステム限定のまま。

照合は共通の `entry-identity.ts` に分離した。別の大会・フォームからも氏名、学年、在籍部員、過去の確認済み対応を渡して利用できる。別開催回の `meet_key` に保存された確認済み対応も候補に使い、進級・表示名変更に対応する。同名の別人や過去の対応との衝突は複数候補として残し、自動確定しない。今後の開催回は別の `meet_key` で取り込み、画面の対象開催回を切り替える。今回、将来の大会や架空データは登録していない。

追加検証: 全412テスト、TypeScript、対象eslint、本番設定build成功。実DBでロールバック付きの取込検証・本人ID保持・匿名/一般部員の閲覧拒否・システムの閲覧・資格記録の直接変更拒否を確認。合成UIの320px幅で本人だけの種目と記録・未回答・記録欄なし・一覧切替・横はみ出しなしを確認。iOS実機は未確認。

## 初回公開

2026-09-24、所有者の「今回はシステムロール限定」の指定に対応。
マイページのシステム管理メニューから `/ob-entries` を開く。
氏名・学年・種目で検索でき、確認済みの本人だけに絞り込める。

## データと本人照合

- 同じ人の最新回答に確認済みの追記・学年訂正を反映した競技エントリー64人、延べ174種目。
- 氏名未入力、種目未回答、卒業生、懇親会、自由記述の記録欄は今回の取込対象外。
- CSV、集計結果、部員IDとの照合表は公開リポジトリに含めない。
- 氏名の空白・全半角を正規化し、学年も比較する。完全一致44人、表示名の確認が必要20人だった。
- 候補提示だけでは保存せず、システム管理者が確認して `profiles.id` に紐付ける。表記変更後もIDで対応を維持する。
- 同名・学年違い・未一致は明示し、全在籍部員から手動選択できる。1大会で同じ部員の二重紐付けは不可。競合時は更新を拒否して再読込を案内する。
- この取込は回答CSVのスナップショット。Googleフォームとの自動同期ではない。追加回答の再取込時は、確定済みの本人IDを保持し、差分を確認する。

## 権限と本番DB

`20260924030000_ob_meet_entries.sql` をPC本番に適用済み。
画面・更新処理・RLSで `manage_system` / `can_manage_system()` を確認する。
一般部員のプレビュー中も画面と更新処理を拒否する。
匿名はテーブル取得不可、一般部員には0件。システム管理者だけが取得・本人ID更新できる。
通常ログインでは元回答の変更・追加・削除はできない。
一般部員への公開は別途判断する。27大戦のアーカイブ・関東新人の準備変更は本リリースに含まない。

## 検証

- 暗号化バックアップ作成・R2保存検証後、実DBで移行・取込・権限検証をロールバック実施してから本適用。64人・174種目・自動紐付け0件を確認。
- 匿名取得拒否、一般部員の取得0件・更新0件・追加拒否、システムの取得・紐付け・解除、二重紐付け拒否、元回答変更・削除拒否、revisionによる競合検知を検証。検証用の紐付けはロールバック。
- アプリ全403テストと追加のServer Action 5テスト、TypeScript、対象eslint、本番設定のローカルbuild成功。
- 実コンポーネントの合成データ画面で390px/320px幅、候補展開、本人絞り込みを確認。320px幅で横はみ出しなし。iOS実機は未確認。
