## 2026-09-25 現在の状態（下の9/24以前の記述より優先）

Claude Opus 5.5: オーナー判断を反映。
- **外部メール監視は取りやめ**（実装の手間に見合わないため）。監視API・GAS・配置スクリプト・DB関数のファイルは `D:\TUAT_TF_removed-20260925\` へ退避（Gitには入れない）。本番DBの `operational_health()` は削除済み（migration 20260925120000）。監査の項目4は「取りやめ」。Vercel環境変数 `MONITOR_SECRET` と、Codexが作った独立GASプロジェクトは未使用のまま残っている。
- **関東新人の番組取込は取りやめ**（エントリーが少ないため）。取込コードとmigration 20260924020000は同じ退避先へ移し、未適用のまま。
- **27大戦はアーカイブ済み**（archive_at=2026-09-25 08:00 JST、PC本番DBへ適用）。ホームには次の大会が自動で出る。大会一覧の「アーカイブ」から閲覧可。
- **OB戦は27大戦と同じ入口・見た目に統一**: 大会一覧 → OB戦 → プログラム（システム限定）。旧 /ob-entries は転送のみ、マイページの専用行は撤去（be5d873）。
- **大会の結果は1か所に**（オーナー確定）: プログラムがある大会（27大戦など）の大会ページは「プログラム・結果」だけにし、部員登録の記録一覧と公式結果一覧は出さない。プログラムが無い大会は部員登録の記録を「結果」として一覧。本名確認済みの公式結果（competition_result_links）を各部員のページへ出す案は一度入れたがオーナー判断で取りやめ、読み取りはシステム限定に戻した（migration 20260925140000、データ72件は保持、今は画面で使っていない）。
- **大会は全員に公開**: マイページの一般メニューに「大会」（一覧・アーカイブ）。追加・編集・並べ替えは画面内で管理者だけ。OB戦のプログラムはシステム限定のまま。プログラムの日付は初日が上。
- **目標画面は1大会だけ**（オーナー確定）: 大会ごとの目標画面から他大会への切替タブを撤去。先頭に「自分の目標」、下に「みんなの目標」を種目ごと（ブロック切替＋検索のみ、表ではなくカード行）。
- **スプシ同期は2つの不具合で止まっていた（9/26修正）**: ①同期が既存記録を1回の問い合わせで読み、PC側の上限1,000行を超えた分を「アプリに無い」と誤判定。既存の日を重複として取り込もうとして失敗し（9/26手動実行で685件すべて重複エラー、データ変更なし）、書き戻し待ち44件（8/13〜）も見えず、アプリ主入力者のシート修正も取り込まれていなかった。ページ分けで全件読むよう修正（fetchAllPages）。②失敗通知トリガーの型の誤りで結果記録が9/9から running のまま・通知ゼロ（migration 20260926000000）。
- **DBの実体は内蔵NVMe SSD（9/26 01:10〜01:30 JSTに移設）**: WSL Ubuntu の ext4.vhdx を `wsl --manage Ubuntu --move D:\WSL\Ubuntu` で外付けHDD(E:)から内蔵SSD(D:)へ移動。移動前の複製を `E:\WSL\Ubuntu-pre-ssd-20260926\ext4.vhdx` に保存（問題が無ければ後日削除可）。**中継(run-backend.mjs)は `D:\TUAT_TF\node_modules` の依存を使う**。以前から空になっていて、再起動した瞬間に起動できなくなった（今回 npm ci で復旧）。main の node_modules を消さないこと。
- **スプシ同期の監査P1〜P2を修正（9/26）**: ①アプリ主入力の部員もスプシで直した値（空でないもの）を取り込む（7/27方針の反映漏れ）②書き戻し待ちの再送は空欄も送る③送信中に内容が変わっていたら送信済みにしない（updated_at照合）④**アプリで記録を消した・日付を変えたらスプシの元の日の欄を空にする**（オーナー確定A。`sheet_pending_clears` にトリガーで予定を作り、削除直後の `/api/sheets/clear-deleted`・保存時の push-record・毎日0時の同期が処理。予定がある日は取り込まない。GASは行が無いと作ってしまうので、中身が残る行だけ空にする）。
- **Supabase予備構成（9/26着手。9/26 16:00ごろ全員のログインをクラウドへ切替済み。停止時の自動切替・復帰時の書き戻しは未実装）**: オーナー依頼「PCが止まったときだけSupabaseを使う（直近3日分を保管）」。旧プロジェクト snbgxocgdhqtuywrlqrs をオーナーが再開（9/26 13:44 ACTIVE、9/9時点のデータ・部員65人・Googleログイン設定あり、旧書き込み凍結トリガーが残っている可能性）。無料枠の上限内（3日分は約0.4MB、転送は停止日のみ）。PCとクラウドのJWT鍵は別。PCのPostgRESTはJWKS（鍵の集合）なので、クラウドを公開鍵方式にすればクラウド発行のログインをPCも受け付けられる。設計: ログインは常時クラウド（部員は1回再ログイン、オーナー了承・告知不要）、データはPC、直近3日分をクラウドへ写し、PC停止時だけクラウドへ切替、復帰時に書き戻し。まだ本番・PC・クラウドの設定は変更していない。9/26 11:17〜11:24 JSTにQuick Tunnel切断で本番が約7分停止（自動復旧）。
- **Vercel負荷削減（REST中継）は稼働中**: 本番の `NEXT_PUBLIC_PC_REST_RELAY_ORIGIN` は9/25 15:30ごろCodexが設定済みで、fa2ae70（15:50）以降の全デプロイが中継経由。秘密鍵登録（23:05）までの約7時間、ブラウザからのDB読み書きは503で失敗していた（下の作業ログ参照）。現在は正常。中継が失敗したら読み込みはVercel経由で取り直し、10分間は全通信をVercel経由にする仕組みを追加。Cloudflare無料枠は1日10万要求（現状の最大見込み約6.8万）。PC定期処理の移設は未着手。

## 2026-09-24 Vercel削減の本番反映準備（切替未実施）

Codex: 所有者から本番反映まで進める依頼。実PC backend/backup正常、Auth/不正中継拒否を確認、Vercel認証更新・公開3f74157を確認。Google側から既存同期設定を秘密値非表示で引継ぎ、本番dryRunは記録/大会とも成功（記録の予測insert609/push1は要差分確認、書込なし）。独立GAS監視プロジェクトへ既存コードを配置したが認可/トリガー/メール送信は未実施。Cloudflare未ログインで所有者へ依頼済み。最新3f74157＋src5ファイルの隔離441テスト、PC合成build/tsc、runner11テスト成功。本番アプリ/DB/Auth/4cron/公開先/OSタスク/料金は未変更。監視APIの未公開依存関係とjobs統合、公開試験、実認証/保存、差分確認後に切替。詳細docs/VERCEL-LOAD-2026-09-24.md。

## 2026-09-24 Vercel定期処理のPC移設候補（本番未反映）

Codex: 所有者の追加依頼で、現行の記録/予定/大会同期と期限切れ整理を同頻度でPC上の同じNext APIへ移すrunnerをops/pc-jobsへ実装。永続journal・曖昧な失敗の非再送・バックアップ/maintenance確認、署名healthと既存GAS監視の追加候補を作成。最新319f550＋対象src5ファイルのみの440テスト、PC/Vercel両候補の合成設定build（tsc込み）、ローカルAPI拒否確認、Node11項目・workerd9項目・eslint成功。crons=[]は隔離候補のみで本番設定/DB/OSタスク/公開/監視/料金プランは変更なし。公開試験・本番PC設定/dryRun・外部監視の稼働確認後、所有者承認で段階切替。旧cron/手動操作との分散排他は未追加のため同時実行を避ける切替が必須。詳細ops/pc-jobs/README.md、docs/VERCEL-LOAD-2026-09-24.md。

## 2026-09-24 補助員の役職設定・人数上限・複数選択

Codex: オーナー指示で種目を開いて役職/略称/必要人数を追加編集、部員枠では複数役職選択と現在の担当者/人数表示。満員と割当数未満への減員を拒否。migration 20260924130000をバックアップ/rollback検証後PC適用、既存担当は役職IDへ保持し必要人数初期値は現在の割当人数、旧複合自由記述は分割しない。種目単位ロックで上限と保存を原子的に検証、旧自由入力RPCを一般ログインから閉じた。役職変更後も担当ID保持。表は略称、CSVは正式名、PC通常セル約94×33px/1600px幅で12種目表示。全437テスト・eslint・TypeScript付きbuild・役職追加/人数編集/複数選択/満員表示の合成UI成功。0492e8eをmasterへpush、システム限定。詳細docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 出場枠○・出場なし空欄

Codex: 補助員表/CSVは同時刻の出場を○、出場なし空欄に変更。当日リレーは当日確認。出場時間帯は新規担当登録不可、既存担当は解除のみ可能。migration 20260924120000で同時刻の別種目を含めDB拒否、既存データ保持。バックアップ/rollback検証・20テスト・eslint・TypeScript付きbuild・合成UI成功、eddaefeをmasterへpush。システム限定。

## 2026-09-24 補助員枠の追加ラベル撤去

Codex: オーナー指示で未割当枠の「＋ 担当」「＋ 補助員を入れる」を撤去。枠全体の編集ボタン/アクセシブル名を維持、登録済み担当だけを表示。eslint・TypeScript付きbuild成功、fdc9d03をmasterへpush。DB変更なし。

## 2026-09-24 補助員一覧を役職別に表示

Codex: オーナー指示で種目の補助員一覧を担当内容ごとの見出し+人数+担当者一覧に変更。同一担当の全半角/空白を表示上正規化してまとめる。DB変更なし。eslint・TypeScript付きbuild・合成3人2役職の表示確認成功。2dea081をmasterへpush、システム限定。

## 2026-09-24 種目タップの補助員一覧

Codex: 補助員表の種目見出しを押すと共通FormModalで氏名/学年/担当内容を一覧表示。表の検索とは独立し、解除済みは除外、0人表示あり。eslint・TypeScript付きbuild・登録済み/0人の合成UI確認成功。3f74157をmasterへpush、DB変更なし・システム限定。

## 2026-09-24 OB画面のPC幅と表密度

Codex: オーナー依頼でOB画面だけPC外枠上限1600px/本文幅制限解除、補助員表の列112px・通常行57pxに縮小（スマホ144×97px維持）。長い出場予定は省略+title/編集画面、担当は最大2行。共通layoutに識別classを追加しCSS :hasでOBだけ拡張。1600px合成画面で本文1288px、390pxで横はみ出しなし。eslint・TypeScript付きbuild成功、0fffd4bをmasterへpush。DB変更なし・システム限定。

## 2026-09-24 補助員対象の訂正（最新）

Codex: 「現役全員」は学年制限だけ解除する意図。オーナー再指示でエントリー未確認は補助員表/CSVから再除外し、B3以上を含む競技回答のある現役部員を対象とした。未照合回答は本人照合後に登録。担当データ削除/DB変更なし。対象9テスト・eslint・TypeScript付きbuild成功、77b0d4aをmasterへpush。システム限定、スクロールバーのみを維持。

## 2026-09-24 補助員は現役全員・PCスクロールバー

Codex: オーナーの追加指示で補助員候補を現役全員へ戻した（B3以上・エントリー未確認も可、OB・OG回答は除外）。migration 20260924110000をバックアップ/rollback検証後にPC本番適用、在籍/システム権限/競合検証を維持。共通CSSのスクロールバー非表示を補助員表だけ12px表示へ変更。左右移動ボタンは最新指示で撤去しスクロールバーのみ。433テスト（対象再実行込み）・eslint・TypeScript付きbuild・合成UI確認。32729baをmasterへpush、システム限定。詳細docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 補助員担当を種目ごとに分離

Codex: オーナー指示で同時刻の3組を分離し、補助員表12種目・CSV27列に変更。担当の主キー/RPCに種目を追加し、同時刻でも別々に保存・解除可能。同時刻の他種目出場は表示を維持。migration 20260924100000をPC本番適用、既存1500m担当5件の内容/revisionを保持。旧合同枠の曖昧な担当があれば移行停止。暗号化バックアップ・rollback付き権限/競合/種目独立性検証・全433テスト・eslint・TypeScript付きbuild・合成UI確認成功。8c34070をmasterへpush、システム限定。詳細docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 補助員はエントリー済みB1・B2限定

Codex: オーナー指定で補助員表/CSVからB3以上とエントリー未確認を除外。枠全体をタップして担当を追加・編集できるようにした。未照合回答は本人照合後に登録。migration 20260924090000で保存時にも学年/競技回答を検証。指定された懇親会回答1件をOB・OG等へ訂正、出欠等は不変。個人情報を含む訂正スクリプトはGit対象外。暗号化バックアップ/rollback検証後にPC本番適用。全432テスト・eslint・TypeScript付きbuild・320px合成UI確認成功。319f550をmasterへpush、システム限定。詳細docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 OB戦補助員の登録・編集

Codex: オーナー依頼で補助員表の「回答なし」を「エントリー未確認」へ変更し、システムロールが部員×開始時刻ごとに担当を追加・編集・解除できるようにした。共通FormModal、同時刻出場の表示、未保存/解除確認、CSV21列へ担当反映。migration 20260924080000を暗号化バックアップ後にPC本番適用、専用RPCで権限/在籍/入力/revision競合を検証、解除後もrevision保持。試験変更はrollback、競技と懇親会の元データは不変。全431テスト・eslint・TypeScript付き本番設定build・320px合成UI・CSV出力確認成功。9002de6をmasterへpush、システム限定。詳細docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 OB・OGのプログラム追加

Codex: オーナー依頼でOB・OG21人64種目と資格記録をPC本番へ追加し、全85人238種目。元CSVの最新回答・氏名欄の明示変更を使用し、既存64人の全列と本人紐付け61件を保持。懇親会21件を同じ競技回答へ関連付け、出欠は不変。プログラムではOB・OG区分を表示、現役の本人照合・補助員検討表には混ぜない。暗号化バックアップ・rollback検証・権限確認、全427テスト・eslint・TypeScript付き本番設定build成功。03dbd19をmasterへpush、システム限定。詳細docs/OB-ENTRIES-2026-09-24.md。個人データはGit対象外。

## 2026-09-24 OB戦の予定・懇親会・新規登録・補助員検討表

Codex: オーナー指定の09:00〜16:00の予定と当日リレーを表示。懇親会19:00ミライザカ府中並木通り店3,500円を追加し、最新回答＋追記＋氏名欄の明示変更を統合して参加64・不参加27・未回答1・氏名確認待ち1をシステム限定取込。migration 20260924070000をPC本番適用、競技と出欠を一括保存するRPC・懇親会のみ登録・出欠編集を追加。元競技64人の全列と照合を保持。補助員は部員×開始時刻の検討表とCSV（担当記入欄）、アプリ内の担当保存は未実装。空き時間と断定せずアップ等の確認を明記。全426テスト・eslint・本番設定build・390px/320px合成UI・実CSV出力・DB権限/競合/原子的保存を確認、試験変更はrollback。a20ade6をmasterへpush。詳細docs/OB-ENTRIES-2026-09-24.md。元CSV/個人データはGit対象外。

## 2026-09-24 OB戦UI再構成・男女区分の誤登録防止

Codex: オーナー指示でOB一覧を27大戦と同じトラック/フィールド・種目開閉式に再構成。共通のSegmentedControl/三点メニュー/FormModalに統一し、種目チェックの直下で資格記録入力。既存の男女区分は種目から引継ぎ、新規は未選択から選ぶ。本人照合にも区分を表示。migration 20260924060000をPC本番適用し、DBでも区分混在・変更を拒否、取消後も区分保持。64人の元回答全列と本人紐付け61人を保持、試験変更はrollback。全418テスト・tsc・対象eslint・本番設定build・390px/320px合成UI成功。a1d3f42をmasterへpush、システム限定。詳細docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 Vercel REST中継削減（ローカル実装・本番未反映）

Codex: 現行srcのみ、ブラウザREST/RPCを任意のCloudflare Workerへ振り分けるtransportと中継を実装。Auth/OAuth/Cookie/SSRは既存経路を維持、公開relay URL未設定なら従来どおり。先行coalesced-fetchを実Supabase SDKで検証し、保存完了後に古い取得へ合流する競合も修正。workerd合成8項目、作業ツリー439テスト・tsc・対象eslint、HEAD d5cbe24＋対象src5ファイルだけの425テスト・合成PC設定build成功。本番公開versionはd5cbe24、Auth200確認。Vercel最新課金使用量・実削減率・実ユーザー認証/RLS通し確認は未実施。commit/push/deploy・DB/Auth/公開接続先変更は未実施、所有者の承認が必要。大会等の未公開変更を混ぜない。詳細: docs/VERCEL-LOAD-2026-09-24.md、段階切替/切り戻し: ops/pc-rest-relay/README.md。

## 2026-09-24 オーナー指示：現行アプリのみ改善

現在の作業先は D:\TUAT_TF の現行アプリ（src/）。新アプリは使用せず、D:\TUAT_TF_NEXT_PAUSED へ移動して休止する。新アプリの開発・自動継続・プレビューを再開しない。過去の tuat-tf-next/ や apps/current 指示は履歴であり、現在の改善対象ではない。新アプリの再開はオーナーの明示指示がある場合のみ。

## 2026-09-24 OB戦の種目別一覧・編集UI整理

Codex: オーナー指示で種目別一覧を初期表示にし、各種目の氏名・学年・資格記録を表で比較、その行から編集可能にした。自分・本人照合は別タブ。種目追加は男女切替と複数選択ボタンの直接タップ、記録は一覧入力、件数と保存は下部固定。変更履歴の画面と取得Actionは撤去し、既存DBは変更なし。全417テスト・tsc・対象eslint・本番設定build成功、390px/320px合成UIで一覧/検索/編集/追加/横はみ出しなしを確認。0cddb10をmasterへpush。システム限定を維持。詳細: docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 OB戦の追加・取り消し・記録編集

Codex: オーナー依頼でシステム限定の追加エントリー、種目追加/取り消し、資格記録編集、再エントリー、変更履歴を実装。全画面フォームで保存・未保存破棄/取り消し確認。migration 20260924050000をPC本番適用し、変更内容は権限/種目/入力/競合を検証するRPCからのみ保存、履歴は同一トランザクションで追記。既存回答は不変で試験変更はロールバック済み。全417テスト・tsc・対象eslint・本番設定build・320px合成UI確認成功。d5cbe24をmasterへpush。Googleフォームへの書戻しは行わない。今後のCSV再取込はアプリ編集を旧回答で上書きしない。詳細: docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 OB戦の本人用資格記録・氏名照合共通化

Codex: オーナー指示でシステム限定のOB画面を「自分のエントリー」初期表示にし、種目と資格記録を対応表で表示。資格記録83件・未回答29件・フォームに記録欄なし62種目を区別。migration 20260924040000適用時に既存の61人の本人紐付けと全元列を保持。氏名照合を共通化し、別開催回の確認済み氏名→部員IDも候補に利用する（自動確定しない）。全412テスト・tsc・対象eslint・本番設定build・320px合成UI確認済み。対象変更のみc1b5cf1をmasterへpush。一般部員は非公開を維持。詳細: docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 OB戦エントリーのシステムロール限定公開

Codex: オーナーがOB戦だけ「今回はシステムロール限定」と本番反映を指定。64人174種目の氏名・学年・種目だけをPC本番へ取り込み、マイページから確認・本人ID紐付けが可能。匿名・一般部員はDBでも取得不可。完全一致44人・要表示名確認20人で、自動確定はしない。migration 20260924030000適用、OB機能だけの529c591をmasterへpush。元CSV・照合表はGit対象外。懇親会と下記27大戦・関東新人変更の公開は引き続き保留。詳細: docs/OB-ENTRIES-2026-09-24.md。

## 2026-09-24 大会整理・エントリー準備（27大戦・関東新人は本番反映禁止）

> 2026-09-25更新: アーカイブと大会ページの整理はオーナー指示で本番反映済み（be5d873）。関東新人は取りやめ。以下は当時の記録。

Codex: オーナー指示で27大戦のプログラム導線統一、9/25 08:00 JSTの非破壊アーカイブ、関東新人の公式番組取込をローカル準備。途中で「まだ本番実装はしないで」と指示があり、commit/push・migration適用・DB書き込み・デプロイ・本番を変更する予約は未実施。明朝の切替も未有効。OB戦CSVは承認された最新回答＋追記で確認用集計のみ、個人データはGit対象外。詳細: docs/COMPETITION-PREP-2026-09-24.md。新たな本番反映指示なしに適用しない。

## 2026-09-23 プログラムの途中経過による出場者欠落（本番反映済み）

Codex: 男子走幅跳の未試技2人が公式途中経過から一時的に消え、同期で空名簿へ置換される原因を確認。競技中の個人種目は既知の出場者を結果待ちで保持する修正と、種目一覧/詳細欠落の取込拒否を実装。オーナー指示後、対象変更だけをmasterへcommit/pushして本番反映。開始前バックアップからB4陣立55番をPC本番DBへ復元し、B2後藤29番と記録も保持。公式56種目・33種目74出場を本番DBと再照合、欠落0。全403テスト（別作業監視4件込み）・対象eslint・TypeScript付きPC本番設定build成功。詳細: docs/PROGRAM-2026-09-23.md。

## 2026-09-23 ホーム表示・取得処理・文書の整理

Codex: ホーム本文を並列取得後に一括表示し、loadingと本文で共通スケルトンを使用。中長距離メニューCSVは本日の予定を開く時だけ取得、予定一覧とQuery定義共用・60秒キャッシュ・短距離予定は対象外。週間集計は距離5列だけ取得し項目定義RPCを省略。遅れて届くメニューの反映も修正。27大戦プログラムは9/23→9/22（各日は時刻順）。古い4文書を削除しREADME・文書案内・運用/UI規約を更新。現行391テスト（別作業の監視込み395）・tsc・対象eslint・PC本番設定build成功。390px/320px合成UIで展開・読み込み・再試行・日付順を確認、iOS実機未確認。詳細: docs/HOME-2026-09-23.md。

## 2026-09-23 現行アプリのUI・通信改善

Codex: 下部タブと全画面フォームを可視viewportへ追従、予定SSR直後の重複取得と復帰時の通信連発を抑制、引き下げ更新の二重取得を削減。フォーム遅延読み込み・スケルトン・見出し先行表示・中長距離以外の不要グラフ取得省略。現行アプリ383テスト（別作業の監視4件を含めると387）・tsc・対象eslint・PC本番設定build成功。390px/320px合成UIで下部配置・フォーム開閉・改行・横はみ出しなし確認。iOS実機での再発と本番速度の改善率は未確認。DB/認証/cron/新アプリ変更なし。詳細: docs/UI-PERFORMANCE-2026-09-23.md。

## 2026-09-22 新アプリの開発フォルダ準備

Codex: オーナーがモデル選定とエージェント指示を委任。`tuat-tf-next/`へ独立package.json、AGENTS.md、確定/未確定要件、担当指示、作業表を作成。設計レビューは親モデル継承、UI指示書はGPT-5.6 Sol Highを指定。現時点は既存Git内のサブフォルダで、別リポジトリ・アプリ実装・本番デプロイは未実施。新アプリ作業は同フォルダの指示を参照。

## 2026-09-22 再構築設計案・質問リスト

Codex: オーナー依頼で `docs/REBUILD-PLAN.md`（機能整理・共通UI・構成候補・受入条件・移行段階）と `docs/REBUILD-QUESTIONS.md`（優先8問＋追加11問）を作成。機能の優先度、スプシ廃止、基盤採用は未確定の仮案。回答前に廃止や移行を実行しない。本番・アプリコード変更なし。

## 2026-09-22 Cloudflare/D1成立性検証

Codex: 動画なし・Cloudflare中心案をMiniflare/D1・合成100人3万件で検証。21項目＋既存大会解析/Shift_JISの2確認成功。基本構成は条件付き採用可、全機能の無料運用は未実証。認証/RLS移植・同期CPU・sharp画像処理が主要課題。`docs/CLOUDFLARE-FEASIBILITY-2026-09-22.md` と `ops/cloudflare-evaluation/` 参照。本番DB・認証・デプロイは変更なし。

## 2026-09-22 大会UI・お知らせアーカイブ

ホーム/プログラム/目標のUIを統一し、組・番は結果欄に集約。開催中カウントダウン0日。お知らせ作成権限者のアーカイブ/解除を追加し全員のホームから除外。既存データを非公開退避、権限・復元・二重適用をrollback検証後、PC本番へarchived_at列追加。全355テスト・tsc・対象eslint・本番設定build成功、390pxの合成表示/結果開閉確認。iOS実機未確認。詳細: docs/UI-2026-09-22.md。

## 2026-09-21 実測監査の結果（着手前に必ず読む）

本番を実測して洗い出した**要修正リスト**: **[docs/AUDIT-2026-09-21.md](docs/AUDIT-2026-09-21.md)**
各項目に測り方を書いてあるので、直した後に同じ方法で確認できる。直した項目はその文書の状態欄を更新する。

| # | 内容 | 状態 |
|---|---|---|
| 1 | 🔴 **権限関数が未ログインでも true を返す**（`is_everyone` が本人確認を短絡。`can_create_notice` が現に開いている。他4つはロール設定ひとつで開く） | 修正済み（2ecafdd） |
| 2 | 🔴 書き込みポリシー95本中71本が `TO public`（2026-06-24の修正はSELECTだけだった） | 修正済み（e1d2961） |
| 3 | 🔴 **Web Push が 2026-09-17 から不通**（Vaultが空・VAPID鍵が移設で消失。9人11台が購読中） | 修正済み（8eb745b） |
| 4 | 🔴 壊れても誰にも通知されない。**同種の事故が5回目**（タスク14の連続失敗アラートが7/4から未着手） | 取りやめ（2026-09-25 オーナー判断。監視は退避、DB関数は削除） |
| 5 | 🟠 R2のバックアップに保持ルールがない。1日240MB増で**約3週間後に画像を上げられなくなる** | 修正済み（35281ef） |
| 6 | 🟠 削除済み投稿を指す通知27件・いいね47件・コメント4件が残っている | 修正済み（8e61706） |
| 7 | 🟡 5秒ごとの死活確認がDB最大の負荷（アプリ最重クエリの6.4倍） | 修正済み（6750acb） |
| 8 | 🟡 練習記録の取得の72%が「記録フォームの項目定義」の繰り返し | 修正済み（2ee98ac） |
| 9 | 🟡 `/auth/v1/user` 439回/時、`roles` 55回/時を毎レンダリング取得 | 修正済み（9b62ec8） |
| 10 | 🟢 無料プランへ戻す条件（Vercel Hobbyはcron毎時が不可／Supabase Freeは転送量が2〜4割超過） | 調査済み |

**この repo は公開**。未修正のセキュリティ項目について、攻撃手順にあたる記述を足さないこと。

## 2026-09-10 オーナー追記（最優先）

大会記録入力のmigration・master反映・既存データ正規化をこのPCで実施する。旧クラウド更新は後回しとし、9/17以降は別PCをサーバー化する予定。長期のSupabaseクラウド継続を前提に余分な復帰実装を追加しない。出欠だけの継続可否は説明のみの依頼で、実装しない。現行の9/16 15:00停止期限は変更していない。

## 2026-09-09 PC本番運用（以前の試験記録に優先）

本番 https://tuat-tf.vercel.app はVercelを維持し、DB/AuthをこのPCへ切替済み。所有者のログイン成功を確認。期間は2026-09-09 15:00〜09-16 15:00 JST。元のSupabaseは書き込み凍結・旧cron停止中。URLだけ戻してはいけない。

Claude・別端末のCodexは、最初に ops/laptop/PC-PRODUCTION-HANDOFF.md と ops/laptop/RETURN-TO-SUPABASE.md を読む。現在の設定/秘密情報の所在/起動/安全な再起動/バックアップ/復帰/残事項を記載。別端末からコード作業は可能だが、本番PCのDB操作を同名ローカル環境で代用しない。DB・環境変数・デプロイを複数エージェントで同時変更しない。

Google callback追加、VercelへのPCキー/R2設定、R2への暗号化DBバックアップは所有者の明示承認済み。匿名・偽造JWT・管理API・外部Origin・中継直アクセス拒否を実環境で検証。秘密値はGit/ログへ出さない。Web Pushは既存VAPID秘密鍵がなく未移行。クラウド復帰は9月16日14:30にこのCodexタスクの再開を登録済みだが、実行環境が使えない場合は手動対応が必要。

以下は履歴。過去の「本番未変更」「承認待ち」「試験版を使う」を現在の運用判断に使わない。
- 2026-09-08 / Codex / PC試験版ログインのOrigin deniedを修正。ゲートのReferrer-Policy: no-referrerがHTMLフォームPOSTのOriginをnullにする仕様と厳密Origin照合が衝突していた。入口とアプリ応答をsame-originへ変更し、外部サイトへのReferer送信は抑止したまま、同一URL内のフォームOriginを維持。null/欠落/外部Originの拒否は緩和していない。専用ゲートテスト成功、実ブラウザの通常フォームがOriginチェックを通ってパスワード検証へ進むことを確認。ゲートのみ再起動しURL/パスワード/PCデータ/本番は維持、エラー中のユーザータブをGETで開き直した。前回のHTTPテストはOriginを手動指定していてブラウザ挙動を見落としていた。 -> (this commit)
- 2026-09-08 / Codex / 本人限定の別URLでPC試験版を起動。本番は320be2eのまま変更なし。Windows cloudflaredを公式SHA256照合後にworkspace内へ配置し、127.0.0.1:3108の専用ゲートだけをQuick Tunnelで公開。ランダム専用パスワードと12時間Secure/HttpOnlyセッション、JWT本人照合、Origin検証、試行制限、ログアウト/再起動時失効を実装。PC内だけの一般部員「PC試験ユーザー」を作成し、本番アカウントは変更なし。パスワードはGit対象外ファイルへ保存しWindows ACLで本人/SYSTEM限定。アプリは3009と.next-pc-trialへ分離、PC表示/ログアウトを追加。GAS/Google/Push/cron/画像変更/Realtime/SWを止めた。HTTPS経由のログイン・4画面200・REST本人限定・一般部員権限・目標CRUD・refresh・ログアウト後401を検証、テスト目標回収。全201テスト・専用ゲート検証・対象eslint・TypeScript付き専用build成功。専用app/gatewayの停止/再開も実証。依頼は本人限定試験のためmaster/Vercelへ公開せず専用ローカルブランチ保持。詳細ops/laptop/PRIVATE-TRIAL.md。スマホ実機未確認。 -> (this commit)
- 2026-09-08 / Codex / 目標一覧を種目別・部員別の表形式へ変更。名前/種目/目標検索、種目絞り込み、自分のみ、件数、長文展開を追加。本人の右上三点メニューから1件ずつ編集/削除し、一覧内FABから複数種目を追加。新規insertと編集updateを分離し、他種目の意図しない上書きを防止。所属ブロックに応じた種目優先順と手動切替、未保存確認を追加。日付をホーム見出し横へ移し、直近7日間のサマリー見出しを撤去。全201テスト・tsc・対象eslint・build成功。PC版DBで編集/追加/削除と他種目保持を検証、49人146件の合成一覧を390px/320pxで確認（一時ルート/アカウント/データ回収済み）。iOS実機未確認。ユーザーはPCへの本番切替を依頼したが、Cloudflareアカウントにドメインがなく固定公開URLとGoogle OAuth等が未設定のため、本番接続先は未変更。PC側11コンテナhealthy。 -> (this commit)
- 2026-09-08 / Codex / PC代替Supabaseの実環境をWSL2 Ubuntuへ準備。Docker Engine/Compose/cloudflared導入、公式PG17構成に稼働元Auth v2.196.0・Storage v1.73.1を合わせ、独立キーとlocalhost API・DB/Functions隔離・cron停止を設定。DB/Authの72テーブル22,602行と59テーブルの全ID一致を検証。通常dumpが落とすAuthトリガー1本・Storageポリシー6本を別途取得/復元し、新規Authユーザー/プロフィール作成・ログイン・一般部員の種目編集拒否・2種目保存をPCで検証、一時データ回収済み。Google暗号化キーの独立指定と画像読み取り専用ガードを追加し、既存Google連携1件の復号成功。全200テスト・tsc・対象eslint・build成功。種目編集は既存「システム」ロールのmanage_systemであることを実DB確認（権限変更なし）。本番DB接続先/外部公開は未変更。固定ドメイン・Google OAuth・通知/同期・最終書き込み停止・切り戻し検証は残る。詳細ops/laptop/README.md。実機未確認。 -> (this commit)
- 2026-09-08 / Codex / ホームの大会カウントダウンと目標を走行距離と同じ2列・p-4カードへ縮小し、タップで詳細を開く方式へ変更。目標は種目別の入力欄を追加して一括upsert可能にし、選択済み種目の重複を防止。competition_eventsマスタ（一般29種目＋既存入力を保持）を追加し、管理者だけが追加・名称変更・表示順変更・未使用種目削除を実行可能。FKのON UPDATE CASCADEで目標を保持し、使用中種目は削除不可。migration 20260908020000は目標0件のスナップショット/dry-run後に本番適用。全197テスト・tsc・対象eslint・build成功。ローカル390px幅プレビューでカード同サイズと複数種目入力を確認しプレビューは削除。実DBで一般部員の種目追加/更新/削除拒否、2種目同時保存、未登録種目拒否を確認、一時データ回収済み。本番で一時管理者権限を付ける検証は自動承認レビューに拒否され未実施（管理者の成功操作・iOS実機は未確認）。 -> (this commit)

- 2026-09-08 / Codex / ホーム最上部に27大戦カウントダウンと種目別のみんなの目標を追加。既存予定に合わせ初日2026-09-21（初日0日、管理者が日付変更可）、本人だけ編集・種目別一覧、プロフィールの旧目標は保持。ノート写真を1記事6枚までR2へ圧縮保存し、ノートRLSで閲覧/編集を制限、再送時の重複抑制と記事/フォルダ削除後の永続回収キューを追加。migration 20260908010000本番適用済み。全194テスト・tsc・対象eslint・build成功、実DBで他人の編集拒否/匿名不可/非公開写真/同時7件中6件上限/cascadeキュー確認、ローカル本番ビルドから実R2保存/再送/取得/削除成功（一時データ回収済み）。ノートPC用公式PG17構成を.contingency/runtimeへ配置、ops/laptopにpreflight/backup/ローカル限定restore/トンネル例と切替手順を追加。Docker/cloudflared/psql未導入、OAuthとDrive暗号鍵移行・復元リハーサル未実施、本番切替なし。実機UI確認は未実施。 -> (this commit)

- 2026-08-29 / Codex / タイムラインの旧一覧表示を復活。日付ごとの2行一覧から投稿をタップしてその場でカード詳細を開閉でき、表示設定はCookie/localStorageへ保存する。現在の長文自動折り畳みと未読ラインはカード・一覧の両方で維持。利用頻度の低い学年絞り込みをタイムラインから撤去し、その位置へアイコン＋「一覧」の常時表示ボタンを配置して押下状態を色とaria-pressedで明示。ホーム・マイページ・部員ページの表示方式は変更なし。全199テスト・tsc・対象eslint・build成功。ローカル目視は認証画面まで（実機未確認）。 -> (this commit)
- 2026-08-29 / Codex / 投稿共有・お知らせ通知先除外・スレッドメッセージ編集を実装。①練習記録/つぶやきの既存パーマリンクを全員がカード右上の「…」からコピー可能にし、編集・削除は従来どおり本人だけに表示。②お知らせの全員/ロール/ブロック/学年一括指定後、個別調整で対象者を除外可能にした。notices.mentioned_excluded_user_ids追加と通知トリガー更新を本番適用し、条件変更後の古い除外IDは保存時に除去。③スレッド内の「返信」を「メッセージ/送信」に統一し、本人のメッセージ編集（編集済み表示・RLS件数確認・ローカル即時反映）を追加。Push通知関数も本番反映。全192テスト・tsc・対象eslint・build成功、DB migration一致確認済み。 -> (this commit)
- 2026-08-29 / Codex / iOS Safari/PWAで下部タブが画面中央へ移動することがある問題を修正。BottomNavをbody直下のPortalへ移し、親スクロールコンテナの影響を受けない固定配置にした。記名投票の投票者一覧は、選択肢タブ操作が投稿カードへ伝播してシートが閉じる問題を修正し、出欠一覧と同じ全画面高固定＋一覧のみ縦スクロールへ変更。選択肢増加時は高さ固定の横スクロールタブとし、最低幅・省略表示・選択タブの中央寄せに対応。tsc・対象eslint・build成功。 -> (this commit)
- 2026-08-21 / Claude Opus 5 / 他の部員のプロフィール（/members/[id]）でつぶやきが表示されない問題を修正。この画面は練習記録（getUserRecordsWithSocialState）しか読んでおらず、つぶやきを読む処理自体が無かった（マイページだけが getUserActivity で両方を出していた）。つぶやき取得を getUserTweets として切り出し（マイページと同じ経路＝内容が食い違わない）、部員ページでは記録と合わせて新しい順に並べ、マイページと同じ ActivityFeed（簡易表示トグル・タップ展開）で描画する。見出しは「これまでの記録」→「これまでの投稿」。記録の取得条件は従来どおりで、表示される記録は変わらない。全190テスト・tsc・対象eslint・build成功。実機確認は未実施 -> (this commit)
- 2026-08-21 / Claude Opus 5 / 投稿・コメントの編集中に改行（Enter）を押すと編集画面が閉じて入力が消える問題を修正。タイムライン／ホームのカードは「Enterで開閉」する role="button" になっており、編集モーダルは Portal で描画されていても React の合成イベントはカードのツリーを通るため、入力欄の改行がカードの開閉として扱われ、カードが折りたたまれた瞬間に編集フォームごと消えていた（保存されていたのではなく破棄されていた）。①FullScreenContent で入力中（textarea/input/select/contenteditable）のキー操作をモーダル内で止める（Escape=閉じる・Tab=フォーカス移動はRadixへ通す）②カード側の Enter/Space 開閉を「カード自身が対象のときだけ」に限定③コメント欄のキー・クリックも同様に止める④共通Button の type 未指定を "button" に固定（form 内で暗黙の送信ボタンになるのを防ぐ）。Enter で保存する処理はアプリ内に一つも無く、保存は明示的なボタンのみ＝通常の投稿と同じ仕様に統一。全190テスト・tsc・対象eslint・build成功。実機確認は未実施 -> (this commit)
- 2026-08-14 / Claude Opus 5 / 予定の「詳細情報」（practice_schedules.note）を通常の作成・編集フォームから編集できるようにした。予定カードには以前から「詳細情報」として表示されていたが、ScheduleForm に入力欄が無く、実質スプレッドシート取込（または「スプレッドシートから入力」の既存を編集）でしか書けなかった。種別に依らず（練習・大会・記録会共通）自動伸縮の Textarea を 1 項目追加し、空なら null で保存する。DB変更なし（note 列は既存・列単位の権限制限もなし）。全190テスト・tsc・対象eslint・build成功。実機確認は未実施 -> (this commit)
- 2026-08-11 / Claude Opus 5 / ito を一旦システム管理者限定（非公開）にした。/ito は manage_system 以外を /home へリダイレクト、マイページの導線も manage_system のみ、ito_invite 通知は権限が無い人には一覧にも未読数にも出さない（開けない画面へ誘導しないため）。管理コンソールに「いまは管理者だけに公開している」旨を明示。公開するときはこの4箇所のガードを外す。DB側のRLSは従来どおり（読み取りは authenticated に開いたまま）。全190テスト・tsc・対象eslint・build成功。
- 2026-08-11 / Claude Opus 5 / ito の並び替えUIを整理。上下ボタンを撤去して三本線のドラッグだけにし、並べ替えるたびにグループへ自動共有、確定は「この順番で提出する」の1操作に変更（保存/提出の2ボタンをやめた）。提出後は読み取り専用＋緑枠＋「提出しました」を明示し、「編集する」で再編集できる。代表者回答の入力例に実在の部員名（武藤さん）を出していたのを撤去。全189テスト・tsc・対象eslint・build成功。
- 2026-08-11 / Claude Opus 5 / ito の画面を手動リロードなしで自動更新するようにした。各コンポーネントに散っていた Realtime 購読を ItoLiveRefresh（描画なしの共通コンポーネント）へ集約し、ゲーム・ラウンド・グループ・並び順・代表者回答・得点・自分の招待をまとめて購読。加えて表示中は5秒間隔のポーリング、タブ復帰時とオンライン復帰時にも再取得する（合宿先でWebSocketが切れたままでも画面が止まらないようにするため）。ラウンド未開始の待機画面でも購読するので、進行役がラウンドを開始すると自動で切り替わる。全189テスト・tsc・対象eslint・build成功。
- 2026-08-11 / Claude Opus 5 / ito 参加者画面を直感的に作り直した。並び替えを既存 ReorderList のドラッグハンドル（三本線）＋上下ボタンの併用に変更し、上下の意味を「↑数字が大きい／↓数字が小さい」と明示。現在のラウンド・フェーズ・自分の班・いま何をすればよいかを画面上部にsticky固定（SubHeader の下、safe-area考慮）。回答済みの招待カードは進行中は表示しない（未回答だけ残す）。自分の班・自分が編集する回答は青枠で強調。結果発表後は各グループの予想の各位置に正解/不正解を✓×と背景色で表示。全189テスト・tsc・対象eslint・build成功。実機確認は未実施。
- 2026-08-10 / Claude Opus 5 / ito にシステム管理者自身が参加できるようにした。進行画面に「自分も参加する」を追加し、招待を持たない参加者（進行役が直接追加した人）でも /ito のプレイ画面とマイページの導線が出るよう getMyItoOverview を ito_participants も見る形に変更。従来は招待レコードが無いと参加者として扱われず、直接追加してもプレイ画面に到達できなかった。全189テスト・tsc・対象eslint・build成功。
- 2026-08-10 / Claude Opus 5 / itoの操作エラーが本番で「サーバーでエラーが発生しました」としか出ない問題を修正。Next.jsはServer Action内の例外を本番で伏せるため、ito のサーバーアクションを全て ItoActionResult（{ok,value} / {ok,message}）で返す形に変更し、呼び出し側は unwrapItoResult で文言を出す。あわせて、参加者0人などラウンドを開始できない構成は押す前に理由を表示しボタンを無効化。実際に出ていたエラー（参加者0人でラウンド開始）はVercelのランタイムログで特定。全189テスト・tsc・対象eslint・build成功。
- 2026-08-10 / Claude Opus 5 / ito にひとりで通しテストできる手段を追加。進行役が①招待を送らず参加者を直接追加（通知なし）②各グループの代表者を代理で決定③各グループの並び順を代理でランダム提出、を行えるようにした。並び順の代理提出のため migration 20260810040000 で ito_can_edit_group にシステム管理者を追加（回答受付中のみという制約は維持）。当日に端末が使えない部員が出たときの復旧手段も兼ねる。全189テスト・tsc・対象eslint・build成功、migration本番適用済み。
- 2026-08-10 / Claude Opus 5 / itoを実際に遊べる状態にした。ラウンド開始（自動グループ編成・手動移動・編成やり直し）、代表者選択（グループ共有・Realtime）、秘密数字の配布と本人だけの表示、代表者回答の管理者入力、グループ共有の並び替え（revision競合制御・提出/未提出）、回答受付終了・予想公開・結果発表・採点・ラウンド結果・個人累計ランキング・次ラウンドまでを実装。管理コンソールはフェーズごとに次の操作だけを出し、不可逆操作は確認ダイアログ。並び替えは上下ボタン方式（iOS PWAのドラッグ競合回避）。学年→名前の共通ソートを member-sort.ts に切り出し。クラウド版Claudeのブランチ（モード選択・お題・進行役の参加）もマージ。全189テスト・tsc・対象eslint・build成功。実機での通し確認は未実施。
- 2026-08-10 / Claude Opus 5 / itoの終了済みゲームを削除できるようにした。削除できるのは作成直後（draft）と終了済み（finished）だけで、進行中は終了してからでないと消せない。終了済みの削除はラウンド・秘密数字・予想・得点履歴もCASCADEで消えるため、確認ダイアログでその旨を明示。サーバーアクションの状態条件と権限をテストで固定（全170テスト）。tsc・対象eslint・build成功。
- 2026-08-10 / Claude Opus 5 / itoのゲーム作成とエントリーを実装。システム管理者だけがゲームを作成・進行でき（サーバーアクションの権限確認＋RLSの can_manage_system() で二重に担保）、対象ロールの部員へ招待を送る。進行役は招待対象から除外。未回答は declined に書き換えず pending のまま残す。招待は追記のみでラウンドごとに履歴化。導線はマイページのみ（管理メニューに「itoゲーム」、部員側は招待がある人だけ表示）で、BottomNav・ホーム・既存画面は変更なし。匿名キーでの実地確認で ito_respond_invitation の所有者チェックが auth.uid() NULL のとき素通りする穴を発見し、migration 20260810020000 で修正・再確認済み。全167テスト・tsc・対象eslint・build成功。
- 2026-08-10 / Claude Opus 5 / itoゲーム機能の土台を実装。docs/ITO-PLAN.md に設計（ゲーム状態とラウンドフェーズの分離、招待履歴の非破壊保持、秘密数字の多層秘匿、round_id を持たせたRealtime購読、revision による共有編集の競合制御、得点履歴からの再採点）を記載し、migration 20260810010000（ito_* 11テーブル・RLS・publication・RPC 8本）を本番適用。フェーズ表・採点・グループ編成を純関数化し、フェーズ許可表はSQLと突き合わせるテストで固定。全146テスト・tsc・対象eslint・build成功。UI・クエリは未実装（次コミット以降）。 -> (this commit)
- 2026-08-10 / Claude Opus 5 / 出欠一覧の「全体」タブで、マネージャーを中長距離・短距離の両方に出すのをやめ、専用の「マネージャー」節にまとめた。所属ごとに節を1つだけ返す attendanceSectionBlock を追加。中長距離・短距離タブの表示と出席・欠席カウントは従来どおり。全126テスト・tsc・build成功、本番デプロイ確認済み（実機の見た目確認は未実施）。
- 2026-08-03 / Codex / 練習予定の同日ブロック別運用を実装。月間予定入力と通常予定フォームに全体・中長距離・短距離の単一選択を追加し、日付×ブロックで時間・場所を独立保存。予定一覧のブロック絞り込み、一般部員向け全ブロック表示設定、マネージャー・システム管理者の全表示、同日時間順、メニュー予定ラベル改善、中長距離CSVメニューの短距離予定混入防止を追加。全124テスト・tsc・対象eslint・diff check・build成功。 -> (this commit)
- 2026-07-30 / Claude Opus 5 / アプリ全体の文言ゆれを統一。全1,477件の日本語リテラルを棚卸しし、主ボタンの動詞（投稿する/作成する/更新する/保存する）、進行中表記（{動詞}中…・三点リーダは…に統一）、エラー文（〜に失敗しました→〜できませんでした。トーストは1文、フォーム内は2文）、空状態（まだ〜はありません／条件に合う〜はありません）、プレースホルダ（例: 〜 か 〜を入力、検索欄は「検索」）、語彙（絞り込み・フォルダ・スプレッドシート・見出し・保存先・連携）、モーダルタイトルの名詞化とFAB項目名との一致、人/件の単位と半角コロンを揃えた。docs/WORDING-GUIDELINES.md に §6「同じものは同じ語で」を追加。全105テスト・tsc・対象eslint成功。実機（iOS PWA）での表示崩れ確認は未実施。 -> (this commit)
- 2026-07-27 / Codex / 走行距離を小数第2位まで入力・保存・表示できるよう統一。強度別4項目を0.01km刻み、DBをNUMERIC(6,2)、スプレッドシート取込を2桁精度へ変更し、カード・ホーム・ランキング・グラフを共通フォーマッター化。本番238件スナップショット、migration dry-run・適用後データ不変とランキングビュー正常を確認。全102テスト・tsc・対象eslint・diff check・build成功。 -> (this commit)
- 2026-07-27 / Codex / 予定作成モーダルを「練習・大会／行事・記録会」のタブ切替に変更。デフォルトは既存の練習月間入力を維持し、大会／行事と記録会は各種別に固定した単件入力フォームから登録可能にした。再表示時は練習タブへ戻り、種別タブ切替時のフォーム状態も正しく再初期化。全99テスト・tsc・対象eslint・diff check・build成功。 -> (this commit)
- 2026-07-27 / Codex / Made non-empty spreadsheet edits authoritative for linked records even when record_source is app. Dates with pending app-to-sheet pushes are excluded from pull to prevent stale sheet rollback; blank sheet cells remain non-destructive for app-main users because public CSV has no edit timestamps. Production dry-run: inserted 0, updated 0, pushed 0, failed 0. All 99 tests, tsc, targeted eslint, and build succeeded. -> (this commit)
- 2026-07-27 / Codex / Fixed spreadsheet custom-field mapping so builtin keyword fallbacks cannot steal configured columns (B3 Muto and B2 Hayashi cases). Removed actual-distance input/sync/display fallback for middle-long records and made weekly ranking use only the four intensity totals. All 98 tests, tsc, targeted eslint, build, and production migration dry-run succeeded. -> (this commit)
- 2026-07-25 / Codex / スプシフォーム設定の初回補正。自動計算の週合計列（実物で5列）を候補から除外し、旧設定移行時は中長距離の感想だけを入力・タイムライン初期選択。タイムライン追加項目を独立ブロック化し、感想を含む入力選択済み項目を一覧化。新旧CSV/GASの感想判定を完全一致のみに統一し、関連列署名と全列署名の比較不整合も修正。全93テスト・tsc・eslint・diff check・build成功、本番同期dry-runは記録差分0・失敗0 → (this commit)
- 2026-07-25 / Codex / スプシ連携の練習記録フォーム・タイムライン表示設定を実装。日付/曜日除外、中長距離固定項目、感想完全一致、短距離4項目初期選択、表示上限、列順、関連列変更時の再確認、長文展開に対応。設定バージョンと記録スナップショットで過去表示を保持し、新入力フォームはmanage_systemかつスプシ連携者だけに限定。全93テスト・tsc・eslint・diff check・build成功、本番DB適用前に対象2テーブル全件スナップショットとmigration dry-runを確認 → (this commit)
- 2026-07-25 / Codex / スプシV2の本番段階テストをmanage_system保持者だけに限定。一般部員は従来の空欄非破壊pull・実距離未取込を維持し、システム管理者も見出し設定保存後かつSHEET_FORM_V2_APPLY_ENABLED=trueの場合だけ空欄削除/実距離取込を有効化。タイムライン即時CSVは本人1シートだけ、関連APIもサーバー側権限検証。全90テスト・tsc・eslint・diff check・build成功 → (this commit)
- 2026-07-25 / Codex / テスト環境向けに、タイムラインのDB即時表示後CSVバックグラウンド同期、スプシ初回連携・見出し変更時のフォーム項目チェック/型選択、感想列の完全一致→部分一致→旧見出し優先、確認済み列の空欄反映、中長距離の強度別優先・全強度0時のみ実際の距離フォールバックを実装。全89テスト・tsc・eslint・diff check・build成功、ローカルサーバー起動とログイン画面200を確認。接続中DBには新規列未適用で、GAS・本番DB・本番アプリは未変更。認証後の実動作確認はテストDBへのmigration適用後に実施する → (このcommit)
- 2026-07-24 / Codex / Safariでアイコン切り抜き確定時にWebP変換できない場合はJPEGへフォールバックし、認証済みサーバーAPIで検証・512px WebPへ再変換して保存する経路を追加。Storage形式はWebPのまま維持。Safari相当2テスト・対象11テスト・全74テスト・tsc・eslint・diff check・build成功 → (このcommit)
- 2026-07-24 / Codex / アプリ返信とスプレッドシート返信の二重表示・二重カウントを解消。ユーザー名変更で本文照合が外れても、保存済みのスプレッドシート列位置で同一返信を判定。表示・同期取込・件数RPCを統一し、既存データは削除せず本番重複7件を件数から除外（89→82を検証）。対象8テスト・全69テスト・tsc・eslint・diff check・build成功 → (このcommit)
- 2026-07-24 / Codex / プロフィール画像にドラッグ・ピンチ・拡大スライダー付き切り抜き調整を追加。切り抜き確定時に画像だけ即時保存・即時反映し、通常のプロフィール保存で既存画像を誤削除し得る処理も修正。画像座標6テスト・全67テスト・tsc・対象eslint・スマホ幅目視確認・build成功 → (このcommit)
- 2026-07-23 / Codex / システム管理権限者だけがマイページ設定から旧アプリを開ける導線を追加。新アプリでログイン状態とmanage_system権限を確認して5分間有効な署名トークンを発行し、旧アプリからの検証APIは署名・期限を確認。旧アプリのURL直開きや偽パラメータでは開けない構成に変更。
- 2026-07-15 / Codex / 記録フォーム編集で「項目名を変更できます」を明示し、中長距離の強度別距離と必須日付以外の既定項目を非表示・再追加可能に変更。非表示項目は入力・タイムライン・スプレッドシート同期から一貫して除外し、並び替えハンドルは専用モード時のみ表示。マイページ管理メニューからFABと重複する予定・お知らせ作成を撤去し、ロール管理・練習場所・スプレッドシート同期を直下へ移動。
- 2026-07-15 / Codex / 人物選択のメンバーリストを名前一覧より上の候補へ移動し、月間メニュープリセットを全撤去。練習記録の既定項目名（日付・中長距離のランキング集計距離を除く）をプロフィールごとに変更でき、入力・タイムライン・スプシ見出し照合へ統一反映。ロールに全員自動適用、カテゴリマスタ選択/作成、初期閉じフォルダ、原子的な所属更新と楽観的反映を追加。重要なお知らせ全体を詳細リンク化し、確認済みはホームから除外。
- 2026-07-15 / Codex / 個別メニュー保存後に予定データのReact Queryキャッシュを即時再取得し、手動リロードなしで反映。共通PersonPickerへ複数保存できる「メンバーリスト」を絞り込み横に追加し、旧対象者セットを引き継いで保存・呼び出し・削除を集約。調査完了済みのタブ切替ラボとマイページ導線を撤去。
- 2026-07-13 / Codex / 全人物絞り込みで、対象一覧に実在する学年だけを表示するよう統一。PersonPicker・お知らせ通知先・メンバー一覧・タイムラインへ適用し、availableGradesを絞り込み部品の必須props化。該当学年が0件なら学年UI自体を非表示。プロフィールの学年設定画面は全候補が必要なため対象外。
- 2026-07-13 / Codex / お知らせ通知先の二重シート罠を解消。ロール・ブロック・学年・個別指定を同一フォーム内のDisclosure展開式へ変更。個別一覧では全員/ロール/ブロック/学年で対象の部員をハイライトして「条件で対象」、明示追加は「個別指定」と表示し、重複を含まない通知対象人数を表示。
- 2026-07-13 / Codex / お知らせ通知先を排他選択から和集合へ変更。全員・複数ロール・複数ブロック・複数学年・複数個人を同時指定可能にし、mentioned_blocks/mentioned_grades列と通知トリガーを追加。複数条件に該当する部員への通知はDISTINCTで1件に統合。
- 2026-07-13 / Codex / タイムラインとメンバー一覧のブロック絞り込みを、常時表示の「全体・中長距離・短距離」タブへ戻した。学年はシート式ボタンのまま、レイアウトが動く選択チップは復活させない。PersonPickerは複数ブロック選択が必要なため共通絞り込みシートを維持。
- 2026-07-13 / Codex / 月間メニュー入力は複数行対応を維持しつつ、メニュー・ペース・補足・補強・説明で改行を要求しない自由入力として文言を統一。「補強メニューを改行して入力」を「補強内容を入力」へ変更。
- 2026-07-13 / Codex / 月間予定・メニュー入力の文言を役割に合わせて修正。予定詳細から「練習内容の概要」を除外し、集合方法・持ち物・連絡事項に限定。メニュー欄は入力例を表示し、短距離の展開ボタンは「説明を入力」、中長距離は「ペース・補足・補強を入力」とした。
- 2026-07-13 / Codex / 学年絞り込み表記をB1/B2/M1等へ短縮。月間メニューの不明瞭なプリセット5枠を撤去し、対象者セットは対象者欄直下、メニュープリセットは適用先の日付行へ配置。呼び出し/現在内容の保存/名前入力を明示し、適用時は既存メニューIDを維持して内容項目だけコピーする。
- 2026-07-13 / Codex / タイムライン入力と人物絞り込みを統一。練習記録・つぶやき・大会結果（新規/編集）に未保存時の保存/破棄/継続確認とスピナー付き保存中表示を適用。人物選択・メンバー一覧・タイムラインで、名前検索＋複数ブロック＋複数学年の共通絞り込み部品を使用し、レイアウトが動く選択チップを撤去。
- 2026-07-13 / Codex / 月間予定・メニュー入力の保存フィードバックを改善。保存中はスピナー付き「保存しています…」を明示し、通常保存の成功時は他の投稿フォームと同様にフォームを閉じる。保存失敗時のみ入力画面を維持。
- 2026-07-13 / Codex / 月間予定・メニュー入力を再改善。作成FABは予定タブ限定、未保存時は保存/破棄/継続を確認、公開を既定として状態説明を追加、予定の時間を9:00/17:00/その他、場所を登録済み練習場所/その他の選択式へ変更。表示切替をセグメント化し、保存操作をFormModal共通フッターへ移動して下部余白を統一。
- 2026-07-13 / Codex / 月間入力を再調整。入力欄をコンパクト化し、ペース・補足・補強・説明は日ごとの展開式に変更。個人指定メニューでは補強を非表示かつ保存対象外、メニューは練習予定がある日のみ表示、過去日は一覧下部へ移動。予定入力は新規追加のため全日表示を維持。FAB共通化は今回対象外。
- 2026-07-13 / Codex / 月間予定・メニュー入力の複数行欄を改善。予定詳細を3行、メニュー本文を5行、ペース/補足を各3行、補強/短距離説明を各4行のTextareaへ変更し、改行内容を確認しやすくした。FAB共通化はオーナー指示により今回対象外。
- 2026-07-13 / Codex / 予定・メニューUX改修を実装完了。月内全日表示のアプリ内月間入力、予定/メニュー別タブ、変更行一括保存、端末内下書き退避、予定なし日の自動予定確保RPC、メニュー/対象者プリセット、FAB統一、予定カード内追加導線撤去、人物選択の共通化（メニュー・ノート・お知らせ・ロール、名前/複数ブロック/複数学年）、メニュー保存/公開/削除のローカル即時反映を実装。対象lint/tsc/build確認。
- 2026-07-13 / Codex / 予定・メニューのアプリ内月間表、FAB統一、共通人物選択UIの実装に着手。ユーザー合意: 同日1練習、月内全日表示、予定とメニューは別タブ、人物検索は名前・複数ブロック・学年、選択は行ハイライト＋右チェック、チップ表示なし。

<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Current implementation status (2026-07-12; supersedes stale backlog notes below)

- **Weekly backup (former task 13) is complete**: `/api/backup` and the GAS weekly Google Drive job are deployed. The owner has manually run and verified a saved backup; `attendances` is included.
- **P2, P5, and P6 are complete**: timeline pagination uses composite cursors, obsolete `getFeed` filter arguments were removed, and sheet-member refresh now runs after My Page/own-member rendering.
- **Tab freeze incident is RESOLVED (2026-07-12)**: 原因は`cacheComponents`。b3698c5で無効化しオーナー実機確認済み。タイムラインのIndexedDB永続化は`c5786a9`で停止したまま（フリーズとは無関係と判明したが、再導入は必要になったときに）。詳細は下の解決済みインシデント節。
- **Home attendance is local-only**: changing attendance does not call `router.refresh()` or reload the page; the home initial state uses one coherent skeleton.
- **Remaining candidates**: sync scalability at 100 members, sync-failure/mismatched-sheet visibility, search (explicitly deferred by owner), generated database types, and sheet bulk-input workflows. Use this section and the actual code over older “unstarted” labels below.

## 【解決済み 2026-07-12】iOS PWAのホーム遷移フリーズ → 原因は cacheComponents

**b3698c5（`next.config.ts`の`cacheComponents: true`無効化）で解消。オーナーが実機で「問題ない」と確認済み（2026-07-12）。**
- 教訓: 有効化（60d6175, 07-12 00:14）と同日に全症状（他タブ→ホーム毎回フリーズ・予定/タイムラインのフリーズ・リロード時ガクつき・もっさり）が始まり、無効化で消えた。**`cacheComponents`と`experimental.staleTimes`は再導入禁止**（少なくともNext 16.2.9 + iOS PWAの組み合わせでは危険）。
- タブ切替速度は ①Vercel関数の東京リージョン固定（vercel.json、削除禁止） ②各画面のreact-queryセッションキャッシュ ③loading.tsx で担保。
- 調査資産: FreezeProbe（フリーズ痕跡の記録・トースト）は残す。Tab Labは調査完了後、2026-07-15にオーナー指示で撤去。
- 以下は当時の調査記録（履歴として保持）:

### （履歴）調査経緯

- **現在の実機症状**: 予定など別タブからホームへ移動しようとすると毎回固まり、ホームへ戻れない。フリーズ、もっさり、スクロール時の構造崩れ、リロード時の配置ガクつきも報告あり。**解消したと扱わないこと。**
- `c5786a9`: タイムラインのIndexedDB永続化を停止。**ホーム遷移フリーズは解消せず**、IDB主因仮説は外れた。
- `e2f2497`: 予定のServer Action queryFnをキャンセル可能なRoute Handlerへ変更、BottomNavの手動・touch prefetchを撤去、PullToRefreshをレイアウトから停止、予定スケルトンを現UIへ合わせた。直後の短時間確認ではエラーなしとの報告だったが、後続の反復実機確認で**他タブ→ホームが毎回フリーズ**。成功ではない。
- `26ad026`: PullToRefreshをpassive・preventDefaultなしで再導入したところ、フリーズ・もっさり・ガクつきが全面再発。`18c948f`で即撤回。PullToRefreshは症状を悪化させることは確認できたが、撤去後もホーム遷移フリーズは残るため**単独の根本原因ではない**。
- **現在の本番状態**: PullToRefreshは未マウント、タイムラインIDB永続化なし、BottomNavの追加手動prefetchなし、予定再取得はRoute Handler。それでもホーム遷移フリーズは未解決。
- **禁止**: PullToRefreshを方式変更だけで再導入しない。IndexedDB・prefetch・Server Actionのいずれかを「根本原因」と断定しない。ローカルE2E成功やbuild成功を実機障害の解消扱いにしない。
- **次の調査**: `/home`遷移時のRSC要求開始/完了、Client Router状態、PPR/Suspense、保持中ルートとメモリ、メインスレッドlong taskを実機で採取する。推測ベースのキャッシュ微修正を続けず、まずホーム遷移そのものの証拠を取る。
- **2026-07-12 Fable 5の切り分け**: 消去法と時系列で **`cacheComponents: true`（PPR Router層）が最有力**と判断し無効化を実施。根拠: ①有効化は2026-07-12 00:14（60d6175）で、全症状は同日から（それ以前はタブ切替一瞬・フリーズ無し） ②Tab Lab実測でRouterを通さない実DOM再構築は安定＝残る容疑はRouter層のみ ③IDB・Server Action・prefetch・PullToRefresh・staleTimes全撤去でも再現 ④`"use cache"`等の依存コード無しで無効化はコンフィグのみ。無効化ビルドでtsc/build/WebKit E2E（他タブ→ホーム含む）全パス。**実機での最終判定はオーナー確認待ち。これで直っても「解消」と書く前に必ず実機確認を取ること。**

# まずこれを読む（エージェント共通の入口）

> このファイルだけ読めば着手できるようにしてある。各リンク先は必要なときだけ開けばよい。
> Next.js 16 固有の注意は上のブロック参照（`middleware.ts`→`src/proxy.ts`、`params`/`searchParams`/`cookies()` は await 必須、Tailwind v4 は `globals.css` の `@theme`）。

## このアプリ
TUAT T&F（陸上部アプリ）。Next.js 16 (App Router) + React 19 + Tailwind v4 + Supabase(@supabase/ssr)。本番 https://tuat-tf.vercel.app 。
複数端末・複数AI（Claude Code / Codex）で **同じ origin/master を共有して開発** している。

## 厳守ルール
- **origin/master が正。force-push 厳禁。作業前に必ず `git pull`**（diverge時は丸ごとマージせず自分の差分だけ載せ直す）。
- マイグレーションは **新しいタイムスタンプ＋冪等**（`IF NOT EXISTS` / `DROP POLICY IF EXISTS`）。適用は `"Y" | npx --yes supabase db push`。
- **検証は `npx tsc --noEmit` を基本**。`npm run build` は **push 直前だけ**（重い）。push 後 `xxxx..yyyy master -> master` を確認。
- **ブランチへの push で終わりにしない。master へ入れて Vercel Production が成功するまで見届ける**（下の「完了の定義」）。
- **コミット署名は作業したエージェント自身の名で**（他AIの名を偽らない）。末尾に各自の `Co-Authored-By` を付ける:
  - Claude Code → `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
  - Codex → `Co-Authored-By: Codex <noreply@openai.com>`
- 認証はユーザー版を維持（proxy は cookie があれば getUser 失敗でもログアウトさせない＋SessionKeepAlive）。
- アクセス制御は最終的に **RLS** が担保。UIガードと RLS の両方で守る。
- **同期・一括更新系のロジック変更は、本番初回実行の前に①対象テーブルのスナップショット取得（例: service roleでCSVエクスポート）②dryRunで差分確認、を必ず行う**（2026-07-03のデータ消失インシデントの教訓。本番DBはPITR/バックアップ無しで、消えたら戻せない）。

## 完了の定義（本番反映まで見届ける。2026-08-29 オーナー確定）

- 2026-09-07 / Codex / ぺい・涼香の9/7記録が距離/本文なし・追加項目「ラン以外の運動」の文字列0だけで投稿表示されることを実DB確認。customの入れ物だけで判定する条件を、空白/null/0だけを除外するcomputed fieldへ変更しページング前に絞り込み。migration 20260907010000適用、SQL回帰fixtures成功・対象2件保存維持/一覧0件を確認。同期の空行取込と新規アプリ入力にも0だけの追加項目を除外。書き出しは固定数値0と空の追加数値を空文字へ変更、明示的な追加数値0と非ゼロ値は維持。記録3752件/プロフィール64件退避、設定確認可能な実記録3674件の変換dry-runで非ゼロ変更0・書込0。全体同期dry-runはローカルSpreadsheet ID未設定で未実施、代わりに退避実データの変換を検証。本番変数一括取得は自動レビューに拒否され未取得。全182テスト・tsc・対象eslint・build成功。過去のDB/シートセル一括書換えなし、実機未確認。

- 2026-09-06 / Codex / Egress調査で名簿とつぶやきの著者情報に未使用の練習記録フォーム設定が繰り返し含まれることを確認し、取得列を削減。練習記録の設定取得は維持。フォルダ編集権限のない閲覧者では名簿取得も省略。実データのJSON比較で名簿64件64,025→10,698 bytes、つぶやき30件56,331→17,553 bytes（請求通信量の削減率ではない）。Authは既存の重複抑制を確認し認証チェックを維持、追加削減なし。R2無料枠とHEAD/GET回数をdocs/EGRESS-AUDIT.mdに記録。全163テスト・tsc・対象eslint・実環境変数入りbuild成功。実機未確認。

2026-09-06 / Codex / R2移行の実施記録: オーナー承認後、非公開画像バケット限定キーを発行しローカルとVercel ProductionのSecretへ保存。事前スナップショット後37画像17,711,786 bytesをコピー、全件SHA-256一致・元画像削除0・DB変更0。image-storage実装で両バケットの署名GETと公開拒否、一時画像の保存/取得/削除を実接続検証。READ=trueはProduction HgymvYiChDSXtgKxdAyy3G2QxRLE（36bd97f）でREADY確認。WRITE=trueも環境設定し、この記録のデプロイで反映する。実環境変数入りbuild成功。コードは前回全163テスト済みから変更なし。大学認証後の画面・iOS実機は未確認、ユーザーへ大学ログインを依頼済み。詳細はdocs/R2-MIGRATION.md。Vercel使用量キーは未発行、Supabase月間転送量は公式へのリンクのみ。

**作業ブランチへの push は完了ではない。** それだけでは Vercel の Preview が1つ増えるだけで、部員が使う本番（https://tuat-tf.vercel.app ）は何も変わっていない。オーナーから別の指示が無いかぎり、次の5つを終えて初めて「完了」と報告する。

1. `npx tsc --noEmit`・対象eslint・`npx vitest run`・`npm run build` を通す。
2. 作業ブランチを `git push -u origin <branch>` する。
3. `git fetch origin master` のうえ **master へ fast-forward して push** する。
   `git checkout master && git merge --ff-only <branch> && git push -u origin master`
   ff-only にならないときは origin/master が進んでいるので、**自分の差分を origin/master の上に載せ直してから**もう一度 ff（force-push 厳禁は従来どおり）。
4. **Vercel Production が自分の commit で成功したことを確認する。** Vercel MCP の `list_deployments`（project `tuat-tf` = `prj_fLc2aGQHb2Ny4IMt21ESzN0ij25p` / team `team_kxzFfl8gjcBDe6W8AayTW156`）で、最新の deployment が `target: "production"`・`state: "READY"`・`meta.githubCommitSha` が自分の commit、の3つを満たすことを見る。`target: null` はブランチのPreviewなので**本番ではない**。ビルド中（`BUILDING`）なら READY になるまで待つ。失敗していれば直してから再度この手順を回す。Vercel MCP が使えないセッションでは、その旨を伝えてオーナーに確認を依頼する。
5. 報告に **commit・master へ入ったこと・Production の状態** を書く。実機（iOS PWA）で見ていないなら「実機確認は未実施」も必ず書く。

- migration・同期・一括更新など本番データを壊しうる変更は、従来どおり**スナップショット取得と dryRun をこの手順の前に**行う（厳守ルール参照）。
- 途中で止めるとき（レビューを挟みたい・オーナーの判断が要る）は、**どこまで進めたか**と**本番にはまだ出ていないこと**を明示する。黙ってブランチ止まりにしない。

## ドキュメント索引
- `docs/README.md` … **現行の文書案内（運用・規約・日付付きの検証記録）**
- `docs/ARCHITECTURE-REFACTOR-PLAN.md` … **巨大ファイルの分割計画**（Phase 1=queries/sheet-sync 実施済み。Phase 2以降の順番と、PC試験版の残骸を消す時期）
- `ops/laptop/SERVER-HANDOFF.md` … **サーバーを別PCへ引き継ぐ手順**（鍵の選択・R2からの復旧・切替と切り戻し）
- `docs/UI-UNIFICATION.md` … UI・操作・システムの **統一規約**（書く=全画面 / 選ぶ=シート、**編集削除=⋯ ActionMenu**、ガクつき禁止、押下は `active:bg-bg` か `pressable` の2種だけ、取得=`lib/queries/`、DBへの往復を直列に積まない、`staleTimes`/`cacheComponents` 禁止）
- `docs/WORDING-GUIDELINES.md` … **文言ガイドライン**（部員向けの言葉づかい統一。開発用語→平易語の対応表・エラー文の型・適用チェックリスト）
- `docs/NOTIFICATIONS-PLAN.md` … 通知機能（通知センター・受信設定・Web Push）※実装・本番投入済み
- `docs/SHEETS-SYNC-PLAN.md` … 練習記録のスプレッドシート同期（コードのコメントから参照している）
- `docs/AUDIT-2026-09-21.md` … **実測監査の要修正リスト**（セキュリティ・停止中の機能・転送量・無料プラン化の条件）
- `docs/COMPETITION-RESULTS-PLAN.md` … 大会記録の入力と既存データ正規化（正規化は未完）
- `docs/R2-MIGRATION.md` / `docs/EGRESS-AUDIT.md` / `docs/SERVICE-STATUS.md` … 画像のR2移行・通信量・外部サービスの状態画面

> **2026-09-13に削除した文書**: `UX-ISSUES-2026-06.md` / `UI-AUDIT.md` / `SCHEDULE-MENU-PLAN.md` /
> `NOTES-PLAN.md` / `SHEETS-IMPORT-PLAN.md` / `QA-CHECKLIST.md` / `ui-data-guidelines.md` /
> `CONSISTENCY-AUDIT.md` / `TASK-codex.md` / `TASK-claude-ui-2026-06-21.md` / `SPA-TAB-ARCHITECTURE-REVIEW.md`。
> いずれも**実装済み機能の当時の指示書**で、済んだ項目を未着手のまま載せていた。内容はGitの履歴にある。
> 今も効いている規約は `UI-UNIFICATION.md` に取り込んである。

## 実装バックログ（2026-07-03 全体コードレビュー → オーナー確定。ここが現在の実装指示）

> 2026-07-03 の全体レビュー（Claude Code / Fable 5）でオーナーが採否を確定した作業リスト。
> **1タスク＝1コミット**。着手前に対象コードの現状を必ず確認すること。
> マイグレーションは従来どおり新タイムスタンプ＋冪等で作成し `"Y" | npx --yes supabase db push` で本番適用。
>
> **着手順（オーナー確定 2026-07-10 改訂。旧: 2026-07-03版）:**
> `13`（週次CSVバックアップ・**最優先**。本番DBはPITR/バックアップ無しのまま書き込み経路だけ増えており、事故ったら戻せない状態が続いている。タスク2完了直後に実施推奨だったが未着手のまま）
> → `16残のfetchAllRawタイムアウト対策`（65部員で約70秒 vs maxDuration=60。100人想定では毎時同期が黙って死ぬ。**着手前に必ず実測**）＋`14残`（連続失敗アラート・シート名不一致の可視化。「死んだことに気づけない」問題とセットで解消）
> → `7残の週間サマリー中長距離限定`（home/page.tsx の WeeklySummary が今も全部員表示。小修正）
> → `12残`（⋯ボタン統一）→ `9`（@メンション）→ `8`（検索）→ `5残`（P2カーソル化・P5引数削除）→ `10残`（a ワンタップ再取込・c 表形式一括入力）→ `11-b`（まとめて入力）→ `4-S3`（GAS secret のPOST化）→ `6残`（型生成・分割等）→ `7残のSWオフライン対応`。
>
> 完了済み: `0` `1` `2` `3` `6-Q3` `7の未来日ブロック` `10-b` `11-a` `4-S4` `5-P1/P3/P4` `12のコメント500字・propsフェッチ` `16主要部`（各節・作業ログ参照）。
>
> **タスク16（練習記録のスプシDB化）は2026-07-10に主要部分を実装・本番反映済み**（詳細は該当節と作業ログ参照。残作業あり）。（旧タスク15＝感想欄未反映バグは、真因「タブに感想見出しが無い」をオーナーがシート側で解消したため削除済み 2026-07-09）
> ※`4-S5`（暗号鍵分離）は Drive 連携者の再連携が必要なため、単独でオーナーとタイミング調整のうえ最後に（この順序リストにも含めない）。

### 前提となる方針転換（オーナー決定・全タスクに影響）
- **承認ゲート（profiles.approved）は廃止**する。理由: 部外の同大生が入ってくる可能性は実質なく、承認フローが不便。部外生ログイン時の閲覧リスクはオーナー了承済み。
- **スプシ同期は双方向をやめ、部員ごとに方向を固定**する（record_source）。LWW・競合スキップ等の複雑さを撤去する。
- **スプシ由来の記録もタイムラインに表示**する。並び順は**「練習日の0時(JST)に投稿された」扱い**（オーナー確定 2026-07-12。当初の「created_at＝取込時刻のまま」はまとめ取込のたびに先頭で団子になりタイムラインが荒れたため変更。insert時にcreated_at=練習日0時を設定＋既存分はmigration 20260712150000でバックフィル済み）。

### 0. 【バグ・最優先】記録フォーム設定（カスタム項目）が保存されない
症状: マイページ→設定→「記録フォームを編集」で項目を追加・保存しても反映されない（オーナー報告 2026-07-03）。
**切り分け済み（2026-07-03 Claude Fable 5、本番DBで実測）:**
- DB層は正常。使い捨てテストユーザー（検証後削除済み）の authenticated JWT で `PATCH /rest/v1/profiles` の `record_fields` 更新が**成功**する。RLS・列権限・スキーマに問題なし。
- オーナーの本番 `profiles.record_fields` は `[]` のまま＝**クライアント側で更新が送信されていない／無言失敗している**。
- オーナー実機の症状: **保存を押すとモーダルは閉じる（＝safeUpdateが成功扱い＝1行更新が返っている）のにDBは`[]`のまま**。→「空の`fields`で保存が成功している」可能性が最有力。追加操作（addFieldのSheet）で項目がstateに入っていない、またはaddField内で例外が握り潰されている線を最初に疑う。
- 調査手順0: まずVercelの本番デプロイが最新コミットか確認（古いビルド配信の可能性を排除）。次にオーナー実機で「追加→リストに項目が見えるか」を確認してもらう。
- 容疑箇所（この順に潰す）: ①`RecordFieldsSetting.tsx` の save→`safeUpdate` 経路（失敗時 message は出るが気づきにくい）②`FormModalFooter` のポータル登録レース（footerCount→target 設定順で保存ボタンが実際に描画されているか）③`ReorderList`（dnd-kit PointerSensor）が行内の入力・ボタンのタッチイベントを奪っていないか（実機タッチで要確認）④保存ボタンがキーボード表示中に可視か。
- 修正時は実機（iOS PWA）で「項目追加→保存→再度開いて残っている→DBの record_fields に入っている」まで確認すること。

### 1. 承認ゲート廃止
- migration: `profiles.approved` を DEFAULT TRUE 化し、既存の FALSE 行を TRUE にバックフィル。新規ユーザー作成トリガー（handle_new_user 相当）も approved=TRUE で作ることを確認。**approved 列と is_member() は残す**（RLSポリシー群は書き換えない。全員 TRUE になるので実質 authenticated ゲートとして機能し続ける）。
- `src/app/(app)/layout.tsx` の `if (!profile.approved) redirect("/pending")` を撤去。`src/app/pending/` を削除。
- `src/app/(app)/home/page.tsx` の PendingApprovalBanner、`queries.ts` の getPendingProfiles、members 画面の承認UI（set_member_approved 呼び出し）を撤去。RPC 自体はDBに残してよい。

### 2. スプシ同期を「部員ごとの方向固定」へ（最重要）
- migration: `profiles.record_source TEXT NOT NULL DEFAULT 'app' CHECK (record_source IN ('app','sheet'))`。バックフィル: **sheet_name 連携済みの部員は 'sheet'、未連携は 'app'**（従来のスプシ取込を切らさないための初期値。部員には切替方法を周知）。
- `src/lib/sheet-sync.ts` を再構成:
  - record_source='sheet' の部員 → **pull のみ**（シート→アプリ、from_sheet=true）。**2026-07-25以降は、ユーザーが確認した対応列について空欄も意図した削除として取り込む**。未マップ列とシートに行が無い日は触らない。アプリ→シート書き戻しはしない。
  - ⚠️ **2026-07-03 インシデント**: 当初仕様「シートの空欄も反映してよい」（Fable 5起案）で実装した結果、17:00 UTCのcron同期でアプリ側にだけあった6レコードの内容がヌルクリアされた（部員2名、復元不能=PITR/バックアップ無しをCLIで確認済み）。5de45d5で非破壊に修正済み。この事故履歴は保持する。ただし2026-07-25にオーナーが、全連携者をスプシ入力へ統一し、アプリもCSV既存値を編集する前提なら空欄保存は意図した削除であると明示したため、**確認済み対応列に限って空欄反映へ方針変更**。列変更時の再確認と本番前スナップショット・dry-runを安全条件とする。
  - record_source='app' の部員 → **push のみ**（アプリ→シート）。シート側のマップ済みセルは同期が上書きしてよい（シート＝写し）。シート→アプリ取込はしない。
  - これに伴い LWW（appIsNewer / updated_at・synced_at 比較）、同日複数記録の conflict スキップ、双方向前提の空値保護ロジックを撤去・簡素化。SYNC_CUTOFF・未来日除外・dryRun・sheet_sync_runs ログ・手動同期ボタンは維持。
  - practice_records の読み込みに `.gte("recorded_date", SYNC_CUTOFF)` を付ける（下記 5-P3 と同時解消）。
- UI: ProfileEditForm のシート選択の隣に「記録の入力元: アプリ / スプレッドシート」切替を追加。'sheet' の部員は自分の記録をアプリ内で**編集も新規作成も不可（閲覧のみ）**にし、「入力元をアプリに切り替えると編集できます」と案内（2026-07-03の事故は sheet 部員がアプリから新規作成できたことが一因）。
- **切替時リコンサイル（オーナー確定 2026-07-04・必須）**: record_source を切り替えた瞬間に、その部員だけを対象に一度だけ「両方を揃える同期」を自動実行してから方向を固定する。app→sheet: アプリの中身のある項目をシートへ書き出してから切替。sheet→app: シートの中身のある項目をアプリへ取り込んでから切替。いずれも非破壊（空で相手を消さない）。**両側に中身があり内容が食い違う場合は「切替前まで正だった側」の値で揃える**（app→sheet切替ならアプリが勝ち、sheet→app切替ならスプシが勝ち。2026-07-04 オーナー確認）。同日複数記録など曖昧なケースは触らずスキップして結果に報告。失敗したら切替自体を中止してエラー表示。実装時は必ずdryRunで差分を確認してから本番反映（厳守ルール参照）。
- `/api/sheets/reply`（コメント→シートのリプライ列追記）は追記専用で衝突しないため現状維持。

### 3. スプシ由来記録のタイムライン表示
- `src/lib/queries.ts` getFeed / getUserActivity の `.eq("from_sheet", false)` フィルタを撤去（全ブロック共通）。並び順は created_at のまま。
- from_sheet 列は出典・同期方向の内部管理用に残す。RecordCard に小さな出典表示（「スプシ」バッジ等）を付けてよい（任意）。
- RecordForm で from_sheet:false が短距離側 payload にしか付いていない非一貫は「**insert 時のみ設定・update では変更しない**」に統一。

### 4. セキュリティ残タスク（承認ゲート関連の指摘は上記1により消滅）
- S3: `src/lib/sheet-sync.ts` gasGet のクエリ文字列 secret 送信をやめ、listMembers / fetchAllRaw も POST ボディ化（GAS 側 doPost 対応＋clasp で再デプロイ）。
- S4: `src/app/api/sheets/sync/route.ts:28` の Bearer 比較を `crypto.timingSafeEqual` に。
- S5: `src/lib/google-drive.ts` の暗号鍵を service role key 派生から専用 env `GOOGLE_TOKEN_ENC_KEY` へ分離。**切替時は Drive 連携者全員の再連携が必要**なので、実施はオーナーと本番反映タイミングを合わせること。

### 5. パフォーマンス（全採用）
- P1: `queries.ts` getNotices / getPersonalNotifications / getUserRecords（fromDate なし呼び出し）に limit または期間窓（通知は直近50件＋ページング目安）。**済**（2026-07-04）。
- P2: `TimelineView.tsx` の「もっと見る」を limit 増の全件再取得から**カーソル式**（最後の created_at より古い分を追加取得）へ。**未着手**（2026-07-10確認: 今も `loadMore` が `limit+PAGE` で0件目から再取得する設計のまま。押すたびに読む量が線形に増える。cursorはrecords/tweetsの2クエリをマージしてる関係で両方に必要）。
- P3: sheet-sync の全履歴取得に `.gte("recorded_date", SYNC_CUTOFF)`（タスク2と同時）。**済**。書き戻しの直列 GAS 呼び出しの一括化は未着手。
- P4: `queries.ts` fetchCommentCounts を全行取得→JS集計から count 集計へ。**済**（2026-07-04）。
- P5: getFeed の limit 後フィルタ問題は、タイムラインがクライアント側フィルタなので **getFeed の block/grade 引数を削除**して一本化。**未着手**（2026-07-10確認: 実際の呼び出し元＝home/timeline/TimelineView は全部 `"all"` しか渡しておらず、引数自体が死んでいる。実害は今のところ無いが、将来「サーバー側でlimit後にfilterして件数が減る」を踏み抜く地雷なので削って一本化推奨）。
- P6（2026-07-10追加・メモのみ）: マイページの即時スプシ反映（`refreshMemberFromSheetLive`、タスク16残作業の実装）が 'sheet' メインの部員のマイページ表示に毎回 GAS 呼び出し（最大5秒タイムアウト）を挟む。他のP項目と違い「DBクエリの効率化」ではなく「外部通信を都度挟む設計そのもの」なので対応方針の判断が要る（例: 短時間キャッシュ、非同期化してSuspenseで後追い表示、等）。**実装せずメモのみ。次のエージェントの判断に委ねる**。

### 6. コード品質（全採用。修正によるデメリットなしと判断済み）
- Q1: `npx supabase gen types typescript` の生成型へ移行し `as unknown as`（21箇所）を解消。
- Q2: from_sheet の扱い統一はタスク3に含めて解消。
- Q3: 「今日」の実装を JST 基準の共通 util（jstToday 等）1個に集約。`queries.ts:240,441` の `new Date().toISOString().slice(0,10)`（UTC）は**実バグ**（JST 0〜9時に前日の予定が今後の予定に残る）なので優先的に。
- Q4: MenuForm.tsx（958行）/ ScheduleSheetsManager.tsx（1015行）の分割（対象者ピッカー・プリセット・プレビュー表などの単位で）。
- Q5: getFeed / getUserActivity の FeedItem 組み立て重複（各約40行）を共通ヘルパーへ。

### 7. UI/UX（採用分）
- 記録フォームの日付に未来日を入力できないようにする（max 属性＋バリデーション。保存後に「消えた」ように見える問題の解消）。**済**（2026-07-04）。
- JST統一は 6-Q3 で解消。**済**。
- ホームの週間サマリー（走行距離・練習回数カード）は**中長距離ブロックの部員のみ表示**。他ブロックには表示しない（週間ランキングも従来どおり中長距離のみ）。**未着手**（2026-07-10 コード確認: `home/page.tsx` の WeeklySummary は今も全部員に表示。profile.blocks を見て middle_long を含む部員だけ描画する小修正）。
- Service Worker にオフラインフォールバック＋静的アセットキャッシュを追加（現状 push 専用で、オフラインだと開けない）。**未着手**（2026-07-10確認: public/sw.js にキャッシュ処理なし）。優先度は低め（着手順の最後尾）。

### 8. ノート・お知らせの検索
- ノート（フォルダ・記事）とお知らせを対象にしたテキスト検索を追加。Postgres の ilike で十分（全文検索基盤は不要）。UI はノートタブ・お知らせ画面それぞれに検索欄。

### 9. お知らせの通知先を @メンション式に刷新
- 現在の「全員/複数ロール」選択UIをやめ、`@ロール名` `@部員名` のメンション式へ（@入力でロール・部員のサジェスト表示）。
- メンション対象: ロール→所属部員全員、個人→その部員。重複しても通知は1人1通（20260629120000 の重複排除ロジックを流用）。
- notices にメンション保存列（例: mentioned_role_ids uuid[], mentioned_user_ids uuid[]）を追加し、通知トリガー/関数をメンション基準に改修。本文中のメンションはハイライト表示。
- 全員通知は **`@All`**（オーナー確定 2026-07-03）。サジェストの最上位に「@All（全員）」を出し、従来の notify_members トグルは廃止。メンションが1つも無いお知らせは通知を送らない（掲示のみ）。

### 10. 予定の一括運用を楽にする（2026-07-03 オーナー確定。運用は当面スプシメイン）
**実際の雛形スプシの構造（2026-07-03 オーナー提供の実物をCSVエクスポートで確認済み。URLは公開リポのためここに書かない。オーナーが schedule_sheets 登録時に指定する）:**
- 冒頭数行は自由メモ（練習理論の解説等）。ヘッダー行はその下（「曜日・時間・場所」を含む行を探して特定する。日付列のヘッダーは固定名でない）。
- 列: `日付 | 曜日 | 時間 | 場所 | メニュー | ペース | 補足 | 補強`。**予定（時間・場所）とメニュー（メニュー/ペース/補足/補強）が同居**している。
- 日付は `6/1` 形式で**年なし**。タブ＝月単位で、月末に翌月初日がはみ出すことがある（例: 6月タブに 7/1）。対象月から年を補完し、月跨ぎも正しく解釈する。
- **全日付の行が最初から存在する**（行がある＝練習日ではない）。時間・場所・メニューのいずれかに中身がある行だけを予定として取り込む（**オーナー確認済み 2026-07-03**。補強列だけの日は予定にせずメニュー情報としてのみ扱う）。
- 取込は予定(practice_schedules)だけでなく、メニュー列から**ブロック全体メニュー(practice_menus)も同時に生成/更新**する（メニュー/ペース/補足/補強を本文に整形）。
- この雛形は**中長距離用**（オーナー確認済み）。短距離のスプシは非公開のため、当面はこの形式のみ対応し、シート登録時にブロックを指定する設計にしておく。
- 現行の `schedule-import.ts` はヘッダー名完全一致・全行=予定前提のため、この実物形式に合わせたパーサの改修が a)〜c) すべての前提になる。
- a) **スプシ登録は初回1回だけ・以後ワンタップ再取込**: schedule_sheets に登録済みのシートを予定画面から選んで「取込」一発で差分反映できる導線に（毎回のURL発行・貼り直しを廃止）。既存の preview/検証/RPC 経路を再利用。
- b) **定期自動取込**: pg_cron で登録済み予定シートを毎日チェックし差分を自動反映。**非破壊**（追加・更新のみ。削除はアプリからのみ）＋取込結果（件数・エラー行）を管理者に通知。記録同期の Bearer 認可・runsログの仕組みを踏襲。
- c) **アプリ内「表形式で一括入力」**: 取込プレビューの編集表（EditablePreviewTable）を流用し、シート無しでも同じ表を空から編集して一括登録できるモードを追加。月を選ぶと日付・曜日行を自動生成し、曜日デフォルト（weekdayDefaults: 月水17:00・土9:00＋場所）をプレフィル。検証・登録は既存インポート経路をそのまま通す。将来のアプリ一本化への布石。

### 11. メニュー作成の手間削減（2026-07-03 オーナー確定）
- a) **プリセット機能を「過去メニューから複製」に置き換え**: 作成画面に履歴ピッカー（自分が過去に公開したメニューを新しい順に表示・検索）を置き、選ぶと本文・対象ブロック・対象者が入る。手動の「プリセット保存・名前付け・上書き」操作を廃止（localStorage プリセットのコードも撤去）。直近値の自動初期値（対象者引き継ぎ）は維持。
- b) **まとめて入力**: 予定のある日（今週/今月）を複数選択 → 日ごとに本文を編集できる縦リストで一括保存。対象ブロック・対象者は共通指定。a) の複製と組み合わせ「先週分をコピーして今週3日分に貼る」を数タップにする。

### 12. 細かいUI改善（2026-07-03 オーナー確定）
- 編集・削除の導線を **⋯ボタンに統一**し、スワイプ＋長押しは廃止（`docs/UI-UNIFICATION.md` の「編集削除=スワイプ＋長押し」規約は**失効**。矛盾があれば本節が正）。
- コメントの最大文字数を 200 → **500** 字に（入力欄・DB制約があれば両方）。
- RecordForm のカスタム項目を開くたびの DB フェッチ（RecordForm.tsx:69-80）をやめ、取得済みプロフィールから props で渡す。

### 13. 週次CSVバックアップ（2026-07-04 オーナー確定。**現在の最優先タスク＝キュー最前列** 2026-07-10 オーナー確定）
- 週1回、主要テーブル（practice_records / profiles / notes / note_articles / notices / practice_schedules / practice_menus）を自動でエクスポートして保管する。
- 実装はシンプル優先の例: Bearer認証の `/api/backup` を新設（service roleで全件をCSV/JSON化）→ GASの時限トリガーから週1で叩いてオーナーのDriveへ保存（sync-api の Bearer/clasp 構成を踏襲）。直近8世代を保持し古いものは削除。
- 背景: 本番Supabase（Free）はPITR・バックアップ無し。2026-07-03の消失事故では戻す手段が無かった。当初「タスク2の直後に実施推奨」だったが未着手のまま経過し、その間にwrite-through等で書き込み経路だけ増えている。**他の全タスクの事故保険になるため最優先**（2026-07-10 オーナー確定）。

### 14. 同期の運用堅牢化（2026-07-04 オーナー確定。タスク2と同時に実施）
背景: 2026-07-02 13:00〜07-03 16:00の**28時間、毎時の同期が「シート『B3 山端』が見つかりません」で全滅していたのに誰も気づけなかった**（sheet_sync_runsに記録は残っていたが通知が無い）。1人のシート不調で全員分の同期が止まる作りだったことが、事故の遠因（失敗が安全弁を兼ねていた→タブ復旧と同時にバグ入りpullが発火）。
- **部分失敗設計**: 部員ごとに try/catch し、1人の失敗（タブ改名・削除等）で他の部員の同期を止めない。失敗した部員と理由を sheet_sync_runs に記録。**済**（2026-07-04）。
- **連続失敗アラート**: 同期が連続N回（目安3回）失敗したら、**システム管理ロール（can_manage_system 保持者）**へアプリ内通知＋Web Push（既存の通知基盤を流用。通知先は管理者=can_manage_members ではない。オーナー指定 2026-07-04）。**未着手**（着手順でタスク16残のタイムアウト対策とセットで実施 2026-07-10）。
- **"running"放置の解消**: タイムアウト等でrunが"running"のまま残る問題（07-02 20:00 / 07-03 14:00 に実例）。finallyで必ず終了状態を書く＋次回実行時に一定時間過ぎたstale runをerrorへ倒す。**済**（2026-07-04）。
- **シート名不一致の可視化**: GASのタブ一覧と profiles.sheet_name の突合結果（不一致者）を管理者の同期画面に表示（タブ改名の早期検出）。**未着手**（連続失敗アラートと同時に実施）。
- **pg_cronのジョブ名は用途ごとに固有名を厳守**（記録同期=`sheet-sync-hourly`、予定取込=`schedule-import-hourly`）。`cron.schedule` は同名で黙って置き換えるため、名前を共用すると既存ジョブが消える（2026-07-04に記録同期cronが約20時間消失した実例）。cron登録SQLをオーナーに案内する際は、必ず `select jobname from cron.job` での事前確認手順を添える。

### 15.（削除済み 2026-07-09）感想欄がスプシに反映されないバグ
真因は「部員タブに『感想』『コメント』見出しの列が無いと、名前ベースのマッピングが黙ってスキップする」こと（実物CSVで確認。タブごとに列構成が異なり、感想列が無いタブが実在した）。**オーナーがシート側に見出しを追加して解消済みのためタスクとしては削除。** 教訓として「未マップ項目の可視化（黙って落とさない）」はタスク16の一項目に引き継ぐ。位置固定フォールバック（旧TFの17列目方式）はタブによって別の列を汚すため採用しない。

### 16. 練習記録の「スプシDB化」（'sheet'部員＝旧TFアプリ方式。オーナー確定 2026-07-09）
方針: 練習記録に限り、部員ごとに**メインDBを選べる**ようにする（個人設定「記録のメイン: アプリ / スプレッドシート」＝record_source の意味を再定義）。**スプシメインの部員は旧TFアプリ（D:\AI\Antigravity\TF）と同じ仕組み**＝スプシが唯一の正で、アプリは「スプシを読み書きする端末」。アプリメインの部員は従来どおりSupabaseが正で毎時push。**いいね・コメント・投稿時刻・通知は全部員Supabaseメイン（オーナー確定）**。想定部員数は**100人**。「一旦広めるため」＝スプシで生活している部員が行動を変えずにアプリのソーシャル機能を使えるようにするのが目的。

**2026-07-10 実装・本番反映済み（Claude Code Sonnet 5）:**
- **write-through（保存→即スプシ反映）を実装**。RecordForm の 'sheet' ブロック（タスク2で追加した閲覧専用表示）を撤去し、通常の編集フォームに統一。保存後、`/api/sheets/push-record`（本人のみ・ユーザーセッション認可）が非破壊push（`appToCellsNonEmpty` 流用）でGAS `writeCells` を呼ぶ。GAS書き込みが失敗してもDB保存自体は失われない（Supabaseへの保存が先行し、既に成功済み）が、成功したとは見せずtoastで警告し、`practice_records.pending_sheet_push`（新設・migration 20260710100000）をtrueにして次回毎時同期での再送対象にする。
- **GAS拡張**: `fetchMember`アクション追加（1部員だけ軽量取得。個人の記録画面・write-through確認用。100人規模でも毎回fetchAllRawを引かずに済む）。`writeCells`が書けなかった見出し(`unmapped`)を返すよう変更（未マップ項目の可視化。旧タスク15の教訓を反映）。clasp pushで本番反映済み（deployment `AKfycbyXUDkqE9BdgdNJmb9sGSZK6CTKt_J7OTyLuLvKXwZC3tPexS-i_XkndjuluQ33D7UK @6`）。
- **記録カード（RecordCard/PostOwnerMenu）の編集可否**: 記録のメインが'sheet'の部員は、from_sheet=trueの過去記録（過去のpull由来）も含めて自分の記録を編集可能にした（従来は from_sheet=true を一律「編集不可・閲覧のみ」にしていた）。`AuthorMini`に`record_source`（optional）を追加し`queries.ts`のAUTHOR_SELECTで取得。
- **⚠️ dry-runで実際に問題を発見・修正済み**: 当初、毎時同期側の「write-through失敗時の再送」を既存の`appIsNewer`（updated_at>synced_at比較）で判定する設計にしたところ、本番dry-runで**write-through導入前からの無関係なタイムスタンプのズレ**（'sheet'部員6名・8件、原因不明の過去データ）を「再送対象」に誤検知し、部員がスプシへ直接入力した内容を古いアプリ値で上書きしかねないことが判明。専用フラグ`pending_sheet_push`（write-through失敗時のみtrue、成功でfalseに戻す）に設計変更し、再dry-runで`pushed:0`（誤検知解消）を確認してから本番migration適用・push。**厳守ルールのdry-run確認がまさにこの事故を防いだ実例**。
- **Supabaseミラー（内部配管）**: pull-onlyを維持し、2026-07-25以降はユーザー確認済みの対応列を空欄も含めて反映する。'sheet'部員の記録は毎時cronと画面表示後のCSV更新で取り込まれ、いいね・コメント・タイムライン・週間集計のキーとして機能する。
- **個人の記録画面の即時反映（2026-07-10実装）**: マイページ・自分の`/members/[id]`表示時に`refreshOwnSheetRecords`（`queries.ts`）→`refreshMemberFromSheetLive`（`sheet-sync.ts`）が`fetchMember`で本人1人分だけ軽量取得し、毎時cronを待たずその場でシート正の内容をDBミラーへ反映してから記録を読む。cronのpull-onlyロジックは`computeMemberPull`として共通化（cron本体の挙動は不変・純粋な抽出）。GAS呼び出しは5秒タイムアウトで失敗しても例外を投げず、DBの現状のまま表示する（ページを壊さない）。他人の記録閲覧時（isSelf=false）は呼ばない（RLSで書けないうえ無駄なGAS呼び出しになるため）。
- **1日1記録の制約**: 既存のRecordForm「同じ日の記録があれば新規ではなく更新」ロジック（重複防止）がそのまま適用されるため追加実装不要だった。

**残作業（次のエージェントが着手する場合はここから）:**
- 100人規模でのfetchAllRaw応答時間を実測したところ**65部員で約70秒**（2026-07-10実測）。既存の`/api/sheets/sync`ルートは`maxDuration=60`のため、**部員数がこれ以上増えるとhourly cronがタイムアウトする可能性が高い**（今回のタスク16実装より前からの既存リスク）。対応候補: ①maxDurationを引き上げる（Vercelプランの上限を確認）②fetchAllRawをfetchMemberの並列呼び出しに置き換える③タブ分割バッチ化。**次のエージェントは着手前に実測すること**。
- 未マップ項目(`unmapped`)をtoastで警告表示するようにしたが、記録フォーム側にも恒常的な表示（例:「この項目はスプシに列が無いため保存されません」の事前案内）はまだ無い。
- 実機（iOS PWA）での動作確認は未実施。特に: ①'sheet'部員が記録を新規作成→保存直後にスプシに反映されるか ②GAS書き込みをわざと失敗させて`pending_sheet_push`が正しく再送されるか ③from_sheet=trueの記録がRecordCardから編集できるようになっているか。

### 17. ノート構造の拡張（オーナー確定 2026-07-13。フォルダ入れ子・スレッド・FAB3択）
オーナー承認済みの叩き台（2026-07-13会話）: 容量は実測14MB/500MBで問題なし。テキストのみなら数年余裕。画像添付を入れる時だけStorage/転送量の再設計が必要。
- **a) フォルダの入れ子**: `notes.parent_id uuid REFERENCES notes(id)`（NULL=ルート）を追加。**深さ3階層まで**（UI側で制限）、**フォルダ削除は中身（サブフォルダ・記事）が空のときのみ**。一覧（NotesView）はルートのみ表示し、フォルダ詳細にサブフォルダ節＋パンくず（例: 共有 > 練習理論 > 補強）を追加。
- **b) スレッド（複数人で話せる場所）**: **v1は掲示板スタイル**（リアルタイム配信なし・時系列返信のみ。LINE化はしない=オーナー了承）。ノートタブ内に「共有/個人/スレッド」の3タブ目。誰でもスレッド作成・返信可。通知は返信時にスレッド参加者（作成者＋返信者）へ既存通知基盤で。実装方式は notes.kind='thread'＋note_articles流用か専用テーブルかを実装者が判断（RLSは「thread配下は全員insert可・編集削除は本人のみ」が要件）。
- **c) FABの3択化**: /notes でFABを押すと「フォルダ / ノート / スレッド」の選択シート（選ぶ=シートの規約）。「ノート」（=記事）はルートから作る場合に保存先フォルダを選ばせる。フォルダ作成は親フォルダ選択（ルート/既存フォルダ、深さ3まで）。
- **d) フォルダのアイコン修正**: BookOpen（開いたノート）→ Folder系アイコンへ（NotesView・ホームのノート節。「フォルダと分かりにくい」オーナー指摘）。**済 2026-07-13**。
- **e) フォルダの移動**（オーナー要望 2026-07-13・未実装）: フォルダ設定編集（NoteEditor）に「場所（親フォルダ）」選択を追加。制約: 同scope内のみ・自分自身と子孫は選べない（循環禁止）・移動後もツリー全体が深さ3以内。候補一覧はRLSで見えるnotes(id,title,parent_id,scope)から構成。
- 新テーブル/列は週次バックアップ（/api/backup）の対象テーブルへの追加を忘れないこと。

### 不採用（実装しないこと。過去の提案を根拠に着手しない）
- メニュープリセットのDB保存化（プリセット機能自体を 11-a で置き換えるため不要）。
- 承認待ち画面の改善（リアルタイム承認検知）… 承認ゲートごと廃止のため不要。
- ダークモード。
- 記録・つぶやきのオフライン下書き保存。
- コメントの @メンション（お知らせのみ対応する）。
- 出欠リマインド通知・カレンダー(ICS)連携・PB自動判定 … 今回は見送り（禁止ではなく未承認）。

## 衝突防止：固定の担当領域は設けない（柔軟運用）＋必ず報告する
どのAIも領域を限定しない（柔軟に動けるように）。代わりに**「何を触るか／何をしたか」を必ず下の作業ログに報告**して可視化で事故を防ぐ。
- **作業は1体ずつ。`git pull`→作業→検証→push まで終えてから次のエージェントに渡す**（push 前の中途半端な状態で別の体に着手させない）。
- 着手したら**着手前に必ず `git pull`**（受け渡し直後でも省略しない）。
- **着手前**：下の「作業ログ」に1行追加（日付・エージェント名・これから触る範囲）。
- **完了後**：同じ行に結果（commit ハッシュ・要点）を追記。大きな変更は日付付きの文書へ詳細を書き、`docs/README.md` から参照できるようにする。
- 他AIが直前に触った範囲は、ログを見て**現状コードを確認してから**触る（古い前提で上書きしない）。

## 作業ログ（着手前に追記・新しいものを上へ）

- 2026-09-26 / Claude Opus 5.5 / **オーナー指示で全員をクラウドのログインへ切替**（16:00 JST ごろ）。Vercel Production の NEXT_PUBLIC_CLOUD_AUTH_MODE を削除して再デプロイ。以後、ログインはクラウド（cookie sb-tuat-auth）、データはPC。旧PCログイン（sb-pc-backend-auth）は使われなくなり、部員は1回ログインし直す（オーナー了承・告知なし）。切り戻しは同変数を test に戻して再デプロイ。 -> (this commit)
- 2026-09-26 / Claude Opus 5.5 / ①ページを開くたびの「ログイン中か」の確認を getUser（ログインサーバーへ問い合わせ）から getClaims（公開鍵で署名を確認、公開鍵は10分ごとに取得、期限切れのときだけ更新）へ変更（src/lib/supabase/middleware.ts）。PCのログインもES256でJWKSを公開しているので両方に効く。本番でクラウド・PCそれぞれのログインでホームが表示されることを確認。API Routeの getUser は据え置き。②OB戦: 新規登録の部員選択・種目の出場者・回答一覧をB1からの学年順に。補助員一覧の各役職に「人を編集」を追加し、候補はその時間帯に出場しない部員を学年順、各人の次の競技／補助員と時刻を表示（保存は外す人→加える人の順に既存RPCを1人ずつ）。役職名・必要人数は「設定」から。466テスト・tsc・対象eslint・build成功。02d6ecf・11a4542、Production READY、本番で画面表示を確認（保存は未試行）。実機確認は未実施。 -> (this commit)
- 2026-09-26 / Claude Opus 5.5 / Supabase予備構成・ログイン切替の準備。`src/lib/supabase/cloud-auth.ts`: NEXT_PUBLIC_CLOUD_AUTH_URL / _ANON_KEY を入れると、Supabaseクライアントをクラウドの URL で作り、/auth はクラウド、/rest・/storage・/functions はPCへ振り分ける（サーバーは pcBackendFetch、PCへは apikey と未ログイン時の Authorization をPCの anon キーに付け替え）。cookie は新名 sb-tuat-auth（切替時に全員1回再ログイン）。callback でPCに同じIDのアカウントが無ければ管理APIで作る（PCのトリガーが部員名簿も作る）。NEXT_PUBLIC_CLOUD_AUTH_MODE=test の間は cookie `tuat-cloud-auth-test=1` を持つブラウザだけ有効。Vercel Production に3変数（MODE=test）を設定。464テスト・tsc・eslint・本番同形のダミー設定でbuild成功。9/26 夕、オーナーがクラウドのURL設定（Site URL=https://tuat-tf.vercel.app、Redirect URLs に https://tuat-tf.vercel.app/**）を確認。アプリ内ブラウザに目印cookieを付けてオーナーが実際にGoogleでログインし、クラウド発行（ES256、iss=クラウド）のトークンでPCのデータを中継・Vercel経由とも読込、ホームのサーバー描画も正常。全員の切替（MODE を外して再デプロイ）は未実施。 → (this commit)
- 2026-09-26 / Claude Opus 5.5 / Supabase予備構成・写し。①大学ドメイン以外の新規登録を拒むフック関数（20260926020000、有効化はクラウドのダッシュボードで未実施）②写しの書き込みを見分ける is_mirror_write() と、写す表の全トリガー（計23関数）で写しの時は何もしない条件（20260926030000/040000）。いずれもPC・クラウド両方にBEGIN…ROLLBACK確認後に適用、PCの動作は不変。③PCにだけいた部員6人をクラウドAuthへ同じID・メールで作成。④`ops/laptop/cloud-mirror.mjs`（npm依存なし）で、名簿・ロール・予定・メニュー・大会・お知らせ等は全件、練習記録・つぶやき・コメント・いいね・出欠は直近3日分をクラウドへ写し、初回適用後の再比較で差分0・クラウドに余計な通知/予定なし。⑤常駐の run-backend.mjs に5分ごとの写し（別プロセス・失敗しても中継に影響なし、結果は .contingency/backend/cloud-mirror-status.json）を追加。オーナー許可で9/26 15:04に restart-backend.ps1 で再起動（26秒で復帰）、15:05の初回自動写しが成功（2.4秒）。オーナーがクラウドのBefore User Createdフックに hook_restrict_signup_by_email_domain を設定済み。 → (this commit)
- 2026-09-26 / Claude Opus 5.5 / Supabase予備構成・段階3の前半。オーナーがクラウドのJWTを公開鍵方式へ移行（ES256、JWKSに2鍵）し、Email providerをオフ（Googleのみ、電話・匿名オフ）。オーナー許可のもと、PCの /opt/tuat-tf-supabase/.env の JWT_JWKS にクラウドの公開鍵2つを追加（既存2鍵は保持、計4鍵、秘密鍵の成分なしを検証）し、4ファイル指定のcomposeで rest/realtime/storage/functions のみ再作成（14:46、数十秒）。変更前の控え `.env.before-cloud-jwks-20260926144653`。全コンテナhealthy、本番Auth 200、オーナーの現行（PC発行）セッションでCloudflare中継・Vercel経由とも読込成功。これでクラウド発行のログインもPCのDBが受け付ける。未実施: 大学ドメイン以外のGoogle新規登録をクラウドで拒否するフック、PC→クラウドのデータ写し、アプリのログイン切替（全員1回再ログイン）、停止時の切替・復帰時の書き戻し。 → (this commit)
- 2026-09-26 / Claude Opus 5.5 / Supabase予備構成・段階2（クラウドDBの構造合わせ）。オーナーの明示許可のもと、旧クラウド（snbgxocgdhqtuywrlqrs）の pc_ops.control.frozen を false にし、`supabase db push --linked` で未適用の35件（20260909010000〜20260926010000）を適用。適用後、public の列450件・関数/トリガー/ポリシー303件がPCと完全一致。クラウドの cron（sheet-sync-hourly / schedule-import-hourly）は停止中のまま（スプシを上書きしないため有効化しない）。クラウドのデータは9/9時点のままで未同期。次はオーナーがダッシュボードでJWTを公開鍵方式へ移行（Migrate JWT secret → Rotate keys）→ PCのPostgREST JWKSにクラウドの公開鍵を追加。 → (this commit)
- 2026-09-26 / Claude Opus 5.5 / オーナー訂正: 「日別の記録」は練習記録ではなく大会・記録会の結果を月別でまとめる意図だった。/mypage/daily-records を撤去し /mypage/monthly-results（管理メニュー「月別の大会結果」）へ。月の前後移動、大学の結果で記録日があるものを大会（日付＋大会名）ごとに新しい順、学年・名前・種目・記録・風・PB/UB/公認、全体/中長距離/短距離。あわせてOB戦の「新規登録」でエントリー済みの部員も選べるようにし、選ぶとその人のエントリー編集に切り替える。460テスト・tsc・対象eslint・build成功。実機確認は未実施。 → (this commit)
- 2026-09-26 / Claude Opus 5.5 / オーナー依頼2件。①大会のアーカイブを⋯メニューの「アーカイブする／解除」1操作に（押した時点でアーカイブ、日時入力欄は撤去。既存のarchive_atは編集保存で変えない）。アーカイブ一覧は終了日（無ければ初日）の新しい順。②システムロール向け「日別の記録」（/mypage/daily-records、管理メニュー）: 日付の前後移動・日付指定、全体/中長距離/短距離、学年→名前順に距離・メニュー/結果・感想、タップで記録を開く、未入力の部員（マネージャーのみの人を除く）。460テスト・tsc・対象eslint・build成功。実機確認は未実施。 → (this commit)
- 2026-09-26 / Claude Opus 5.5 / 9/22監査のスプシ同期P1×3とP2を修正（上の「現在の状態」参照）。migration 20260926010000 を、実在の連携済み部員の記録で削除・日付変更・本文のみ編集・未連携部員の削除を試すBEGIN…ROLLBACKで確認（予定2件のみ作成、一般部員は読めない）してから本番PCへ適用。460テスト・tsc・対象eslint・build成功。index.ts の古い「アプリ主入力の既存日は上書きしない」説明を更新。 → (this commit)
- 2026-09-26 / Claude Opus 5.5 / オーナーが管理者PowerShellで `install-backend-task.ps1 -WithoutLogon` を実行し、本番タスクを S4U（ログオン不要）へ切替。LogonType=S4U・Running・中継healthy・maintenance=false・本番Auth 200・Node 22を確認。**無人再起動（ログインせずに再起動→Auth疎通）の実試験は未実施**（オーナーの都合のよい時間に行う）。9/22監査の項目6はこれで設定済み・実試験待ち。 → (this commit)
- 2026-09-26 / Claude Opus 5.5 / オーナー指示でDBを外付けHDDから内蔵SSDへ移設。01:10の自動バックアップ完了を待って停止（タスク無効化→中継停止→WSL停止。コンテナの個別停止はコマンドの受け渡し不具合で1台しか効かず、DBはWSL停止と同時に停止。起動時に自動回復）。vhdxをE:上へ複製（328秒・同サイズ）→`wsl --manage --move`でD:へ（127秒）→タスク再開。中継が `ERR_MODULE_NOT_FOUND` で起動せず、main の node_modules が空だったことが判明、npm ci で復旧。停止は約19分（01:10〜01:29）。全11コンテナhealthy、audit通過、記録4,202件で変化なし、本番画面とCloudflare中継の読み込みを確認。 → (this commit)
- 2026-09-26 / Claude Opus 5.5 / オーナー指示でスプシ書き戻しを実行。practice_records 4,202件をCSV退避し（当初CSVの改行込み行数4,763を件数と誤記）、本番のdryRunで取込684・書戻し1。実行すると取込0・書戻し0で、685件すべて (user_id, recorded_date) の一意制約違反＝既存の日。原因は run.ts が既存記録を1回のSELECTで読み、PostgRESTのmax-rows=1000で打ち切られていたこと。fetchAllPages（テスト3件）でページ分けに修正。457テスト・tsc・対象eslint・build成功。デプロイ後のdryRunは取込0・更新100・書戻し25（上限）・失敗0。本番で3回実行し、シート修正の反映 計108件、書き戻し 計39件、書き戻し待ち44→5件。記録件数は実行前後とも4,202件。残る5件は「B3 山端」タブが見つからない（タブ名の不一致、オーナー確認待ち）。GASの「POST required」「接続できません」は一時的で再実行で解消。 → b0d3b45
- 2026-09-26 / Claude Opus 5.5 / 点検で、スプシ同期の実行記録が9/9以降すべて running のまま残っていることを発見。原因は alert_sheet_sync_issue() が `SELECT DISTINCT ... NULL` を text と解釈して notifications.actor_id(uuid) への INSERT が失敗し、BEFORE UPDATE トリガーごと status 更新を弾いていたこと（ルートは更新の失敗を確認していないため無言）。NULLに型を付けた migration 20260926000000 を、実際の更新を含むBEGIN…ROLLBACKで確認（更新成功・通知1件）してから本番PCへ適用。データの同期自体は毎晩動いていた（最終 synced_at 9/26 00:00）。 → (this commit)
- 2026-09-25 / Claude Opus 5.5 / **障害の判明と対策**: 本番の中継先設定が9/25 15:30ごろ（Codex）から入っていて、fa2ae70（15:50デプロイ）以降の本番はブラウザのREST/RPCをCloudflare Workerへ送っていた。Workerの秘密鍵は23:05まで未登録で、ログイン済み要求はすべて503（Cloudflare集計で約1,400要求）。画面の初期表示はサーバー経由のため表示は出ていたが、出欠・記録・つぶやき・いいね等のブラウザからの保存と再取得が失敗していた。私（Claude）も今日のデプロイ前に環境変数を確認していなかった。対策として中継の読み込み失敗時にVercel経由で取り直し、失敗後10分は全通信をVercel経由にする処理を追加（失敗した保存自体は二重保存を避けて再送しない）。**教訓: 公開環境変数を先に入れない。中継を有効にする前に秘密鍵登録と実認証の確認を終える。デプロイ前にVercelの本番環境変数を確認する。** 454テスト・tsc・対象eslint・中継設定入りbuild成功。 → (this commit)
- 2026-09-25 / Claude Opus 5.5 / REST中継の本番Workerへ秘密鍵を登録し、オーナー本人のセッションで読込・件数・同じ値での保存を確認（4要求で約1.1秒）。**事故**: 「他人の行は書き換えられない」ことを確かめるつもりで他の部員への書き込みを送ったところ、オーナーは管理者なのでRLSが許可し、70人の`profiles.timeline_default_block`を「全体」で上書きした（実際に値が変わったのは1人）。直前23:07 JSTの暗号化バックアップを一時スキーマへ復元して突き合わせ、その1人を元の値へ戻した。他の列の差分0件・profilesに更新日時列なし・副作用のあるトリガーなしを確認、一時コピーは削除。**本番で「拒否されるはず」の書き込みを試さない**（ROLLBACK内か読み取りで確認する）。 → (this commit)
- 2026-09-25 / Claude Opus 5.5 / オーナー判断で、公式結果をマイページ・部員ページへ出すのを取りやめ。表示部品と取得関数を削除し、RLSをシステム限定へ戻した（一般部員での読取0件をBEGIN…ROLLBACKで確認後に適用、データは保持）。大会ページの案内文から個人記録への誘導を削除。 → (this commit)
- 2026-09-25 / Claude Opus 5.5 / 目標画面の改善（オーナー指示）。他大会へ切り替えるタブと、そのための大会一覧の取得を撤去。自分の目標を先頭にまとめ、みんなの目標は種目見出し＋カード行（名前・PB・目標）に変更。種目プルダウンと「自分のみ」を撤去してブロック切替と検索だけにし、スケルトンも同じ並びへ。449テスト・tsc・対象eslint・build成功。実機確認は未実施。 → (this commit)
- 2026-09-25 / Claude Opus 5.5 / 大会の結果表示の重複解消と一般公開（オーナー確定）。プログラムの日付を初日から順に変更（6fd1397）。大会ページはプログラムがあれば「プログラム・結果」だけにし、部員登録の記録一覧とシステム限定の公式結果一覧を撤去（プログラムが無い大会は部員登録の記録を「結果」として表示）。本名確認済み公式結果（competition_result_links）はマイページ「大会・記録会の結果」と部員ページの「公式記録」へ移し、RLSを全部員の読み取りへ変更（非管理者での読取72件をBEGIN…ROLLBACKで確認後に適用、anonは権限なしのまま）。マイページの「大会」を管理メニューから一般メニューへ移動。隔離worktreeでテスト・tsc・対象eslint・build成功。実機確認は未実施。 → (this commit)
- 2026-09-25 / Claude Opus 5.5 / オーナー指示の整理。①外部メール監視を取りやめ: 監視API/GAS/配置スクリプト/migrationを `D:\TUAT_TF_removed-20260925\` へ退避し、本番DBの`operational_health()`をBEGIN…ROLLBACKの予行演習後に削除（migration 20260925120000）。②関東新人の番組取込を取りやめ（未適用migrationとコードを退避、大会取得ルートから分岐を除去）。③27大戦のアーカイブを本番反映: competitionsを事前にCSV退避し、予行演習で対象が27大戦1件だけと確認してから列追加を適用。④OB戦を27大戦と同じ入口（大会一覧→大会ページ→プログラム、システム限定）と見た目（大会名・日付の上部カード、時刻列付きの行）へ統一。旧/ob-entriesは転送、マイページの専用行は撤去、大会一覧のアーカイブ切替はSegmentedControlへ。隔離worktreeで449テスト・tsc・対象eslint・build成功。be5d873、Vercel Production READY。⑤AGENTS.md冒頭に「9/25現在の状態」を追加し、監査項目4・大会準備・Vercel削減の文書に更新注記を付けた（Codexの未コミット記録もあわせてコミット）。Vercel負荷削減はCloudflareのオーナーログイン待ちで停止。実機確認は未実施。 → (this commit)
- 2026-09-25 / Claude Opus 5.5 / 外部レビュー（クラウド呼び出し・UI統一）への対応。①出欠一覧/タイムラインの初期表示・他ブロックの予定/メニュー表示の4設定で`router.refresh()`を撤去（使う画面は毎回サーバー描画のため次に開けば反映。画面全体の取り直しとちらつきが消える）。一般部員プレビュー・保存元表示・記録フォーム設定はレイアウト/戻る操作に影響するため据え置き。②記録保存はDB保存後すぐフォームを閉じ、スプシ書込(GAS)は裏で実行。保存時点でDBトリガーが`pending_sheet_push`を立て、同期の取り込みはその日を除外するため、古いシート値で上書きされないことをコードで確認。③つぶやきのメンション保存失敗をトーストで通知。④16pxの角丸`rounded-2xl`/`rounded-[16px]`(58か所)を未使用だった`@theme`の`rounded-card`へ統一（見た目は同一、ビルドCSSで確認）。**見送り**: 大会プログラム取得cronの間引きは同じルートに「本番反映禁止」の未コミット変更とCodexのPC移設作業(ops/pc-jobs)が重なるため未着手。画像URLの一括発行は署名URL(5分)がフィードのキャッシュより先に切れるため設計見直しが必要。記録/つぶやきの楽観更新と文字サイズの段階化は別作業。隔離worktreeで445テスト・tsc・対象eslint・build成功。実機確認は未実施。 → 1dadc08
- 2026-09-25 / Claude Sonnet 5 / 着手: 前回コミット(4205cf5)で残った27大戦結果の非一致23件を、OB戦で確認済みの本名→部員ID履歴(ob_meet_entries、meet_key問わず全件)をhistoryとして`matchEntryMember`（src/lib/entry-identity.ts、既存関数そのまま）に再適用し反映する。/ 完了: 23件中19件が履歴一致(previous)で新規確定、4件（800m・1500m・5000m・4×400mRの各1名）は履歴にも名簿にも該当なし(none)で反映せず。曖昧一致(ambiguous)は0件で自動確定はしていない。scratchpadで結果・既存links・profiles・履歴をJSON抽出しentry-identity.tsの実関数で突合、生成したINSERT文を本番PC DBへBEGIN…ROLLBACKでdry-run(53→72件を確認)後に適用。`competition_result_links`は72件（既存53件は無変更、追加19件のみ）。新規のmigration・コードは不要（既存テーブル/UIをそのまま利用）、個人名を含む中間ファイル・突合スクリプトはコミットせずscratchpadのみ。AGENTS.md以外の差分なし。実機（iOS PWA）確認は未実施。 → (this commit)
- 2026-09-25 / Claude Sonnet 5 / 27大戦の結果を、本名一致が機械的に確認できた部員だけシステムロール限定で反映。既存の速報同期（competition_program_entries、sairiku.net由来、全件洗い替え）に既に格納済みの結果（氏名・学年・順位・記録）を対象に、OB戦と同じ照合ロジック（src/lib/entry-identity.ts、完全一致＋学年一致のみ「確実」扱い、曖昧な候補は自動確定しない）で名簿と突き合わせ。対象76件中「確実」一致53件のみ`competition_result_links`（新規・can_manage_system限定read、他は権限なし）へ投入、非一致23件（現在の名簿に該当なし。OB/OGや退部者等の可能性、曖昧一致は0件）は反映せず。大会詳細ページ（/competitions/[id]）へシステム限定の「公式結果（本名確認済み）」節を追加。個人名を含む中間ファイル・突合スクリプトはコミットしていない（scratchpadのみ）。migrationは本番PC DBへ適用前にBEGIN…ROLLBACKでdry-run確認済み。関東新人・アーカイブ機能・27大戦の公式番組全面取込など、docs/COMPETITION-PREP-2026-09-24.mdにある「本番反映禁止」の広い変更（作業ツリーに未コミットのまま残置されていた）は今回のコミットに含めていない（別のissueとして残る。含めると存在しないarchive_at列参照等でproduction buildが壊れる状態だったため、隔離git worktreeで自分の変更だけを検証してからコミットした）。対象tsc・対象eslint・vitest（459件）・本番設定build成功。実機（iOS PWA）確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 実測監査項目3: 所有者承認後、非公開スナップショットとVaultのrollback検証を経て新VAPID鍵・Vault2設定・送信関数ソース・外向き通信を本番反映。4Compose指定でfunctionsだけを再作成し他コンテナ不変、認証なし401・購読者なし200を確認。Vercel Production公開鍵を更新。Vault/Push鍵/cronをDBダンプと結び付けて暗号化するバックアップと復元検証を実装。tsc・対象eslint・全323テスト・運用テスト5件・build成功。秘密情報を含む復旧材料のR2送信は自動承認審査で停止し所有者の追加承認待ち、常駐バックアップへの適用も中継再起動の承認待ち。実機確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 実測監査項目9: 表示用rolesを3時間のサーバー共有データキャッシュへ変更し、profile_rolesはIDだけ毎回取得。管理APIは既定で最新ロール取得、ロール編集/削除/並替後は認証付きAPIでキャッシュを即時失効。IDのみ必要な3画面とブラウザ9箇所をgetCurrentUserIdへ変更し、JWT検証・RLS・APIの直接認証は維持。実APIで全68人のロール/権限一致、全員分のロール取得は12815→2860 bytes（共通定義の初回6734 bytesは別）を確認。tsc・対象eslint・全323テスト・build成功。1時間単位のトラフィック/CPU再実測と実機確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 実測監査項目8: タイムライン・部員記録一覧から繰り返し定義を外し、同じ定義を1組にまとめるSECURITY INVOKER RPCで一括取得して共有配列へ復元。版ありは保存時定義、版なしは著者定義を維持。非公開退避・rollback・二重適用・匿名実行拒否・全4019記録の定義一致を確認してRPC本番適用。実APIの同じ30件は111694→67459 bytes（定義RPC込み39.6%削減、25組の異なる定義を保持）、その他データも一致。tsc・対象eslint・全315テスト・build成功。実機確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 実測監査項目2: 書込ポリシー71本をTO authenticatedへ変更。本番publicの非公開スナップショットと二重適用rollback検証後に適用。139ポリシーを個別比較し、対象71本の条件式・コマンド・permissive属性・authenticatedへの適用と1337テーブルgrant、他68ポリシーの不変を確認。書込95本中TO publicは0本。tsc・関連eslint・全310テスト・build成功。実機確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 実測監査項目6: 投稿削除時にlikes・comments・notificationsを回収するトリガーを追加。対象5表の非公開スナップショット取得後、rollback内で両投稿種別の本人削除成功・他人削除拒否・他の部員の参照とコメントへのいいねの回収・無関係行保持・再適用を検証。本番の孤立通知27件・いいね47件・コメント4件を回収し、有効参照は保持。tsc・対象eslint・全310テスト・build成功。実機確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 実測監査項目1: 5権限関数に本人確認を追加。本番schema・roles・profile_rolesを非公開退避し、BEGIN…ROLLBACKで2回適用の冪等性・匿名5関数false・全68人の権限一致（管理者1人は5権限true）・ロール18件と割当27件の不変を確認後、本番DBへ適用。tsc・関連eslint・全310テスト・build成功。実機確認は未実施。 → (this commit)
- 2026-09-21 / Claude Opus 5 / **本番を実測して監査し、要修正リストを `docs/AUDIT-2026-09-21.md` に起こした**（AGENTS.md冒頭に一覧を追加）。オーナーの「非効率・非合理な点は」「根本的な不具合は」という依頼に対し、推測ではなくenvoyのアクセスログ（86時間・95,819リクエスト）、`pg_stat_statements`、`vercel usage`、RLSポリシーとDB統計から洗い出した。**一番重いのはセキュリティ**: 権限関数が「全員に自動適用されるロール（`roles.is_everyone`）」で本人確認を短絡していて、**`auth.uid()` が NULL でも true を返す**。`notices` の書き込みポリシーはその関数だけが条件で対象が `public`。匿名ロールでのinsertは権限では弾かれず必須列のNOT NULLでだけ止まった＝判定を通過している（検証はすべてROLLBACK、データ無傷）。同じ短絡が `can_create_menu`/`can_create_schedule`/`can_manage_members`/`can_manage_system` にもあり、**管理画面で「全員」ロールにチェックを1つ足すと匿名へ開く**。書き込みポリシーは95本中71本が `TO public` で、2026-06-24の修正がSELECTだけだった取り残し。**次に重いのは停止中の機能**: Web Pushが**9/17の移設以降ずっと不通**（`vault.decrypted_secrets` が空、VAPID鍵が送信関数にもファイルにも無い、7.5時間で34回スキップ、9人11台が購読中）。9/10に「DBを移しても黙って止まらないように」とURLをVaultへ移したのに、**Vault自体が移行対象に入っていなかった**。バックアップにVault・鍵・cronが含まれていないので、復旧してもPushは戻らない。**同じ形の事故はこれで5回目**（7/02同期全滅28時間・7/09取込0件3日・9/09新規ログイン不可1週間・9/10Push未配信・今回）で、対策のタスク14が7/4から未着手。**効率面**: 5秒ごとの `postgrest --ready` が毎回スキーマ全体（227,058行）を読み、8.5時間で5,851回・94.8秒＝**アプリ最重クエリの6.4倍**でDB最大の負荷（アイドル13.6% CPUの正体）。タイムライン30件の実測109.1KBのうち**78KB（72%）が記録フォームの項目定義の繰り返し**で、外すと30.7KB（練習記録は全転送量の47%）。`/auth/v1/user` 439回/時、`roles`/`profile_roles` 各55回/時。削除済み投稿を指す通知27件・いいね47件・コメント4件（多相参照の後始末が手動削除の経路に無い）。同日重複記録は0件で1日1件は守られている。**無料プラン化の条件も確定**: Hobbyはcron100本まで可だが**1日1回まで**（本数制限は無い・公式確認）、残る毎時は予定取込1本でpg_cronへ移す。使用量はPC中継をやめてもActive CPUが無料枠4時間に対し6.4〜7.6時間で足りず、項目8・9の削減とセット。Supabase Freeは容量34MB/500MBで余裕、転送量が月6〜7GB/5GBで2〜4割超過。**コード変更は vercel.json の大会プログラム取得を毎時→日次にしたもののみ**（別コミット0d5793b）。それ以外は調査と文書化。 -> (this commit)
- 2026-09-21 / Claude Opus 5 / **本番停止（約6時間25分）の復旧と再発防止**。オーナー報告「サーバー止まってる」。**9/20 23:20にPCが再起動し、翌朝05:40に所有者がログインするまでDB/Authが停止**していた（最後の心拍23:14、boot 23:20、ログイン05:40、復旧05:45）。アプリはVercel側なのでログイン画面は開くが、その先が全部失敗する状態。データ損失なし（停止中は書き込み自体が起きない。復旧後: 部員68・記録4,019・つぶやき243・認証68）。復旧は自動で進行し、11コンテナhealthy・心拍20秒ごと更新・監査（匿名401/偽造JWT/管理API403/接続先の鮮度/バックアップ）通過・`/api/pc-supabase/auth/v1/settings` が本番URL経由で200を確認。**原因は2つ**。①起動タスクが `LogonType=Interactive` で所有者のデスクトップセッションの中でしか動かない（再起動後は誰かがログインするまで自動では戻らない）。②**ログイン後の最初の2回の起動も失敗していた**——外付けHDDからの初回起動は遅く、DBの準備を待つ `supabase-storage` が一度 unhealthy に倒れる。`compose up --wait` は**180秒の待ち時間を使い切る前に、unhealthyを見た瞬間に**諦めるため `runtime.ps1 start` が例外を投げ、`backend-task.ps1` は中継を起動せず終了していた（`task-failure.json` に "PC runtime did not start"）。1分ごとの再試行に救われて3回目で起動。**②は修正済み**: `runtime-wsl.sh` で `--wait` が早々に諦めたら実際に回復するかを最大420秒見届ける（単独で使える `wait [秒]` も追加。実機の11コンテナで解析結果と戻り値0を確認）。**①は管理者権限が要るのでオーナーの操作待ち**: `check-logon-free-start.ps1`（本番タスクに触らず、読み取り専用の別タスクでセッション0からWSL/Dockerへ届くか確認して消す）→ 通れば `install-backend-task.ps1 -WithoutLogon`（S4U＝パスワードを預けない「ログオンしていなくても実行する」。スイッチ無しで元に戻る）。届かなければ自動ログイン方式（Windows 11 HomeなのでHyper-Vの常駐VMは使えない）。手順は `ops/laptop/PC-PRODUCTION-HANDOFF.md` に追記。**あわせて罠を1つ塞いだ**: installer が `task-runtime.json` に `(Get-Command node).Source` を書いていたが、昇格したシェルはバージョン管理のshimを見落として古いNode 18を拾うことがあり、そのまま書くと中継が起動しなくなる。いま動いている中継のパスを引き継ぎ、Node 20未満は拒否するようにした。全310テスト成功（アプリのコードは変更なし）。 -> (this commit)
- 2026-09-19 / Claude Opus 5 / **オーナー依頼5件**。①**「保存して閉じる」をやめた**: 書きかけの投稿を閉じようとしたときの確認が、どの画面でも「保存して閉じる」で、下書きとして取っておくのかそのままタイムラインに出るのか読み取れなかった。`UnsavedChangesDialog` に intent を足し、新規投稿=「投稿しますか？/投稿する/投稿せずに閉じる」、編集=「更新しますか？/更新する」、設定類=「保存しますか？/保存する」に分けた（動詞は WORDING-GUIDELINES §6-1）。②**フォルダの中身の数**: 記事しか数えておらず、サブフォルダやスレッドだけのフォルダが「0件の記事」＝空に見えた。`NOTE_SELECT` にサブフォルダとスレッドを足し（PostgRESTの埋め込みなのでDBへの往復は増えない）、0件の種類は並べず「フォルダ2件 · 記事3件 · スレッド1件」、本当に空のときだけ「まだ中身はありません」。**注意**: `notes!parent_id` は実サーバーではサブフォルダ（配列）を返すのに生成型は親（1件）と推測するので、`normalizeNoteRow` でどちらの形でも配列へ揃えている。③**スプシ返信の取得を直近1週間へ**: 返信列の突き合わせだけ `sheetReplyCutoff`（7日）に分け、記録の取り込み（1か月・初回は全履歴）は据え置き。範囲外の返信は消さない。④**投稿を引用**（引用リツイート相当）: どの投稿カードの「…」にも「投稿を引用」が出て、つぶやきから他のつぶやき・練習記録を引用できる。引用元は likes/comments と同じ多相参照（`quoted_type`/`quoted_id`）で**外部キーは張らない**——`ON DELETE SET NULL` にすると何を引用していたかごと消えて本文だけが宙に浮くため、参照を残して「この投稿は削除されました」と出す。引用元の取得はいいね件数などと同じ `Promise.all` に入れたので往復は増えない。引用だけして回せるよう本文なしを許し（`tweets_content_check` を緩和）、自分自身の引用はCHECKで拒否、ストーリーは消えるので引用元にできない、引用先はあとから変えられない。**引用されたことの通知は入れていない**（notificationsの種類追加と一覧UIが必要なため別件）。migration 20260919100000 は**本番PCへ適用済み**——適用前に tweets 234件をスナップショット（WSLの `/opt/tuat-tf-supabase/transfer/`）し、BEGIN…ROLLBACK のdry-runで既存行が新CHECKを通ることを確認。適用後は234件のまま・引用0件・列/CHECK/索引あり・RESTが新列を返すことを確認し、RLS下の挿入もロールバック付きで検証（不正な種類・IDなし・本文も引用もなし・自己引用がそれぞれ弾かれる）。⑤**サーバーPCの消費電力**: `ops/laptop/power-saving.ps1`（status/apply/restore）と `POWER-SAVING.md` を追加。ディスプレイ120分→5分、PCIe省電力を最大、スリープ/休止を「しない」に固定（今までは常駐タスクが抑止していただけで、落ちた隙に寝る状態だった＝電力は増えず止まる経路が減る）。**CPUはオーナー判断で触っていない**（ターボだけ止める `-MaxCpuPercent 99` は用意済み）。実測: GPUアイドル13W、DBは外付けHDD上なので止まらない、11コンテナ中8つが5秒ごとに死活確認していてCPUが休みきれない（延ばせば減るがコンテナ作り直しでAPIが一時停止するため未実施）。全310テスト・tsc・対象eslint・PC設定build成功。**実機（iOS PWA）確認は未実施**（特に引用の見た目と、引用で本文なし投稿ができること）。 -> (this commit)
- 2026-09-19 / Claude Opus 5 / **返信すると並び順が入れ替わるバグを修正**。オーナー報告。記録へのコメントは「アプリの返信」と「スプレッドシートの返信列」が混ざるため、`CommentSection.tsx`は列番号(`sheet_reply_index`)で並べていたが、**列番号を持つ返信を無条件に先頭へ寄せる**比較になっていた（`if (aIndex != null) return -1`）。列はアプリから返信した直後にはまだ無く、`/api/sheets/reply`の書き込みが終わってから付く。つまり投稿直後は末尾に出て、数秒後に列が届いた瞬間へ前へ跳ねる。**本番データで再現を確認**: 記録8395895dのコメント3件が、投稿時刻では 08:23→13:15→14:02 なのに画面では 08:23→14:02→13:15（13:15は作者未連携等で列が無い）。旧ロジックを取り出して流し直し、列が付く前後で並びが変わることを実際に確認した。修正は並び替えを`sheet-replies.ts`の`sortRepliesInWrittenOrder`へ切り出し、**列の有無で先後を決めるのをやめ、列を持たない返信はその投稿時刻より前に書かれたアプリ返信の列へ寄せる**方式へ変更。これで列が付く前後の並びが一致し、跳ねなくなる。シートだけにある返信は従来どおり列順（左から古い順）で挟まる。列を持たないコメントは本番に50件/268件、うち両方が混在して実際に症状が出る記録が8件あった。**DBは変更していない**（表示の並べ方だけで直るため、一括更新は不要と判断）。5テスト追加（列が後から付いても動かないこと・古い未連携返信を追い越さないこと・シートのみの列順・全列より前の返信・列なし同士の投稿順）で全298テスト・tsc・対象eslint・build成功。**実機確認は未実施**（実際に返信して跳ねないことのiOS PWAでの確認はオーナーにお願いしたい）。 -> (this commit)
- 2026-09-17 / Claude Opus 5 / **DB/Authサーバーを旧PCから新PC（GALLERIA）へ移設し、本番を復旧**。新PCは初期状態からの構築で、WSL2有効化・Ubuntu 24.04導入・Docker Engine 29.8.1/Compose 5.5.1・cloudflared 2026.8.3を入れた。**DBの永続データはSSDではなく外付けHDD(E:、I-O DATA HDPL-UT 3TB)へ置く**という所有者指示に対し、内蔵は2台ともNVMe SSDでHDDは外付けのみと確認したうえで、`wsl --import`でUbuntuの仮想ディスク(`E:\WSL\Ubuntu\ext4.vhdx`)ごとHDDに載せる方式を採用した（`/opt/tuat-tf-supabase`が動かないのでスクリプト改修が不要、かつPostgresのデータがDrvFs経由のNTFSではなくネイティブext4のままになる）。`df -hT`でデータ先が`/dev/sdd ext4`であること、vhdxがE:に実在することをファイルシステムで確認済み。公式rootfsはcloud-images.ubuntu.comからSHA256照合して取得。データは**R2の暗号化バックアップから復旧**（旧PCのinstanceId配下・最新703件目、`handoff-restore.mjs`でsha256照合と復号）。移行後 auth.users 66 / profiles 66 / practice_records 3,936 を確認。鍵は旧PCの`/opt/tuat-tf-supabase/.env`を持ち込む選択A（部員の再ログイン不要）。**復元で踏んだ落とし穴2件**: ①`pg_restore`が`pc_ops.guard_writes()`を参照するトリガーで止まるので`freeze-install.sql`を先に適用する必要がある ②`--no-owner`復元でstorage/auth/realtimeの全テーブルが`supabase_admin`所有になり、各サービスが自分のmigrationを流せず起動ループに入る（`permission denied for table migrations`／`schema_migrations already exists`）ため、スキーマごとに所有権を対応するadminロールへ戻した。**本番が503から復旧しなかった真因はPCのシステム時計が約9分遅れていたこと**——`run-backend.mjs`が署名する`endpoint.json`の`expiresAt`が、Vercel側の正しい時計では既に期限切れに見え`verifyPcEndpoint`が弾いていた。NTP再同期で解消。**コード修正3件**: `server-profile.mjs`/`server-profile.ps1`の`wslpath`呼び出しで、wsl.exeが引数のバックスラッシュを食う（`C:\a\b`→`C:ab`）ため全解決が失敗していたのをスラッシュ変換で修正（**別PCへの引き継ぎでは必ず踏む**）。`save-preview-env.mjs`のVercel CLIパスを新PCのnpxキャッシュへ更新。全293テスト・tsc・対象eslint成功。**`npm run build`は当初のチェックアウト位置では実行不可だった**——パスに日本語（`OneDrive\画像\ドキュメント`）が含まれるとTurbopackがマルチバイト境界でpanicする（`start byte index 23 is not a char boundary`）。コード起因ではなく置き場所起因（Vercel側のビルドは正常）。**そのためチェックアウトを`D:\TUAT_TF`（内蔵SSD・ASCIIのみ・OneDrive外）へ移し、移動先でbuild成功を確認した。**移動は本番を止めないよう先にrobocopyで複製（53,666ファイル・1.95GB・失敗0）してからDocker停止→状態ファイル再同期→タスク再登録→起動の順で切り替え、実停止は約1分。スケジュールタスクの`task-runtime.json`は`(Get-Command node).Source`が古いNode 18を拾うため、Node 22をPATH先頭に置いて登録した。**片付け済み**: 旧チェックアウト（OneDrive配下）から`.contingency`と`.env.local`を削除しクラウド同期を解消、場所を示す目印を残した。引き継ぎHDDの`secrets/`（env.local/local-app.env/backend-config.json/stack.env/compose.auth.json）も、新PC・WSL内・旧PCに同じ内容があることを1件ずつ照合してから消去（持ち運べるドライブに平文の鍵を残さないため）。**未実施・要対応**: ①**所有者による実機ログイン確認**（唯一の未確認項目。移行後に練習記録が3,936→3,960件へ増えており部員の書き込みは実際に通っている） ②旧PCのタスク削除と秘密ファイル消去（新PCが数日安定してから。それまで旧PCは原本の控えとして残す） ③OS再起動後の自動復帰は未検証（タスクは所有者のログインセッションで動く仕様）。**運用上の注意**: DBの実体が外付けHDDにあるため、**稼働中にHDDを抜くとWSLごと停止する**（作業中に実際に発生し、`wsl --shutdown`で復旧・データ損失なし）。 -> (this commit)
- 2026-09-16 / Claude Sonnet 5 / **サーバー切替期間（9/16 15:00〜9/17 19:00 JST）のメンテナンス画面を追加**。オーナー依頼で、エラー画面ではなく計画メンテナンスである旨を出すため。`src/lib/maintenance.ts`（時刻だけで判定する純関数、DB非依存）＋静的な`/maintenance`ページを新設し、`src/proxy.ts`でこの期間中は`updateSession`（DB/Authを読みに行く処理）自体を一切呼ばずに全ページを`/maintenance`へrewriteする（URLは変えない）。バックエンド切替中で不安定なこの期間、DBに依存する経路を通らないようにしたのが要点。期間終了後は時刻比較だけで自動的に通常表示へ戻る（追加のデプロイ不要）。5テスト・tsc・対象eslint・build成功（`/maintenance`は静的prerender）。master 12ff3b3、Vercel Production dpl_ByK8jHj5bqAtYmEjefa2882nGbEx がREADYで本番URL配信、実際に本番URLでメンテナンス画面表示を確認済み（現在時刻が既に9/16 15:00 JSTを過ぎていたため、デプロイと同時に本番へ即反映されている）。 -> 12ff3b3
- 2026-09-16 / Claude Sonnet 5 / **27大戦プログラム（農工大の出場種目・出場選手）をホームに追加**。オーナー提示の速報サイト(sairiku.net、Shift_JIS配信のタイムテーブルHTML)を解析し、種目ごとの出場選手一覧（29大学分の全選手データ）から農工大の選手だけを抽出する`src/lib/competition-program.ts`を新設（個人種目・組分けなし種目・リレー(チーム行の姓名+学年が空白無しで連結される表記)・エントリー未確定の4パターンに対応、固定フィクスチャで22テスト）。`competition_program_entries`テーブル(全件洗い替え・閲覧のみ全部員に開放)と`competitions.program_source_url`(設定した大会だけが対象)を追加。取込は`/api/competition-program/sync`（記録・予定同期と同じSHEET_SYNC_SECRET二系統認可、Vercel Cronから毎時）。ホーム最上部にカードを追加し、普段は出場予定人数だけを表示、大会当日いま行われている種目に農工大の出場者がいる間だけ「男子OP1500m 競技中 B2山田」のようにトラック/フィールド別に展開する（現在時刻とプログラムの開始時刻を比較する方式）。大会詳細ページと管理画面（大会編集フォームに取得元URL欄・プログラムページに管理者限定の手動取得ボタン）にも導線を追加。全288テスト・tsc・対象eslint・build成功。**本番反映**: migration適用（新規テーブルのみ・既存データ変更なし）・27大戦の`program_source_url`設定・実データ90行(選手エントリー78件)を投入し確認済み。master ee1753e、Vercel Production dpl_DLtmv9k2XMj6eGiohTcgRVCEAixo が READY で本番URLが同SHAを配信。**未実施**: 実際のcron自動取込（次回:20分に発火予定、または管理者が「今すぐ取得」で即時確認可）・iOS実機確認・大会当日の「競技中」表示の実地確認（今日時点では大会前のため出場予定人数表示のみ確認）。 -> ee1753e
- 2026-09-15 / Claude Opus 5 / **「ログインを押してGoogleアカウントを選んでも、またログイン画面に戻される」の原因を特定して解消**。アプリのバグではなくPC側Authの設定だった。GoTrueのログに `422: Signups not allowed for this instance`（path:/callback）が今日だけで10回（09:57〜10:04 JSTに7回、21:14・21:55 JSTに3回＝同じ人が何度も試して諦めた形）。**このPCのauth.usersにまだ居ないGoogleアカウントだけが弾かれていた**——GoTrueが新規ユーザー作成へ進もうとして拒否し、302で戻り、`/auth/callback`がセッションを作れず`/login?error=auth`へ落ちる。原因はPC切替時に入った`GOTRUE_DISABLE_SIGNUP=true`（`ops/laptop/configure-backend-auth.mjs`）で、元の構成は`DISABLE_SIGNUP=false`。つまり**PC運用中は新入部員・まだ一度もログインしていない部員が誰一人ログインできない状態**が続いていた。Google認証自体は正常で、既存の部員は今日も問題なくログイン・更新できている（データ移行も無事: auth.users 65 / google identities 65 / profiles 65 が完全一致）。所有者の承認を得て`compose.auth.json`を`false`へ戻し、**4ファイル指定のcompose**（2026-09-10の12分障害の教訓どおり）で`auth`だけ再作成。再起動後 `GET /settings` が `disable_signup:false`・`google:true`・`email:false`・`anonymous_users:false` を返すことと、既存部員の`/user`が200で流れ続けていることを確認（体感できる停止なし）。再実行で元へ戻らないよう`configure-backend-auth.mjs`側も`false`＋理由コメントへ変更。**あわせて症状を分かりにくくしていたバグも修正**: `login/page.tsx`はURLの`?error=`を一切表示しておらず、callbackが`auth`/`domain`/`profile`等を付けて戻しても画面には何も出ず「押したのに戻された」としか見えなかった。エラー文を出すようにし、`domain`のときは大学アカウントで入るよう案内する。266テスト・tsc・対象eslint・build成功。**未実施**: 新規Googleアカウントでの実ログインは手元に該当アカウントが無いため未確認（弾かれていた方に再試行を依頼したい）。iOS実機確認も未実施。**注意**: PC運用中に新しく作られたauth.usersは、9/16のクラウド復帰時に持ち戻す対象になる。 -> (this commit)
- 2026-09-13 / Claude Opus 5 / タブ切替の遅さ・UIの不統一・文書の陳腐化に対処（前のコミットの続き）。**①切替の遅さ**: 原因は「DBへの往復を直列に積んでいたこと」。PC中継では1往復が数百ミリ秒で、それが素直に足し算になっていた。(a)`getCurrentUserId()`を追加——cookieのセッションを読むだけで**DBを使わない**。自分のIDだけで引ける問い合わせがプロフィール取得を待つ必要はないので、ヘッダーのベル・タイムライン・マイページ・ホームのお知らせ/タイムライン節を**直列2回→1回**にした。(b)`getFeed`は「本体→投票/メンション→いいね/コメント件数」の**直列3回**だった。後ろ2つは1回目にだけ依存して互いには依存しないので同時化（`attachTweetSocialData`は渡した順序とIDをそのまま返すのでIDは先に確定できる）。`getUserTweets`と通知から1件開く経路も同様。(c)下タブが`prefetch={false}`で、**タップしてまずスケルトンを取りに行っていた**。Next公式docs（`node_modules/next/dist/docs/.../linking-and-navigating.md`）に「動的ルートは`loading.tsx`があれば部分的に先読みされる＝共通レイアウトとスケルトンだけで、ページの取得処理は走らない」とあり、5タブ全てに`loading.tsx`がある。先読みを戻し、間に合わない回線のために`useLinkStatus`で押した瞬間に選択状態にした。**②UI統一**: ヘッダー/SubHeaderは既に統一済みだったが、押下フィードバックが`active:opacity-50/60/70/80/[0.78]`の**5種類**に割れていた（同じ種類のボタンでも画面ごとに違う）。共通`Button`が使っている0.78に合わせ、`globals.css`の`.pressable`1つへ57箇所を統一。濃さを変えるときはCSSの1か所だけ。`active:scale-*`は2026-07-23に全廃済みで復活させない。**③文書**: 済んだ指示書を未着手のまま載せていた7本を削除（UI-AUDIT/UX-ISSUES-2026-06/SCHEDULE-MENU-PLAN/NOTES-PLAN/SHEETS-IMPORT-PLAN/QA-CHECKLIST/ui-data-guidelines）。残した文書はコードと突き合わせて訂正した——**AGENTS.mdの索引が「編集削除=スワイプ＋長押し」と書いていた**（実際は⋯メニューで、UI-UNIFICATION本文は正しかった＝索引だけが誤り）。UI-UNIFICATIONは「staleTimesを維持」（撤去済み・禁止）「GradeMenuに統一済み」（存在するのはGradeFilter）「簡易表示」（8月に廃止）を訂正。**READMEは存在しない`supabase/schema.sql`を貼れと案内し、旧単一ロール方式の`UPDATE profiles SET role`を書き、完成済みのMenuFormと予定作成UIを「未実装」と載せていた**ので全て訂正し、実装状況欄は追跡をやめてAGENTS.mdへ誘導。**検証**: 260テスト・tsc・`src`と`ops`全体のeslint・PC設定build成功。本番PCは11コンテナhealthy・心拍とバックアップ最新・audit（匿名401/偽造JWT/管理API403）通過。**未実施**: iOS実機確認、本番での実測タイム、master反映とVercel Production確認。**保留**: PC試験版の分岐（`NEXT_PUBLIC_PC_TRIAL`ほか）は認証cookie名とproxyに触るため、本番稼働中・サーバー移設前の今は消していない（手順は`docs/ARCHITECTURE-REFACTOR-PLAN.md` Phase 4）。 -> (this commit)
- 2026-09-13 / Claude Opus 5 / 巨大ファイルの分割（Phase 1）・別PCへのサーバー引き継ぎ準備・不要ファイルの処分。**①分割**: `queries.ts`(1183行)と`sheet-sync.ts`(1312行)をドメイン別モジュール＋入口(`index.ts`)へ。呼び出し側は`@/lib/queries`・`@/lib/sheet-sync`のままで**1箇所も書き換えていない**。移動は行単位でロジック変更なし、依存はtypes/dates/field-map/gas-client → pull/push/reconcile/replies → run の一方通行（循環なしを確認）。lintが`pull.ts`→`run.ts`の循環importを1件検出したので除去。計画と残りの順番は`docs/ARCHITECTURE-REFACTOR-PLAN.md`。**classにはしていない**（RSC/Client Componentは関数が前提で、取得関数は状態を持たないため。classが効くのは`runSheetSync`だけ＝Phase 2として保留）。**②引き継ぎ**: PC固有値を`ops/laptop/server-profile.mjs`/`.ps1`へ集約し`.contingency/server.json`で上書き可に（既定は現行PCと同じ）。`initialize-wsl.sh`の`/mnt/c/Paimon Dayo/TUAT_TF`直書き（**別PCでは動かない**）と`backend-task.ps1`・`configure-backend-auth.mjs`の同様の直書きを`wslpath -a`解決へ変更。新規`handoff-preflight.mjs`（新PCの準備確認）と`handoff-restore.mjs`（R2の**別インスタンス**の最新バックアップを取得・sha256照合・復号）。手順は`ops/laptop/SERVER-HANDOFF.md`。**実機検証**: 本番PCで`runtime.ps1 status`11コンテナhealthy、`preflight.ps1`、`handoff-preflight.mjs` ready:true、実バックアップ1件をR2へ作成・検証、R2から取得→復号→`pg_restore --list`でTOC 1071件（auth/public/storage）を確認し復号dumpは削除。**③処分**: 使われていない`member-sort.ts`(ito削除で孤立)、参照0の`public/splash/assets/`9点(1.7MB)、`gas/sync-clasp/Code.js`とバイト同一の`gas/sync-api.gs`、解決済みインシデントの`repro-freeze*.mjs`、終了した本人限定試験のops14点、期限切れdocs4点を削除。`backup-backend.mjs`が15分ごとのローカル控えを**一度も削除していなかった**ため保持数24＋R2未送分は消さない方式を実装し、R2に存在することを確認した332件(695MB)を削除。`.contingency`867MB→166MB、`.next-pc-trial`34MBも削除。平文の`restore-rehearsal.dump`も除去。**⚠️未実施の必要作業**: ローカル控えの自動削除は**バックエンドのプロセスを再起動するまで効かない**（稼働中のrun-backend.mjsが旧コードを保持）。所有者が任意のタイミングで`restart-backend.ps1`を実行する。**⚠️保留**: PC試験版の分岐(`NEXT_PUBLIC_PC_TRIAL`ほか)は認証cookie名とproxyに触るため本番稼働中・移設前の今は削除していない（手順はPhase 4に記載）。260テスト・tsc・対象eslint・PC設定build成功。**master反映・Vercel Production確認・iOS実機確認は未実施**。 -> (this commit)
- 2026-09-10 / Claude Opus 5 / スケルトンの穴を埋めた。①**ヘッダーが画面全体を止めていた**（起動高速化の続き）: `Header`が async で、自分のプロフィール→未読数を直列に取りに行っていたため、**どの画面も見出しすら出せず、その間ずっとルートのloading.tsxのまま**だった。ホームを節ごとに分けても、この1か所が全部を待たせていて効いていなかった。Headerを同期にし、通信が要るベルだけを`HeaderBell`として切り出してSuspenseで後から流し込む（待っている間もベルの場所は空ける）。②**ホームの`fallback={null}`を実物に**: ノートの節に見出し＋カード2枚の場所取りを追加。走行距離のカードは「中長距離の人にだけ出す」ので、出すか決まる前に枠を置くと出さない人の画面で枠が消えてしまう。プロフィールが届いてから内側のSuspenseで2列の場所取りを出す形にした。③**作成ボタン(FAB)の場所取り**: レイアウトの`fallback={null}`を、本体と同じ位置・大きさの丸へ。④**loading.tsxが無かった14ルートに専用スケルトンを追加**: goals / goals/past / events / competitions / competitions/[id] / competitions/[id]/goals / mypage/pb / blog / blog/[id] / members/[id] / notes/[id] / notes/[id]/articles/[articleId] / notes/threads/[id] / admin/services。今までは`(app)/loading.tsx`の汎用スケルトン（2列カード＋投稿カード3枚）が出ていて、どの画面でも形が合っていなかった。page-skeletons.tsxに共通部品（SubHeader相当の見出し`SubTitle`・区切り線付きの行`Rows`）を足し、各ページの実際の並びに合わせて14個を定義。**実際の見た目を確認済み**: /login配下（認証不要）に一時プレビューを置き、ローカル(3008)で390px幅の実描画を14種すべてスクリーンショットで確認してから削除した。全260テスト・tsc・対象eslint・PC設定build成功。**注意（2回連続で発生）: 私が編集中でまだコミット前だったところへ、別エージェントのコミット`2c90b76`と`e01f8e1`が巻き込んで先にmasterへ入れている。`e01f8e1`（Web Pushの話）には上記①〜④に加えて、私の一時プレビュー`src/app/login/skeleton-preview/page.tsx`まで入って本番へ出てしまった**（認証不要のURLで、スケルトンが並ぶだけの実害の無いページ）。このコミットで削除。**iOS実機確認は未実施**。 -> (this commit)
- 2026-09-10 / Claude Opus 5 / 起動してから画面が出るまでを短くした。原因は「1画面ぶんの表示が、DBへの往復を何回も順番待ちしてから始まる」こと。PC中継では1往復が実測で約0.5秒あり、それが直列に積み上がっていた。①**ホームを節ごとに流し込む**: 今までホーム全体が1つのSuspenseだったため、いちばん遅い節（予定＝Googleスプレッドシートのメニュー取得を含む）が終わるまで画面に何も出なかった。見出しはプロフィールを待たずに出し、お知らせ・大会・予定・ノート・タイムラインを各自のSuspenseへ分けて、届いた順に見えるようにした。各節は`getCurrentProfile()`（React cacheで重複排除）を自分で待つ。②**プロフィールとロールを同時取得**: `getStoredProfile`が profiles→profile_roles と直列だったのを`Promise.all`へ。全ページの表示がDB往復1回ぶん速くなる。③**予定の節の直列を解消**: 出欠の取得はスプレッドシートのメニューに依存しないのに、メニュー取得の完了を待ってから出欠を取りに行っていた。両方を同時に投げ、メニュー取得の対象月も「実際に表示する予定の月」だけに絞った。④**メニューCSVを60秒だけ覚える**: ホームと予定は開くたびGoogleへ数本取りに行っていた（既存のタブ一覧キャッシュと同じ寿命に合わせた）。⑤**起動直後の先読みをやめた**: 起動画面が表示200ms後から10画面ぶんの`router.prefetch`を投げており、その1本ずつがサーバー上で画面を丸ごと組み立て直して同じPCのDBを取り合い、いま出そうとしているホーム自身を遅くしていた。下のタブ4つだけに絞り、`load`後800ms・500ms間隔へ後ろ倒し。起動画面を切っている人にも効くよう表示判定から切り離した。⑥**Service Workerで自前の部品をキャッシュ**（バックログ タスク7の残り）: `/_next/static/`はURLに中身のハッシュが入る＝内容が変われば別URLなので、cache-firstで安全に使い回せる。今までキャッシュ処理が一切なく、起動のたびにJS/CSSを取り直していた。250件を超えたら丸ごと捨てて入れ直す。認証後のHTML・RSC・APIは従来どおり一切キャッシュしない。全260テスト・tsc・対象eslint・PC設定build成功。**注意: ①〜④の4ファイルは、私がまだコミット前だったところへ別エージェントのコミット2c90b76が巻き込んで先にmasterへ入っている**（コミットメッセージは大会記録入力の話しか書いていない）。この記録とsw.jsの変更が実際の内容。**iOS実機での体感確認は未実施**。 -> (this commit)
- 2026-09-10 / Claude Opus 5 / 結果入力を種目ごとの書き方に合わせた＋Web Push配信を最終開通。①**記録の入力**: 全種目一律の「時・分・秒・1/100」をやめ、**秒は小数1欄**（「1/100」という語を廃止）。書き方は**種目マスタの1設定**（分と秒 15分32.40 / 秒 61.85 / メートル 6.85 / 得点）で管理者が選ぶ。migration 20260910060000 で `competition_events.time_format` を追加し、400mまで（100/200/400/100mH/110mH/400mH/4×100mR/4×400mR）を `seconds` で初期投入、残り21種目は `minutes`。距離は m の小数1欄だけ（cm欄を廃止、内部はcmのまま）。表示も秒書きの種目は分へ繰り上げない。②**大学以前→高校以前**へ改称（DB値 `pre_university` は不変）。③**UBは大学のみ**: フォームで高校以前のときトグルを出さず保存もせず、DB側も `pb_records_ub_university_only` CHECK で拒否。既存の高校以前UBは0件だった（13件のUBはすべて大学）。④マイページの結果追加を一覧下のボタンから**FAB**へ、「すべて見る（n件）」を**下矢印だけ**へ。⑤**Web Push**: オーナーがVercel CLIへログインしたので `NEXT_PUBLIC_VAPID_PUBLIC_KEY` を新公開鍵へ差し替え、production を redeploy。`/api/push/vapid` が新鍵を返すことを確認し、**PC側の秘密鍵と一致＝配信経路が開通**。全260テスト・tsc・対象eslint・PC設定build成功。migration適用前に全DBスナップショット(pre-event-format-20260910)とdry-runを実施。master 2c90b76、Vercel Production が同SHAを配信。**iOS実機・実際の通知受信は未確認**。**未着手: DBが停止しても出欠だけは入力できるようにする件**。 -> (this commit)
- 2026-09-10 / Claude Opus 5 / **【自分が起こした障害と復旧】+ Web Push復旧 + 起動画面/大会カード**。①**障害**: Edge Functionに環境変数を渡すため `docker compose up -d functions` を **1ファイル指定だけ**で実行し（正しくは docker-compose.yml/compose.local.yml/compose.rehearsal.yml/compose.auth.json の4ファイル）、composeがネットワークを作り直そうとして失敗、その過程で envoy・studio・functions が停止。**約12分間、本番のAPIが落ちた**（Google認証 `/auth/v1/authorize` の503を実ログで確認）。4ファイル指定で再起動したが envoy が `tuat-contingency_default` へ再接続されず全リクエスト503のまま。`docker network connect --alias api-gw --alias envoy --alias kong` で復旧（最初 api-gw エイリアスを付け忘れて studio/functions の `http://api-gw:8000` が壊れ、付け直した）。**DBコンテナは一度も停止しておらずデータ影響なし**、15分バックアップも継続。教訓: **このスタックへ compose を打つときは必ず4ファイルを揃える**（`ops/laptop/runtime-wsl.sh` が正）。merged configでは api-gw は default+local_gateway に付くので通常起動は正しい。②**Web Push復旧**: 切替以降 `net._http_response` が0件＝配信を一度も試していなかった。原因は(a)トリガーが凍結中の旧クラウドURL直書き(b)PC側Vaultが空で `push_webhook_secret` 無し(c)VAPID秘密鍵がPCに無い。migration 20260910050000 で送信先URLもVaultから読むよう変更（DBを移しても黙って止まらないように）。VAPID鍵は旧クラウドの `supabase secrets set` 分が読み出せないためオーナー承認のうえ**新規生成**（`ops/laptop/generate-push-keys.mjs`、秘密は `.contingency/backend/push.env` にACL付きで保存し表示しない）。既定ネットワークは `internal: true` で外へ出られないため、**送信関数のコンテナだけ**を auth が使っている `auth_outbound` にも参加させた（DB等は隔離のまま）。compose にも永続化。購読0件の架空ユーザーで実測: 合言葉なし/誤り=401、正しい=200「No subscriptions found」、DB→pg_net→関数の実経路も200。**誰にも通知は送っていない**。**残: Vercel Productionの `NEXT_PUBLIC_VAPID_PUBLIC_KEY` を新しい公開鍵へ差し替え＋再デプロイ（オーナー作業。CLI未ログインのため私からは変更不可）。差し替えるまで配信は届かない。** 端末側は公開鍵が変われば PushSubscriptionSync が自動で購読し直すので部員の操作は不要。③起動画面を1日1回→**毎回起動時**へ（オフは設定でできるため回数制限しない）。④ホームの大会2カードを、下の練習カードと区別できる濃色＋上端に起動画面と同じ色の細線のデザインへ。「みんなの目標」を **「{大会名}の目標」** へ改称。全260テスト・tsc・対象eslint・PC設定build成功、ローカル(3008)で起動画面の再表示をDOMで確認。master 78037ac、Vercel Production READY。**iOS実機・実配信は未確認**（公開鍵の差し替え後に要確認）。**未着手: DBが停止しても出欠だけは入力できるようにする件**（オーナー依頼4件目）。 -> (this commit)
- 2026-09-10 / Claude Opus 5 / ノートの写真を本文中へ差し込めるようにし、ホームの大会カードを元に戻し、起動画面をカウントダウンへ差し替え。①本文は今までどおりただのテキストのまま `[[photo:<キー>]]` を持たせる。キーは画像の保存先パス `<記事ID>/<キー>.webp` の真ん中で、写真を選んだ瞬間に決まるので保存前に差し込んでも保存後に同じ写真を指す。目印の無い既存記事は今までどおり本文の後ろに写真が並ぶのでデータ移行は不要。編集画面は目印を出さず、本文を文章/写真のブロックに分けて素のtextareaと写真を交互に並べる（iOSの入力挙動は不変）。写真を選ぶとその時のcaret位置へ入り、✕で「本文に置いていない写真」へ戻し、「本文に入れる」で置き直す＝これが移動手段。一覧の抜粋は目印を途中で切らない（truncateNoteBody）。note-body.ts に18テスト。②ホームの大会カードは2列（カウントダウン＋みんなの目標の件数）へ戻した。オーナー指摘「カウントダウンと目標を統合するな」の意図は目標の入力導線の話で、閲覧タイルは残す。目標の入力はマイページ→目標が入口。③起動画面を SplashIntro（TUATタイトル演出）から SplashCountdown へ差し替え。**1日の最初の起動**だけ「27大戦まで/N/日」を桁ごとに回るリールで見せ、下線を引いて退場。当日は「いよいよ今日」。大会日は端末へ覚えさせて開いた瞬間から出せるようにし（通信を待つとアプリが見えた後に覆いが被さる）、取り直しは1日1回、初回起動は覚えるだけで何も出さない。設定のスキップ・reduced-motion・タップで閉じるは維持。旧SplashIntroとCSSは削除（履歴に残る）。ローカル（ops/laptop/run-local-app.mjs、127.0.0.1:3008＝PC側Supabase）で375px幅の実描画を確認、11日表示・当日表示ともスクリーンショットで確認済み。全262テスト・tsc・対象eslint・PC設定build成功。master 3f2b96f、Vercel Production dpl_ppmDFD9zLSSJ1G4Jhqb2KB3wgrwW が READY で本番URLが同SHAを配信。migrationなし。**iOS実機確認は未実施**（特にノートの写真差し込みと起動画面の回転演出）。 -> (this commit)
- 2026-09-10 / Claude Opus 5 / コメント1件ごとのいいねを追加。likes は (target_type,target_id) の多相テーブルなのでCHECKと set_like_state を 'comment' 許可へ広げるだけで、既存7,231件（tweet1800/record5431）は不変。コメント欄を開いたときの件数と自分の状態は get_comment_like_state で1回にまとめ（PC中継下では1往復=1関数実行のため）、本文描画の後に実行して表示を待たせない。多相参照でFKを張れないぶん、コメント削除時にそのいいねを消すトリガーを追加。UIはタップでいいね・長押しで LikersSheet（投稿と同じ操作）。スプシ由来の返信は sheet_record_replies で同期のたび作り直されるため対象外。migration 20260910040000 は全DBスナップショット→本番dry-run（BEGIN…ROLLBACK）で検証後に適用し、既存件数・CHECK・関数・トリガー・履歴を確認。全233テスト・tsc・対象eslint・PC設定build成功。master e57228b、Vercel Production dpl_92zW77YmgAxaydQFTqhjyhgASdpH が READY で本番URLが同SHAを配信。**iOS実機確認は未実施**（特にコメントの長押しでいいねした人が出るか）。あわせて無料プラン(Hobby)復帰の可否を公式ドキュメントで確認: **毎時cron `5 * * * *`（予定取込）はHobbyではデプロイが失敗する**（1日1回まで・発火は±59分）。maxDuration 300はHobby上限とちょうど同じで可、`regions:["hnd1"]`は単一リージョンなので可、next/image未使用・Vercel Analytics未導入。無料枠は呼び出し100万/Edge Request100万/Active CPU 4時間/Provisioned Memory 360GB時間で、PC中継中はブラウザのDB呼び出しが全部関数化するためインスタンス時間が伸びる。**復帰後に予定取込をpg_cronへ戻してからHobbyへ落とすのが安全**という結論をユーザーへ報告済み（コード変更はまだ入れていない）。 -> (this commit)
- 2026-09-10 / Claude Opus 5 / 目標のページ化・予定/出欠の複数日対応・ノートの写真と投票・左上導線の統一・Vercel呼び出し削減を実装し本番反映。①マイページ「目標」を /goals へ（自由入力＋これからの大会＋過去の目標→/goals/past）。大会ごとの目標入力は既存の /competitions/[id]/goals を再利用。ホームのカードは目標タイルを外しカウントダウンのみにし、目標の入口をマイページ1本へ。「大会」は管理メニュー（manage_system）へ移動。②出欠に attend_date を追加し一意制約を (schedule_id,user_id,attend_date) へ。既存870件は開催日でバックフィルし内容は不変。開催期間外の日付はトリガーで拒否。複数日開催は予定カードが日ごとに1行（遅刻・欠席連絡も日別）、単日の見た目は不変。③「今後の予定」「本日の予定」を最終日まで残すよう変更（初日を過ぎても消えない）。④月間一括入力に「対象」を追加し、予定のブロック取り違えをその場で全体/中長距離/短距離へ付け替え可能に。移動先に同日の予定があれば重複を作らず拒否。⑤ノート記事の写真をサムネイル格子＋「写真を追加」ボタンへ変更し、本文→写真→投票の並びに統一。⑥ノート記事に投票を追加（note_poll_options/note_poll_votes、RLSはノートの can_view_note/can_edit_note に追従、集計は get_note_poll_options）。tweet_poll_* は未変更で、UIは PollView に共通化。票が入った選択肢は編集時に外せない。⑦全画面フォームの左上を「‹戻る」に統一し右からのスライドへ（動作は閉じるのまま）。ボトムシートは下から。⑧Vercel削減: /api/version を force-static 化（関数実行ゼロ・ビルド出力で ○ 確認）＋取得を5分間隔へ、お知らせベルのポーリングを30秒→120秒かつ表示中のみに変更（表示中1時間あたり1人180回→30回）。migration 20260910020000/20260910030000 は全DBスナップショット取得後、本番PC DBへのdry-run（BEGIN…ROLLBACK）で検証してから適用。dry-runが name[] と text[] の比較エラーを検出し修正済み。適用後に870件/NULL0件/不一致0件/制約・トリガー・6ポリシー・関数・履歴2件を確認。全233テスト・tsc・対象eslint・PC設定build成功。master 5698c1b、Vercel Production dpl_AJ3u339YRn6Kbh7AqAz6x4jckroK が READY で本番URLが同SHAを配信、11コンテナhealthy・バックアップ最新を確認。**iOS実機確認は未実施**（特に複数日の出欠、ノートの写真追加と投票、全画面フォームのスライド）。 -> (this commit)
- 2026-09-10 / Codex / 着手: Claudeの大会記録機能をPC本番へ統合。migration・正規化・クラウド復帰互換性を検証しmaster/Productionまで反映する。

- 2026-09-09 / Codex / 着手: オーナーの最終実装依頼により、Vercelの既存URLを保った1週間のPCバックエンド運用を実装する。全員のGoogle認証、再接続できるAPI中継、PCデータのバックアップと逆移行、書き込み停止と切替検証を担当。本番切替は認証・データ復帰を検証してから行う。
- 2026-09-09 / Claude Opus 5 / 種目と大会の並び順を、表示順の数値入力からドラッグ並べ替え（ReorderList・10刻みで振り直しupsert）へ変更。並べ替え中は⋯とトグルを隠す。どちらもマイページの管理メニュー（/events・/competitions）から操作する。全214テスト・tsc・対象eslint・build成功。実機未確認。 -> (this commit)
- 2026-09-09 / Claude Opus 5 / 大会・記録会の結果の入力を統一。種目と大会をマスタ化し、システム管理者が /events・/competitions から管理（種目には記録の測り方を持たせ、名称変更は結果・目標の表記へ追従）。結果は種目選択＋その他自由入力、記録は種目ごとの入力欄（時間1/100・m/cm・得点）と風速・DNS/DNF/DQ/NM、記録日は日/月/年の粒度、「大学以前」は日付不要で一覧もまとめ表示。大会を選ぶと初日が記録日に入り、大会別の結果ページに種目別で並ぶ。PBは既定オフ・種目ごと1件（トリガー）で、日付が新しい順にバッジを付ける旧判定とPB/UBの連動は撤去。システム管理者は部員ページから他人の結果を編集可（RLS更新）。目標はシートから /competitions/[id]/goals へ移し大会別タブ＋各行にPB併記。migration 20260909010000 は列追加とポリシー更新のみで既存データは不変。全214テスト・tsc・対象eslint・build（ダミーSupabase設定）成功。**本番DBへのmigration適用・master反映・既存データの正規化・実機確認は未実施**（このセッションに本番の接続情報が無いため）。既存データの修正手順は docs/COMPETITION-RESULTS-PLAN.md と scripts/dryrun-pb-normalize.ts。 -> (this commit)
- 2026-09-06 / Codex / プロフィール・ストーリー画像のR2対応と管理者専用サービス状態画面を実装。認証付き署名URL、404のみ旧Storageへフォールバック、8GB目安の容量ガード、移行dry-run・参照スナップショット・SHA-256コピー検証を追加。設定→サービスの状態で3社の公式障害情報、R2容量と切替フラグ、任意キー設定によるVercel UTC暦月使用量を確認可能。ページ/API双方でmanage_systemを検証、15分キャッシュ。Supabase月間Egress等の未取得項目は0とせず公式使用量へ案内。全163テスト・tsc・対象eslint成功、buildはダミーSupabase設定で成功（実接続設定なし）。R2利用開始済み・非公開Standardバケットtuat-tf-images作成済み、バケット限定Object Read & Writeキーの発行確認待ち。実画像コピー・R2本番環境設定・実機確認は未実施。R2フラグ未設定では既存Supabase経路を使用する。次は docs/R2-MIGRATION.md と docs/SERVICE-STATUS.md を参照して接続を継続する。
- 2026-08-30 / Codex / タイムラインとマイページの投稿表示順を、練習日ではなくアプリへ投稿した時刻（created_at）の新しい順へ統一。過去日の練習記録を今日入力しても新着として先頭に出し、タイムラインの日付見出しも投稿日基準、記録カード内の練習日は従来どおり保持する。マイページ取得時のlimit適用前の記録順もcreated_atへ変更し、遅れて入力した記録が候補から漏れないようにした。同時刻はIDで安定ソート。遅れて入力した昨日分が今日の投稿群へ入る境界テストを更新。全199テスト・tsc・対象eslint・build成功。実機確認は未実施。 -> (this commit)
- 2026-08-30 / Codex / タイムラインの一覧行を展開したときは、つぶやき本文と練習詳細の高さ制限・ぼかし・「続きを読む」を無効化して最初から全文表示。展開カード内のリンク・ボタン・入力欄・タブ等を操作したクリックは外側の行開閉から除外し、投票、選択肢追加、投票者タブ、コメント、編集メニューなどを使っても展開状態を維持する。通常のカード表示にある長文折り畳みは従来どおり。全199テスト・tsc・対象eslint・build成功。実機確認は未実施。 -> (this commit)
- 2026-08-29 / Claude Opus 5 / 未読の目印の文言と置き方を変更（オーナー指摘「ここまで読みましたではなく新着の投稿でいいのでは」）。ただしタイムラインは新しい順なので、線は新着の下に付く＝そこに「新着の投稿」と書くと線より下を指して見える。そこで先頭に「新着の投稿 N件」（件数が分かる）を出し、新着が終わる位置の区切りは「以前の投稿」（ここから先は前回までに見た分）とした。どちらも新着があるときだけ出る。UnreadDivider.tsx は NewPostMarkers.tsx へ改名し UnreadHeading / SeenDivider の2つに分けた。一覧表示（6bf3086 でオーナーが戻した2行リスト）とカード表示の両方に同じ目印が出る。全199テスト・tsc・対象eslint・build成功。実機確認は未実施 -> (this commit)
- 2026-08-29 / Claude Opus 5 / 「完了の定義」節を追加（オーナー確定）。作業ブランチへの push は Vercel の Preview が増えるだけで本番は変わらないのに、そこで完了として報告していたため。以後は ①検証 ②ブランチ push ③master へ fast-forward して push ④Vercel Production の最新 deployment が target=production・state=READY・githubCommitSha が自分の commit であることを確認（target=null はPreview）⑤報告に commit・master 反映・Production の状態・実機確認の有無を書く、までを完了とする。途中で止めるときは本番に出ていないことを明示する。ドキュメントのみでコード変更なし -> (this commit)
- 2026-08-29 / Claude Opus 5 / 投稿の「簡易表示」を廃止し、投稿自身が高さを決める形へ作り直した。従来は同じ名前で中身が3種類（タイムライン＝2行リスト・ホーム＝カードのまま本文を畳む・マイページ＝カードのまま＋トグル）あり、初期値も画面ごとにバラバラ、切替はラベルの無いアイコン1個、タップで開ける手掛かりも無く、切り替えると開いていた投稿が全部閉じていた。主要SNS（X・Instagram・Facebook・Strava・Slack等）はどこも密度モードを持たず「1投稿の高さに上限＋続きを読む」で一覧性を作っているため、その形へ寄せた。ExpandableSection を新設し、記録の詳細（150px）とつぶやき本文（140px）だけを畳んで下端をぼかし、収まる投稿にはボタンを出さない（短いつぶやきが2行で切られて読めない問題も解消）。あわせて Slack の未読ラインに相当する「ここまで読みました」をタイムラインに追加（use-last-seen-feed が端末内 localStorage に最終閲覧時刻を保持、開いた時点の値を固定するので見ている最中に線が動かない。既読判定は絞り込み後の実際に表示した投稿が対象）。日付の見出しは簡易表示専用だったが全投稿で常に出す。RecordCard/TweetCard の compact・embedded prop、CompactFeedRow、useFeedDisplay、useBooleanPreference、boolean-preference、timeline-compact cookie を削除（差し引き約400行減）。unreadBoundaryIndex を純関数化してテスト追加。全199テスト・tsc・対象eslint・build成功。実機確認は未実施 -> (this commit)
<!-- 形式: YYYY-MM-DD / エージェント / 触る範囲 → 結果(commit・要点) -->
- 2026-07-31 / Claude Opus 5 / 上記4件の実装後にレビューして3点修正。①notificationclick が WindowClient.navigate() 失敗時に既存ウィンドウへfocusして終わっており、iOSでは通知をタップしても直前の画面のままで目的の投稿へ行けなかった（openWindow を先に試し、素のfocusは最後の手段へ。同一オリジン判定の new URL 例外も握る）→ 48b96f1 ②Service Worker の pushsubscriptionchange が /api/push/vapid を叩くのに、そこが認証ゲートの内側で401になり通知の自動復旧だけが黙って失敗していた（VAPID公開鍵はクライアントバンドルにも入る公開情報なので proxy の公開パスへ）→ 73e2349 ③重複していた設定側の「タイムラインを簡略表示」をオーナー確認のうえ撤去（タイムライン画面のトグルは維持・同じcookie）→ 821bf44。あわせて旧アプリ（別リポジトリ D:\AI\Antigravity\TF, menu20260404）を修正・デプロイ: 旧オリジンのPWAはSWにpushハンドラが無く構造的に通知が届かないのに、index.html が新アプリへ自動リダイレクトするため誰も気づけなかった。案内ページ /moved.html へ変更し、SWは自己解除＋キャッシュ削除のみに差し替え、登録も撤去（管理者の #access= 経路は不変）。本番の2オリジンとも反映確認済み。全118テスト・tsc・対象eslint・build成功。実機確認は未実施 -> (this commit)
- 2026-07-31 / Claude Opus 5 / 設定をマイページ内の展開式（SettingsAccordion）から専用ページ /mypage/settings へ分離し、「表示 / 通知 / 練習記録 / システム管理」の4節に整理。よく触る順に並べ替え、出欠一覧とタイムラインの初期表示を隣接させた。行の見た目が5種類（枠あり/枠なし・p-3.5/py-3/py-2・font-medium/semibold）に割れていたのを Toggle の variant="row" と px-4 py-3 の1種類へ統一し、白→灰→白の3段カード入れ子を解消。SystemSyncStatus が自前で描いていた「システム」見出しの二重表示を撤去し、見出しの無かった記録フォーム設定に「練習記録」節を与え、スプレッドシート連携者・非連携者でラベルが割れていたのを「記録フォームを編集」へ統一。RecordSourceSetting の「スプシ由来」を文言ガイドライン準拠の「記録の保存元を表示」へ。設定系4ファイルに残っていた \uXXXX エスケープを実際の日本語へ戻した。設定項目は1つも削っていない（簡略表示トグルの重複と、プロフィール編集内の記録フォーム導線の重複はオーナー確認待ちで残置）。全118テスト・tsc・対象eslint・build成功。実機確認は未実施 -> (this commit)
- 2026-07-31 / Claude Opus 5 / 通知の購読が黙って死んだまま戻らない問題を修正。配信拒否(410/404)で push_subscriptions の行が消えても端末側の購読は残るため、設定は「オン」のままなのに通知が1件も届かない状態が続いていた。src/lib/push.ts と PushSubscriptionSync（(app)/layout.tsx に常駐）でアプリを開くたびに購読を静かに登録し直し、VAPID公開鍵が変わっていたら購読を作り直す。sw.js に pushsubscriptionchange を追加、設定に「通知が届くか試す」ボタン（/api/push/test → send_test_push RPC。Vault の秘密で既存 Edge Function を叩くのでVAPID秘密鍵はアプリ側へ持ち出さない）、manifest に id/scope を追加。migration 20260731120000（last_seen_at 追加・register_push_subscription 更新・send_test_push 新設）を本番適用済み、send-web-push も --no-verify-jwt で再デプロイ済み。端末に購読が残っている人は開くだけで復旧するが、購読自体が消えている人は通知をオフ→オンし直す必要がある。全118テスト・tsc・対象eslint成功。実機確認は未実施 -> 4f22d98
- 2026-07-31 / Claude Opus 5 / 通知タップの着地点を「タイムラインの先頭」から該当投稿そのものへ変更。/timeline/[kind]/[id] を新設し、返信を開いた状態のカード1枚を表示する。getFeedItemById はタイムライン表示用の間引き（未来日除外・空記録除外・SHEET_TIMELINE_CUTOFF）を一切かけないので古い投稿の通知からも必ず開ける。お知らせ画面の通知行と send-web-push のコメント通知 data.url を同じパーマリンクへ。Service Worker の notificationclick を「既存ウィンドウを focus→navigate、無ければ openWindow」に変更（PWAがもう1枚開くのを防止）。全105テスト・tsc・対象eslint・build成功。実機確認は未実施 -> 018f4a5
- 2026-07-31 / Claude Opus 5 / タイムラインの初期表示（全体/中長距離/短距離）を追加。migration 20260731100000 で profiles.timeline_default_block を追加・本番適用済み（追加のみ・既定all）。TimelineViewSetting を新設し設定の「表示」に出欠一覧の初期表示と並べて配置、TimelineView の初期ブロックをサーバーから渡すようにした。あわせて、プロフィール保存のたびに attendance_default_block が所属ブロックで上書きされ設定が黙って戻る既存バグを修正（ブロックを変更したときだけ初期値を入れ直す）。出欠一覧の初期表示の説明文に、ホーム・予定の人数にも効くことを明記。全105テスト・tsc・対象eslint成功。実機確認は未実施 -> (this commit)
- 2026-07-31 / Claude Opus 5 / ホーム・予定の出席/欠席人数を「出欠一覧の初期表示」で選んだブロックに連動させた。共通の attendanceCounts を AttendeesButton に追加し、出欠チップの人数は設定ブロック固定（シート内でタブを切り替えても戻る）、ホームの今後の予定カードは全員合計から設定ブロック集計へ変更（attendees と myProfile を受け取り、出欠タップ時もその場で数え直す）。出欠一覧の全体表示ではマネージャーを中長距離・短距離の両方の節に表示し（マネ バッジ付き）、実データが入らない「マネージャー」節は廃止。ATTENDANCE_BLOCK_ORDER を constants に追加。全105テスト・tsc・対象eslint成功。実機確認は未実施 -> (this commit)
- 2026-07-24 / Codex / プロフィールアイコンをURL入力からSupabase Storageへの画像アップロードへ変更。中央正方形512px WebP変換でEXIFを除去し、12MB入力・2MB保存上限、本人フォルダだけINSERT/SELECT/DELETE可能なRLS、公開読取バケット、保存失敗時の新規画像回収と成功後の旧画像削除を実装。既存の外部URLは変更するまで維持。バケット設定・匿名アップロード拒否・空オブジェクト確認、モバイルUI、tsc・eslint・64テスト・diff check・build成功 → (このcommit)
- 2026-07-23 / Codex / アプリ返信とスプシ返信を、スプレッドシートの列位置を共通キーにして左から古い順へ統一。新規アプリ返信はGASの書込列を即時保存し、既存分は公開CSVの本文＋投稿者名照合で次回同期時に列位置を復元。返信順カラム追加時にコメント編集時刻が変わらないようトリガーも調整。対象2テーブルをC:\tmpへ退避後、マイグレーション適用・同期dryRun成功（実書込0件）・tsc・eslint・61テスト・diff check・build成功 → (このcommit)
- 2026-07-23 / Codex / iOSでいいね長押し時に残る青い選択UIを追加修正。操作対象をtouch-action:noneにし、子要素までWebKit user-select/touch-calloutを無効化。長押し中はselectstart/contextmenu/dragstart/selectionchangeを抑止して既存選択範囲も消去し、通常タップ・指外れキャンセル・いいねした人表示は維持。生成CSSでtouch-action/calloutを確認、tsc・対象eslint・59テスト・diff check・build成功、実機確認待ち → (このcommit)
- 2026-07-23 / Codex / 中長距離向け走行サマリーを改善。直近7日の実日付範囲、前の7日との差、走行日数を追加し、総距離→比較文脈→強度構成の順に再設計。増減を良し悪しの色で評価せず、当期間0kmでも前期間との差を表示。強度バーに読み上げラベルを追加。tsc・対象eslint・59テスト・diff check・build成功、認証後の実機確認は未実施 → (このcommit)
- 2026-07-23 / Codex / 中長距離の月別練習メニューを旧アプリと同じ公開CSVから軽量取得。予定・出欠とは別APIで必要月だけ並列取得し、予定画面の表示時・画面復帰時にReact Queryで最新化。DBの中長距離共通メニューだけを置換し、個人メニュー・他ブロックは維持。旧アプリ風のメニュー/ペース/補足カード表示、CSV失敗時DBフォールバック、権限制御を追加。実シートの7月CSV 200/見出し確認、対象テスト3件・tsc・eslint・build成功 → (this commit)
- 2026-07-23 / Codex / 他部員ページでは空のノート欄を省略。記録カードのいいね状態を閲覧者付きで取得し、画面間の共有キャッシュを初期値で上書きしないよう修正。練習量グラフの各棒に距離値を表示し、中長距離プロフィールに直近7日間の総距離・強度別割合/距離サマリーを追加。旧アプリと関連研究を確認し、固定の理想比率は示さず事実ベースの構成表示を採用。tsc・対象eslint・59テスト・diff check・build成功、認証後の実機確認は未実施 → (このcommit)
- 2026-07-23 / Codex / 予定カードの押下時スケール演出を廃止し、背景色の短い変化へ変更。共通Button・出欠・FAB・お知らせカテゴリ・コンディション・プロフィール選択に残っていたactive:scaleも濃度変化へ統一し、共通transitionはtransformを外して0.12秒、prefers-reduced-motion時は無効化。tsc・対象eslint・active:scale全件検索・diff check・build成功。ローカル共通ボタンでcomputed transform:noneを確認、認証が必要な予定画面の実機確認は未実施 → (このcommit)
- 2026-07-23 / Codex / いいね長押し時の文字選択表示を修正。PostActionsの操作行でselectstartをネイティブに抑止し、WebKit向けuser-select:noneも明示。通常タップと長押し一覧の挙動は維持。tsc・対象eslint・diff check・build成功、実機確認は未実施 → (このcommit)
- 2026-07-13 / Claude Code (Fable 5) / **タスク17-b（スレッド）＋17-c（FABメニュー化）実装・本番反映**。migration 20260713150000（threads/thread_posts新設・RLS=閲覧authenticated全員/作成本人名義/更新本人/削除本人or管理者・返信でthreads.updated_atを更新するトリガー。オーナー承認済み）。UI: ノートタブに3タブ目「スレッド」（ThreadList、cookieのタブ保持もthreads対応）、スレッド詳細 `/notes/threads/[id]`（時系列投稿＋返信欄＋本人/管理者の投稿削除・ThreadPostsView）、FABは/notesで「フォルダを作成/スレッドを立てる」の2択メニュー（**当初案の3択のうち「ノート(記事)」は入れていない**: 記事作成はフォルダ内FABが既に担い、ルートからだと保存先フォルダ選択を挟んで複雑になるため。オーナーが3択を望むなら次のエージェントがフォルダピッカー付きで追加）。週次バックアップ対象にthreads/thread_postsを追加。**残: 返信時の参加者通知（既存notifications基盤に'thread_reply'типеを足す場合は通知一覧UIの対応も必要）**。tsc/build成功・**実機確認は未実施** → (このcommit)
- 2026-07-13 / Claude Code (Fable 5) / **タスク17-e（フォルダの移動）実装**。フォルダ設定編集（NoteEditor・既存フォルダ＋管理可のときのみ）に「場所」ピッカーを追加: ルート＋移動可能なフォルダ一覧（階層インデント表示）。制約をクライアントで担保＝同scopeのみ・自分自身と子孫は除外（循環禁止）・移動後もツリー全体が深さ3以内（サブツリー深さ＋移動先深さ≤3）。種類（共有/個人）を切り替えたら移動先はルートへリセット（別scopeの親に残さない）。DB変更なし（既存parent_id列のupdate）。これで**タスク17はb（スレッド）とc（FABの3択化=スレッド追加時）を残すのみ**。tsc/build成功・**実機確認は未実施**（フォルダ編集→場所変更→一覧/パンくず反映を要確認） → (このcommit)
- 2026-07-13 / Claude Code (Fable 5) / オーナー指摘のUX問題を批判的に洗い出して修正: **①サブフォルダ作成導線が2つあった問題**＝SubfolderSection.tsxのインライン「サブフォルダを追加」ボタンを廃止し**作成はFABに一本化**（規約「書く=FAB」準拠。SubfolderSection自体を削除しページ直描画に）。**②フォルダ行に⋯が無く編集入口が分からない問題**＝FolderRowActions.tsx新設で**フォルダ一覧・サブフォルダ一覧の各行に⋯メニュー（編集/削除）**を追加（記事行と同じ導線・タスク12の⋯統一とも整合。作成者/管理者のみ表示。行はLink全体のためpreventDefault+stopPropagationで遷移抑止）。members/[id]のNoteList呼び出しも新propsへ更新。**17-e（フォルダ移動）は引き続き未実装**（仕様は17節）。tsc/build成功・**実機確認は未実施** → (このcommit)
- 2026-07-13 / Claude Code (Fable 5) / タスク17続き（オーナー追加要望）: **①フォルダごと削除**＝migration 20260713120000で parent_id FK を ON DELETE CASCADE 化（親削除で配下のサブフォルダ・記事も一括削除。空チェック方式を撤回、削除確認文言も更新）。**②フォルダ内FABの2択化**＝フォルダ詳細のFABが「記事を作成/サブフォルダを作成」のスピードダイヤルに（サブフォルダを作れる立場＝フォルダ作成者or管理者かつ深さ3未満のときのみ。それ以外は従来どおり記事作成へ直行）。scopeは親を継承。**③フォルダの移動は17-e として未実装**（仕様は17節に追記済み）。tsc/build成功・**実機確認は未実施** → (このcommit)
- 2026-07-13 / Claude Code (Fable 5) / タスク17-a（フォルダ入れ子）実装・本番反映。migration 20260713100000（notes.parent_id＋index＋自己参照禁止CHECK。オーナー承認のうえ適用済み）。一覧（NotesView）はルートのみ表示、フォルダ詳細にパンくず（ノート/親/現在）＋サブフォルダ節＋「サブフォルダを追加」（作成者/管理者のみ・深さ3で打ち止め・scopeは親を継承。SubfolderSection.tsx新設）。戻るボタンは親フォルダへ。サブフォルダを持つフォルダの削除はFKで拒否され専用メッセージ表示（記事のみのフォルダは従来どおり削除可）。NoteEditor/NoteComposerにparentId追加（insert時のみ）。**残: 17-c FAB3択（フォルダ/ノート/スレッド）と17-b スレッド**。tsc/build成功・**実機確認は未実施**（サブフォルダ作成→表示→パンくず遷移を要確認） → (このcommit)
- 2026-07-13 / Claude Code (Fable 5) / オーナー確定の新機能をタスク17として起票（フォルダ入れ子・スレッド掲示板・FAB3択。仕様詳細は17節）＋**d) フォルダアイコン修正を実施**（NotesView・ホームのノート節: BookOpen→Folder。「フォルダと分かりにくい」指摘対応）。a〜cは未着手＝次のエージェントは17節の仕様で実装（順序: a→c→b推奨。bのRLS要件に注意）。tsc/build成功 → (このcommit)
- 2026-07-13 / Claude Code (Fable 5) / オーナー報告「ノートの編集が反映されない」を修正。原因: 3つのセッションキャッシュ（NotesCachedView / ScheduleCachedView / MyTrainingChartCached）が useQuery の initialData＋refetchOnMount:false 構成のため、**編集後のrouter.refresh()でサーバーが新データを返してもinitialDataは初回マウントでしか使われず、古いキャッシュが表示され続けていた**（ノートだけでなく予定・マイページのグラフも同じ構造バグ）。修正: 3コンポーネントとも initialData propが変わったら queryClient.setQueryData でキャッシュへ反映（サーバーレンダー＝正）。tsc/build成功・**実機確認は未実施**（ノート編集→一覧に反映、を要確認） → (このcommit)
- 2026-07-12 / Claude Code (Fable 5) / オーナー報告2点を修正。**①FreezeProbeのトースト廃止**（タイムライン→ノート遷移時に「フリーズ痕跡」トーストがたまに出る報告。実態はiOSがメモリ回収でPWAページを静かに再読込したケースも異常終了として拾うため。フリーズ本体は解決済みなので、以後はlocalStorage（tuat-freeze-reports）とconsole.warnの記録のみ＝調査が必要になったら参照）。**②ノートの共有/個人タブを保持**: 選択をcookie（tuat-notes-scope）に保存し notes/page.tsx がSSRで復元（タイムライン簡易表示cookieと同じ方式・ハイドレーションフラッシュ無し）。ノート詳細から戻っても個人タブのまま。tsc/build成功・**実機確認は未実施** → (このcommit)
- 2026-07-12 / Claude Code (Fable 5) / Codex引き継ぎのオーナー依頼キュー②③を実装（末尾の「Claude handoff — 2026-07-12」節参照）。**②ホーム今後の予定カードの出欠数即時反映**: `UpcomingScheduleCard.tsx`新設（クライアント側で出席/欠席数を差分更新。AttendanceToggleの失敗時onChanged再発火でロールバックも成立）→ dd848ff。**③スプラッシュ終了をシーン間と同じ白フラッシュ明転に**: flash-exit-in(3.98s→4.28s白ピーク)→ピークでステージ非表示・白の下からホーム→白450msフェードアウト。ヘッドレスWebKitのコマ撮りで確認済み → bf64ab4。**残: キュー①（記録・つぶやきの楽観更新＝router.refresh待ちをやめる大掛かりな変更）は未着手**。HomeFeed/TimelineViewの整合まで含む設計判断が要るため、次のエージェントはまず両コンポーネントの現行キャッシュ構造を読んでから着手すること。tsc/build成功・**実機確認は未実施（②の人数増減・③の明転はオーナー実機で要確認）** → (このcommit)
- 2026-07-12 / Claude Code (Fable 5) / ホーム遷移フリーズの根本原因切り分け: **`next.config.ts`の`cacheComponents: true`を無効化**。根拠は重大インシデント節に追記（有効化=07-12 00:14と症状開始の一致、Tab LabのRouter外安定、他容疑の全撤去でも再現、依存コード無し）。タブ切替速度は東京リージョン＋react-queryセッションキャッシュ＋loading.tsxで担保。tsc/build成功・WebKit E2E全パス（他タブ→ホーム遷移含む）。**実機判定待ち＝これで直ったらcacheComponentsは再導入禁止リスト入り、直らなければ「Router層でもない」ことが確定するので次はNext.jsのバージョン更新(16.2.9→最新patch)を試す** → (このcommit)
- 2026-07-12 / Codex / Tab Lab実機結果: A=61回 平均10ms/最悪23ms/停止6ms、B=59回 24/29/9ms、C=61回 24/75/26msで全モード安定。ただしCの遷移元は空画面で、実予定DOM破棄→実ホーム構築は未検証とのオーナー指摘。D=実予定⇔実ホームをRouter/Activityなしで破棄・再表示するモードと、切替後DOMノード数・最大DOM数の計測を追加。tsc/対象lint/build成功、実機判定待ち → (this commit)
- 2026-07-12 / Codex / iOSタブ基盤の再構築案を推測で本番採用せず検証するため、通常画面から隔離したClient Tab Labを実装。A=空画面、B=36枚の軽量DOM、C=現ホームをRouter/Activityなしで破棄・再表示。タップ→2描画時間、最大イベントループ停止、直前50操作をlocalStorageへ保存し結果コピー可能。システム管理者だけマイページ→その他に導線表示。通常5タブ・DB・同期処理は変更なし。tsc/対象lint成功（home既存unused警告2件のみ）、本番build成功。実機A→B→C判定待ち → (this commit)
- 2026-07-12 / Codex / iOSフリーズ調査の実機結果を反映。`c5786a9`(IDB停止)、`e2f2497`(予定Route Handler化・重複prefetch撤去・PullToRefresh停止・スケルトン修正)、`26ad026`(passive方式でPullToRefresh再導入)→`18c948f`(即撤回)を本番投入したが、最終実機結果は**別タブからホームへ毎回遷移不能・フリーズ**。PullToRefreshは明確な悪化要因だが撤去後も再現し、根本原因未特定。今回の対策群を成功扱いしない。AGENTS.mdに重大未解決インシデントとして記録 → (このcommit)
- 2026-07-12 / Claude Code (Fable 5) / FreezeProbeの初回実機データ入手: **フリーズは /schedule と /timeline に集中**（オーナー報告）。＋リロード時の「一瞬上にズレてガクッと戻る」報告。ローカル再現不可のためCodexへ引き継ぎ（**詳細は docs/CLAUDE-HANDOFF.md 冒頭の2026-07-12節が正**。最有力仮説: この2画面だけにあるreact-queryキャッシュ機構＝timelineのIndexedDB永続化／scheduleのServer Action queryFn）。コードはドキュメントのみ変更 → (このcommit)
- 2026-07-12 / Claude Code (Fable 5) / オーナー提案を実装: **スプシ由来記録のタイムライン並び順を「練習日の0時(JST)投稿扱い」へ変更**（取込時刻のままだと、まとめ取込のたびに数日分が先頭で団子になり順番が荒れるため）。`sheet-sync.ts` computeMemberPullのinsertに `created_at: 練習日T00:00:00+09:00` を追加＋migration 20260712150000で既存のfrom_sheet=true全80行をバックフィル（冪等UPDATE）。厳守ルールどおり適用前にスナップショット取得（scratchpadのsnapshot_from_sheet_created_at_20260712.json、80行のid/created_at）→オーナー承認→db push→RESTで全80行が0時JSTになったこと検証済み。タスク3の「created_at＝取込時刻」方針は本節で上書き（方針転換の節も更新済み）。tsc OK → (このcommit)
- 2026-07-12 / Claude Code (Fable 5) / オーナー報告続報3点（①予定タブ表示時に一瞬UIズレ ②ホームのリロード=スピナー回りっぱなし ③予定→他タブ→予定で完全フリーズ・要アプリ再起動）。**E2E再現テストを実施**（オーナー許可を得て使い捨てテストユーザー作成→検証→削除。scripts/repro-freeze*.mjs 新設、playwrightは`npm i --no-save`）: ローカル本番ビルド＋Chromium/WebKit＋タッチ合成のpull-to-refreshで**いずれも再現せず**＝実機iOS PWA固有。②③は「メインスレッド停止」で説明が付く（スピナーはCSSアニメでGPU駆動のため固まっても回り続ける。スクロール不能はPullToRefreshの非passiveリスナー経由）。対応3点: (1)**FreezeProbe新設**（(app)layout常駐。1秒心拍をlocalStorageへ、hidden/pagehideで正常離脱マーク。次回起動時に異常終了を検出しconsole+直近5件保存、can_manage_system保持者にはトーストで場所を通知→**実機の証拠が取れる**）(2)**experimental.staleTimesを撤去**（cacheComponentsと併用の"use with caution"実験。導入以降に症状が出始めた最有力容疑。タブ速度は東京リージョン＋各画面のreact-queryキャッシュで担保）(3)**PullToRefreshの非passiveなtouchmoveを常時登録→タッチ中のみ動的登録に変更**（全スクロールが毎フレームJSを通っていた＝iOSカクつきの一因除去）。tsc/build成功、新ビルドでWebKit E2E全パス（pull表示→900ms消灯・タブ5往復・フリーズ無し）。**次: オーナーの実機でフリーズ再発時、次回起動時のトースト（フリーズ痕跡+パス）を報告してもらう** → (このcommit)
- 2026-07-12 / Claude Code (Fable 5) / オーナー再々報告「リロードが終わらない・タブ切替が今ももっさり（YouTube等はサクサクなのに）」→ 計測で**物理的な根本原因を特定**: 本番の `X-Vercel-Id` が `hnd1::iad1::` ＝ **サーバー関数が米国東海岸(iad1)で実行**されていた（Vercelのデフォルト。リージョン未指定だった）。DBは東京Supabase（日本から実測45〜75ms）なので、**全DBクエリがバージニア⇔東京を往復（~150-180ms/回）**。ホームはプロフィール＋5セクションで多数往復するため1画面数秒、`router.refresh()`も数秒→「終わらない」体感、キャッシュ切れタブへの遷移も同様に「もっさり」。実測: `/apple-icon`(dynamic) TTFB 1.6s、401即返しのAPIですら345ms。修正: `vercel.json` 新規作成で `"regions": ["hnd1"]`（関数を東京へ固定）。これで1往復~2-10msになり全画面のサーバー時間が桁で縮む見込み。**デプロイ後の実測で確認済み（d365017）**: `X-Vercel-Id: hnd1::hnd1::`、`/apple-icon` TTFB 1.63s→0.16-0.35s、401即返しAPI 345ms→~150ms。DBクエリを多数打つログイン後ページはさらに大きく縮むはず。**実機での体感確認はオーナーに依頼中**。あわせて同時刻、823b6ceの同期修正が本番cronで効いたことも確認: 05:00 UTCのrunで pulled_count:10（3日分の滞留解消）・failed_members空 → d365017
- 2026-07-12 / Claude Code (Sonnet 5) / オーナー再報告「ホームでのリロードが終わらない」＝直前の自分のcommit(56ff64c)が原因の回帰と判明。`PullToRefresh.tsx`のインジケーターを`useTransition`のisPending連動に変えたが、これはまさに46e79a7（"Prevent refresh indicator...from hanging"）で**一度直したのと同じ不具合**（ホームは5つのSuspense境界に分かれたasync Server Componentなので、isPendingが解消されずスピナーが回りっぱなしで固まるケースがある）を再発させていた。useTransitionを撤去し46e79a7と同じ固定900msタイマー方式に戻す（router.refresh()の完了を示すコールバックはNext.jsに無いため、完了連動は諦め安全な固定時間に統一）。tsc/build成功。**実機確認は未実施** → cb0278a
- 2026-07-12 / Claude Code (Fable 5) / オーナー報告「リロードが終わらない・タブ切替がカクつく/遅い・演出中に5タブ読み込んでるはずでは」を調査・修正。**判明した事実**: BottomNavの`Link prefetch={true}`は動的ルートもデータ込みで完全プリフェッチする（スプラッシュ中に5タブ先読み自体は機能していた）が、`staleTimes.dynamic: 30`のため**30秒で失効**し、以降のタップは毎回サーバー往復待ちだった。修正: ①`next.config.ts`のdynamicを300秒へ延長（タイムラインIndexedDBの5分TTLと整合。鮮度は引っ張って更新と各画面のセッションキャッシュで担保）②`PullToRefresh.tsx`のインジケーターを固定900msの見せかけから`useTransition`の実完了連動＋保険6秒キャップ＋最低表示500msに変更（「回ってるのに内容が変わらない/いつ終わったか分からない」の解消。以前useTransition単独で固まった経緯があるためキャップ必須）。tsc/build成功。**実機確認は未実施** → (このcommit)
- 2026-07-12 / Claude Code (Fable 5) / 【インシデント対応】毎時の記録同期が**2026-07-09 12:00 JSTから3日間・73回連続で新規取込ゼロ**だったことをsheet_sync_runsで発見（オーナーの「様々なエラー」調査中）。原因: 部員のシートの「流し」列に小数`0.3`が入っており、`practice_records.strides`がINT型のため一括insertが丸ごと失敗（`invalid input syntax for type integer: "0.3"`）。runのstatusは"success"のままだったため誰も気づけず（タスク14の連続失敗アラート未実装も遠因）。修正(`sheet-sync.ts`): ①stridesは`Math.round`で整数化して取り込む②一括insert失敗時は1件ずつ入れ直し、不正な行だけ部員名・日付つきでfailed_membersに記録して残りを取り込む（型不一致の道連れ全滅を構造的に防止）。`scripts/dryrun-sync.ts`新設（`npx tsx --env-file=.env.local scripts/dryrun-sync.ts`）で本番dryRun実施: inserted:10（3日分の滞留が復活）・failed:0を確認してからpush。**本番反映後に手動同期で実際に10件入るかの確認が必要** → (このcommit)
- 2026-07-12 / Claude Code (Sonnet 5) / スプラッシュ第3弾: プリロード改善後も実機iPhoneでカクつき・ワイプが「読み込みラグ」に見える問題が解消せず、**CSSアニメを廃止して事前レンダリング動画方式に全面変更**（オーナー指示「なんとかして」）。あわせてオーナー指示でシーン2→3の横ワイプをフェードに変更（プロトタイプ側で修正後キャプチャ）。手順: プロトタイプをヘッドレスEdge(390x844@2x)で25fps・85フレームキャプチャ→ffmpegで`/splash/intro.mp4`(780x1688, H.264 crf24, 5.0s=演出3.4s+余韻1.6s clone、約1.8MB)にエンコード。`SplashIntro.tsx`を`<video muted playsInline preload=auto>`再生に書き換え（canplaythrough待ち・保険8秒/25秒、未ログイン時は`playbackRate=2/3`で5.1s相当、sessionStorage 1回制・reduced-motionスキップ・タブprefetch分散は維持）。不要になったWebP6枚+ArchivoBlack.woff2と`<link rel=preload>`群・Shadow DOM・SVGフィルタ複製を削除。tsc/build成功・dev再生確認。**iOS実機での再確認はオーナーに依頼中** → (このcommit)
- 2026-07-12 / Claude Code (Sonnet 5) / スプラッシュ実機フィードバック第2弾（画像が間に合わず背景なし・最終カードが右から流れ込む）。原因: 実機回線ではプリロード上限3秒が先に切れて画像が揃わないまま再生開始していた。修正: ①`<link rel="preload">`（画像6枚＋フォント）を初期HTMLに追加しeffect実行前からDL開始②画像を1280w→1024w/q68へ再圧縮（計約1MB→約523KB）③待機を「揃うまで待つ（保険8秒）」に変更。tsc/build成功・devで再生確認 → (このcommit)
- 2026-07-12 / Claude Code (Sonnet 5) / スプラッシュのiOS実機フィードバック対応（文字が細い・序盤ガクつき・最終カードで一瞬白・ステータスバーが白いまま）。原因と修正: ①iOSに'Arial Black'が無くフォールバックの細字になっていた＋画像未ロードのまま再生開始→**画像6枚とフォントをプリロード（上限3秒）してから再生開始**、フォントは`document.fonts.load()`で明示ロード（当初`FontFace.load()`のreject未処理で演出自体が死ぬバグを混入させ、ヘッドレス検証で発見して修正）②SafariはShadow DOM内の`filter:url(#id)`をdocument側で解決するため質感フィルタが無効→**フィルタ定義SVGをlight DOMにも複製**③タブprefetchを開始0.8秒後から150ms間隔に分散（開始直後のジャンク軽減）④`statusBarStyle`を`default`→`black-translucent`に変更（ヘッダー類は既にsafe-area-inset-top対応済み）。tsc/build成功、devサーバーで再生→フェードアウト→ログイン画面表示まで確認。**iOS実機での再確認はオーナーに依頼中** → (このcommit)
- 2026-07-12 / Claude Code (Sonnet 5) / 起動スプラッシュ演出の新規実装（オーナー依頼。MV風タイトルアニメ「TUAT / Track / Field」約3.4秒＋余韻）。`src/components/SplashIntro.tsx` 新規（アプリCSSと衝突しないよう **Shadow DOM に注入**。1セッション1回のみ＝sessionStorage、`prefers-reduced-motion` はスキップ、**未ログイン時（sb auth cookie無し）は5.1秒のゆっくり再生**＝オーナー指定）・`src/app/layout.tsx` の body 末尾にマウント・`public/splash/` に WebP画像6枚(計約1MB)とArchivo Blackフォント追加。再生中に `router.prefetch` で全タブ（home/schedule/timeline/members/notes/notices/ranking/venues/mypage）を先読み。既存機能のロジック変更なし。tsc/build成功、devサーバー＋ヘッドレスEdgeで縦画面の各フェーズ描画を確認済み（途中、absolute+left:50%でタイトルが2行に折返す問題を white-space:nowrap で修正）。**実機(iOS PWA)確認は未実施** → (このcommit)
- 2026-07-11 / Claude Code (Sonnet 5) / オーナー再報告（Codexが再改修した遅刻UIが「浮いてる」・遅刻の送信成否が分からない）を受け`AttendanceToggle.tsx`/`ScheduleCard.tsx`を再修正。**「浮いてる」原因**: `LateAttendanceControl`が設定画面用の`Toggle`（フルwidth・カード枠・説明文付きの大きい行）と`Input`（h-11フルwidth）をそのまま流用し、出欠行の下に別ブロックとして挿入されていたため、コンパクトなピル型UI（出欠ボタン・出席者数チップ）の中で見た目が浮いていた。専用の小型チップ（他のピルと同じh-8・rounded-full）に作り直し、出欠ボタン・出席者数チップと**同じ行に並べる**よう`ScheduleCard`側のレイアウトも変更（別行の`space-y-2`ブロックを廃止）。**送信成否が不明な問題**: 出欠タップ・遅刻トグルはSupabaseのerrorを握りつぶしており失敗しても何も表示されなかったため、両方に`error`チェック＋失敗時はローカル表示を元に戻しつつ`useToast`でエラー表示するよう追加。遅刻メモ（デバウンス保存）は特に無言だったため、入力欄の右に保存中(スピナー)/送信済み(チェックマーク・1.8秒で消える)/エラー(赤枠)を表示する`noteState`インジケータを追加。tsc/lint/build成功。**実機確認は未実施** → (このcommit)
- 2026-07-11 / Claude Code (Sonnet 5) / オーナー報告（Codex実装の遅刻機能: UIが良くない・やたらロードが入る）を受け`AttendanceToggle`/`ScheduleCard`を修正。**根本原因**: `AttendanceToggle`が`refreshOnChange`経由で出欠タップ・遅刻トグル・遅刻メモ入力欄のblurの**都度`router.refresh()`**（ページ全体のサーバーコンポーネント再取得）を呼んでおり、1操作で最大3回の全体リロードが発生していた。`onChanged`コールバックを`{status, isLate, lateNote}`を返す形に拡張し、`ScheduleCard`側でその部員の出欠だけをローカルstate（`attendeesState`）で即時反映する設計に変更（`myProfile`propを新規追加し出欠一覧の表示名等に使用）。`router.refresh()`呼び出しをこの機能から全廃。あわせてUI: 出席時に遅刻トグルが出ると出欠ボタン自体の幅が116px→flex-1に急拡大していた挙動（レイアウトが跳ねる）を廃止し、ボタン幅を常時104px固定・遅刻トグルは横に固定幅で追加される形に変更。呼び出し元(`ScheduleView.tsx`/`schedule/page.tsx`/`home/page.tsx`)に`myProfile`受け渡しを追加。tsc/lint/build成功。**実機確認は未実施** → (このcommit)
- 2026-07-10 / Claude Code (Sonnet 5) / オーナー確定を受け着手順を2026-07-10版に改訂（コード変更なし）。新順序: **13（週次CSVバックアップ）を最優先**→16残のfetchAllRawタイムアウト対策＋14残（連続失敗アラート・不一致可視化）→7残の週間サマリー中長距離限定→12残（⋯統一）→9→8→5残（P2/P5）→10残（a/c）→11-b→4-S3→6残→7残のSW対応。完了済み項目をタスク5/7/13/14の各節に**済/未着手**として明記（7の週間サマリー全部員表示・sw.jsキャッシュ無しは2026-07-10にコード確認済み）。4-S5は従来どおりオーナーとタイミング調整のうえ最後 → (このcommit)
- 2026-07-10 / Claude Code (Sonnet 5) / オーナーの依頼で「表示の効率化」を調査しタスク5節にメモを追記（**コード変更なし・実装はしていない。判断・着手は次のエージェント(Fable)に委ねる**）。P1/P3/P4は済でP2/P5は未着手のままと確認。優先度が一番高いと判断したのはP2（`TimelineView.tsx`の「もっと見る」がlimit増の全件再取得のまま）。あわせて今回自分が入れたP6（タスク16残作業の`refreshMemberFromSheetLive`が'sheet'部員のマイページ表示に毎回GAS呼び出しを挟む設計）も新規にメモ化。詳細はタスク5節参照 → (このcommit)
- 2026-07-10 / Claude Code (Sonnet 5) / オーナー指示「練習記録は1日1つ」を明示化。RecordForm.tsxの新規作成モード（FAB経由）で、日付を選ぶたびその日の既存記録をDB検索し、あれば実質「編集」としてフォームへ内容を読み込むよう変更（従来は空欄のまま保存でき、未入力項目がnull/0で上書きされ既存内容が消える恐れがあった）。既存の同日dedupロジック（submit時にsameDayを検索しupdateへ回す）は変更なし・そのまま安全網として残置。見つかった日は案内文表示＋保存ボタンを「更新する」に変更。**オーナー確認: この1日1件ルールはDBをSupabase単独に統合した際に見直す可能性があるため、コードコメントにその旨明記**。tsc/lint/build成功。**オーナーが実機で動作確認済み（2026-07-10）** → (このcommit)
- 2026-07-10 / Claude Code (Sonnet 5) / タスク16残作業「個人の記録画面のCSV/fetchMember直読み化」を実装。オーナー報告（'sheet'切替後もスプシの内容が反映されない）を受け着手。マイページ・自分の`/members/[id]`表示直前に`fetchMember`で本人分だけ軽量取得し毎時cronを待たず非破壊pullする`refreshMemberFromSheetLive`(`sheet-sync.ts`)＋`refreshOwnSheetRecords`(`queries.ts`)を追加。cronの`runSheetSync`側pull-onlyロジックは`computeMemberPull`として共通化しただけで**挙動は不変**（純粋な抽出、本番cronへの影響なし）。GAS呼び出しは5秒タイムアウト・失敗時は例外を投げずDBの現状のまま表示（ページを壊さない設計）。tsc/lint/build成功。**実機確認は未実施**（特に：スプシへ直接入力した内容がマイページ再訪で即座に反映されるか、GAS遅延時にページ表示が極端に遅くならないか） → 7ae16bd
- 2026-07-10 / Claude Code (Sonnet 5) / タスク16（練習記録のスプシDB化）を実装・本番反映。RecordFormの'sheet'閲覧専用ブロック撤去、write-through保存(`/api/sheets/push-record`)、GAS拡張(`fetchMember`アクション追加・`writeCells`のunmapped返却。clasp deploy @6で本番反映済み)、RecordCard/PostOwnerMenuの編集可否をrecord_source考慮に変更。**dry-runで実際に危険な誤検知を発見**: 再送判定を`appIsNewer`(updated_at>synced_at)にしていたところ、'sheet'部員6名・8件の無関係な過去の時刻ズレを再送対象と誤検知し、部員がスプシへ直接入力した内容を古いアプリ値で上書きしかねない状態だった。専用フラグ`practice_records.pending_sheet_push`(migration 20260710100000)に設計変更し、再dry-runで`pushed:0`を確認してから適用(Local/Remote一致確認済み)。tsc/build成功。**個人記録画面のCSV直読み化・実機確認は未着手**（詳細はタスク16節の「残作業」参照）。100人規模でfetchAllRawが65部員で約70秒かかることを実測、既存のmaxDuration=60と衝突する可能性がある既存リスクを発見・記録 → (このcommit)
- 2026-07-09 / Claude Code (Fable 5) / タスク15を削除（オーナーがシートに感想見出しを追加して解消・報告受領）。「未マップ項目の可視化」はタスク16へ引き継ぎ。キュー最前列はタスク16のみに。コード変更なし → (このcommit)
- 2026-07-09 / Claude Code (Fable 5) / オーナー確定を受けタスク16を「練習記録のスプシDB化」（'sheet'部員＝旧TF方式: CSVエクスポート直読み＋GAS write-through、Supabaseはいいね/コメント/タイムライン用ミラー、いいね・コメント・時刻は全部員Supabaseメイン）に全面改稿。コード変更なし → (このcommit)
- 2026-07-09 / Claude Code (Fable 5) / タスク15の真因特定（実物スプシをCSVエクスポートで確認: 感想/コメント見出しが無い部員タブが実在し、名前マッピングが黙ってスキップ。B2駒井タブで実測）・タスク15/16に調査結果を反映（pull読み取りのCSVエクスポート直読み化＝オーナー指摘を16に追記）。あわせて記録フォームの「結果・タイム」ラベルを「結果」に変更（オーナー指示。RecordForm/RecordCard/RecordFieldsSetting）。tsc/build成功・**実機確認は未実施** → (このcommit)
- 2026-07-09 / Claude Code (Fable 5) / オーナー指示によりバックログにタスク15（感想欄がスプシに反映されないバグ・キュー最前列）とタスク16（'sheet'部員のハイブリッド入力＝write-through、旧TF方式・部員100人想定）を追記。着手順にも最前列の注記を追加。タスク2の「'sheet'部員は編集不可」は16が上書き。コード変更なし → fc7c66e
- 2026-07-08 / Claude Code (Sonnet 5) / オーナー再報告により訂正: 前項のPWAマニフェストアイコンの話ではなく、**プロフィール編集の「アイコン画像URL」欄**の話だった。Avatar.tsx(共通コンポーネント)は avatarUrl があれば無条件でimgタグを出すだけで、読み込み失敗時(リンク切れ・Googleドライブ共有リンク等の直リンクでないURL)は崩れた画像アイコンのまま表示されるバグ。onErrorでイニシャル表示にフォールバックするよう修正し、ProfileEditFormに直リンクが必要な旨の注記を追加。tsc/build成功。**実機確認は未実施** → d356c0f
- 2026-07-08 / Claude Code (Sonnet 5) / オーナー報告「ホーム画面に追加してもアイコンが？になる」を修正。manifest.tsのiconsがicon.svg(sizes:any, image/svg+xml)のみで、AndroidのChromeインストール時にSVGアイコンが解決できず「?」プレースホルダになっていたのが原因。next/ogのImageResponseでicon-192.png/icon-512.png(purpose:any)・icon-maskable.png(purpose:maskable、角丸無しの全面塗りつぶし)をroute handlerとして生成しmanifestに登録。ローカルでdev起動し3つとも200/image/pngで実際にPNGが生成されること・manifest.webmanifestの内容を確認済み。tsc/build成功。**実機(Android/iOS)でのホーム追加後の見た目確認は未実施** → 23a6573
- 2026-07-04 / Claude Code (Sonnet 5) / 簡単なものから消化: タスク4-S4(Bearer比較をtimingSafeEqualに、src/lib/timing-safe.ts新設)、タスク12(コメント最大500字・migration 20260704190000、RecordFormのカスタム項目フェッチをprops経由に・FAB新規作成パスのみ配線)、タスク5-P4(fetchCommentCountsをDB側GROUP BY RPC化・migration 20260704200000)、タスク5-P1(getUserRecordsのfromDate省略時を無期限→直近400日、getNoticesに直近200件、getPersonalNotificationsに直近50件のlimit追加)。tsc/lint/build成功・本番反映済み。**実機確認は未実施**。タスク12の「⋯ボタン統一」、タスク5の残り(P2/P3/P5)は未着手 → ed45fa5, be84403, 1287d13, 0063fbf
- 2026-07-04 / Claude Code (Sonnet 5) / インシデント再発防止2点を実装。①タスク2追加要件「切替時リコンサイル」: record_source切替の直前にその部員だけ一度だけ非破壊で両側を揃える(reconcileOnSwitch、/api/sheets/reconcile、ProfileEditForm.save)。app→sheet切替はアプリの中身のある項目をシートへ、sheet→app切替はシートの中身のある項目をアプリへ。失敗したら切替自体を中止。②タスク14を部分実装: sheet-sync.tsのpush/update処理を部員単位でtry/catchし1人の失敗で全員の同期を止めない部分失敗設計、10分以上running放置されたrunをerrorへ倒すクリーンアップ、SheetSyncButtonに失敗詳細表示。migration 20260704180000(sheet_sync_runs.failed_members)本番適用済み。**タスク14の残り(連続失敗アラート・シート名不一致の可視化)は未着手**。tsc/lint/build成功。**実機確認・実際のreconcile/sync動作確認は未実施**（特にreconcileはGAS通信を伴うため、次の入力元切替操作で実際に動くか要確認） → 829e92d, 6fc790f
- 2026-07-04 / Claude Code (Sonnet 5) / **タスク0 真因特定・修正・完了**。オーナーの再現報告(「追加する」を押すと保存するを押す前にモーダルが閉じる)を元に特定: `FullScreenContent`(fullscreen.tsx)のonPointerDownOutsideが、Select等のRadixポップアップは除外していたが入れ子のSheet(RecordFieldsSettingの追加項目用ボトムシート、Dialog.Root)は対象外だった。SheetはPortalでdocument.body直下に描画されるため中をタップすると外側クリックと誤判定され、親のフォームモーダルごと閉じていた。role="dialog"を許可リストに追加して修正。tsc/lint/build成功・本番反映済み。**実機確認は未実施だが原因は構造的に特定できており高確度**。この不具合パターン(入れ子Dialog全般)は他の画面にも影響しうるため注意 → 7ef0a3d
- 2026-07-04 / Claude Code (Sonnet 5) / pg_cron確認・予定取込cron登録。`npx supabase db query --linked`（Management API経由でcron/vaultスキーマにも到達可能。過去に「CLIでは呼べない」と誤って案内したが訂正）で状態確認: sheet-sync-hourly(記録同期)は1件のみ稼働中・vault秘密(sheet_sync_secret/sheet_sync_url)も健在で正常。オーナー承認のうえ、vault.create_secret('schedule_import_cron_url')＋cron.schedule('schedule-import-hourly', '5 * * * *', ...)で予定/メニュー自動取込(`/api/schedule-sheets/cron-sync`)を**別名で**新規登録。実行後にcron.jobを再確認しjobid 1(sheet-sync-hourly)・jobid 2(schedule-import-hourly)が両方activeで名前衝突が無いことを確認済み。コード変更なし → (このcommit)
- 2026-07-04 / Claude Code (Sonnet 5) / 【インシデント対応】オーナーから「過去に入力した記録が消えた」報告。本番DBを直接調査し確認: タスク2+3で入れた「'sheet'方向はシートの空欄もアプリへ反映してよい」仕様が、2026-07-03 17:00の自動同期(hourly cron)で実際に発動し、山端さんの練習記録3件(6/24, 6/29, 7/1)がスプシ側の空欄で上書きされ消失。7/1分は消える1.5時間前にアプリで入力されたばかりだった。①sheet-sync.tsのpull処理を「空でないシート項目だけ取り込む」非破壊の挙動に戻した(5de45d5)。②record_source='sheet'の記録編集画面が実際にはデータが無事なのに「編集できません」というメッセージだけを出して中身を全く見せない作りだったため、これも「データが消えた」ように見える別のUIバグと判明(駒井さんの件はこれが原因で実データは無事)。読み取り専用表示に修正(e9093c6)。**根本論点として、'sheet'方向はアプリ入力が一切スプシへ書き戻されない設計であることをオーナーに説明し、この安全性について要相談中**。消えた3件のデータ自体は復元できていない(要:オーナーとPITR等の復旧要否を相談)。tsc/lint/build成功・本番反映済み → 5de45d5, e9093c6
- 2026-07-04 / Claude Code (Sonnet 5) / タスク10-b（予定スプシの自動取込pg_cron）着手・完了。オーナー提供の実物スプシURLで実構造を確認したところ、**日付列の見出しが「日付」ではなく数字("2")で、既存のヘッダー名一致方式では全行が日付エラーになり一切取り込めない重大バグを発見・修正**（practice kindは先頭列=日付を位置ベースで扱うbuildHeaderRow）。schedule-import.tsにメニュー/ペース/補足/補強列を追加認識し、時間・場所・メニューのいずれかがあれば予定として取込むよう修正（実物仕様どおり）。apply_schedule_sheet_import RPC(migration 20260704170000)を拡張し、単一ブロック指定の行なら予定と同時にブロック全体メニューも生成/更新。新規 `/api/schedule-sheets/cron-sync`（Bearer認証・SHEET_SYNC_SECRET共用・非破壊・admin client直書き）を追加、proxy.tsのBearer専用パスにも登録。**pg_cron登録SQLはオーナーに案内済み・実行待ち**（Supabase SQL Editorで一度だけ実行。docs/SHEETS-SYNC-PLAN.md §7と同じVaultパターン、新規Vercel環境変数は不要）。補強列だけの日（時間・場所・メニュー無し）は予定にならないためメニュー化されない既知の制限あり（practice_menus.schedule_id NOT NULL制約のため今回は対応見送り）。tsc/lint/build成功。**実機確認・実際のcron動作確認は未実施** → 77c7ddb
- 2026-07-04 / Claude Code (Sonnet 5) / オーナー指摘3点を修正。①タイムラインのスプシ由来記録表示(タスク3で追加)を recorded_date >= 2026-07-04 のものだけに限定(SHEET_TIMELINE_CUTOFF定数、queries.tsのgetFeed/getUserActivity)。連携直後に古い記録が一気に流れ込む事故を防止。マイページ集計(getUserRecords)は従来どおり全期間。②migration 20260704160000でprofiles.sheet_linked_at追加(sheet_name設定トリガーで自動記録)。sheet-sync.tsのpull側カットオフをSYNC_CUTOFFとsheet_linked_atの遅い方に変更し、新規連携者は連携日以降のみ取り込み。既存連携済み部員はsheet_linked_at=NULLのままなので影響なし。③ProfileEditFormのシート選択欄が候補取得中は欄ごと非表示で遅れて出現していたのをSkeleton表示に変更。tsc/lint/build成功・**実機確認は未実施** → c906c5e
- 2026-07-04 / Claude Code (Sonnet 5) / オーナー指摘2点を修正。①メニューのスプシ/CSV一括登録は予定作成画面ではなく「メニューを追加」画面内(MenuCreatePanel、通常入力/スプシ・CSVから入力の切替)に移動。②中長距離のメニュー項目に「補足」(remark)が抜けていたため追加、表示・入力・CSV列順を メニュー→ペース→補足→補強 に統一。「詳細」ラベルは「メニュー」へ改称。migration 20260704150000（practice_menus.remark追加・save_practice_menu RPCにmenu_remark追加）本番適用済み。tsc/lint/build成功・**実機確認は未実施** → 9f21e81
- 2026-07-04 / Claude Code (Sonnet 5) / タスク11-a（メニューのプリセットを「過去メニューから複製」に置き換え）着手・完了。MenuForm.tsxのlocalStorageプリセット保存・上書き・削除・旧DBプリセット移行コードを全撤去し、自分が過去に公開したメニューを新しい順(検索可)で複製できるピッカーに置き換え。直近値の自動初期値(対象者引き継ぎ)は維持。DBマイグレーションなし(menu_target_presetsテーブルは未使用のままDBに残置、削除は未実施)。tsc/lint/build成功・**実機確認は未実施**。**11-b（予定のある日を複数選択してまとめて入力）は未着手**。番号順ではこの後タスク12（細かいUI改善）へ進む → 4c5cbcb
- 2026-07-04 / Claude Code (Sonnet 5) / タスク10（予定の一括運用）に部分着手。実物スプシの現物が手元に無い（URLは公開リポのため非掲載・オーナーのみ保有）ため、安全に検証できる範囲のみ実装: schedule-sheets/preview/route.tsに見出し行自動検出（冒頭メモ行を読み飛ばし「曜日・時間・場所」を含む行を探す）、schedule-import.tsに対象年月±1ヶ月の許容（月またぎ対応）、既存予定の突合クエリも同範囲に拡張。**a)ワンタップ再取込導線・b)pg_cron自動取込・c)アプリ内表形式一括入力・メニュー列からのpractice_menus自動生成は未着手**（実物CSVでの検証が前提条件のため）。tsc/lint/build成功・DBマイグレーションなし。次のエージェントは可能なら実物CSVでこのパーサ変更を検証してから残りに着手すること。番号順ではこの後タスク11（メニュー作成の手間削減）へ進む → ce92322
- 2026-07-04 / Claude Code (Sonnet 5) / タスク2+3（スプシ同期の方向固定＋タイムライン表示）着手・完了。migration 20260704140000でprofiles.record_source('app'|'sheet'、sheet_name連携済みは'sheet'初期値)を追加・本番適用。sheet-sync.tsをpull-only('sheet')/push-only('app')に再構成しLWW・コンフリクトスキップを撤去、practice_records読み込みにSYNC_CUTOFFフィルタ追加。ProfileEditFormに入力元切替UI追加、'sheet'の部員はRecordForm(新規/編集とも)をアプリから使えないようブロックし案内文表示。from_sheetをinsert時のみ設定に統一。queries.tsのgetFeed/getUserActivityからfrom_sheet=falseフィルタ撤去でスプシ由来記録もタイムライン表示。tsc/lint/build成功。**実機確認・実際の同期動作確認は未実施**（特にpull時の空欄反映・push時のシート上書きの実データでの確認が必要）。この後タスク10（予定の一括運用）へ進む → 936da59
- 2026-07-04 / Claude Code (Sonnet 5) / タスク6-Q3（JST共通util化）+タスク7（記録フォーム未来日ブロック）着手・完了。`src/lib/date.ts`にjstToday/jstNowを新設し、queries.ts内の重複dateInJapanと、UTC基準バグだった`new Date().toISOString().slice(0,10)`（queries.ts/MenuForm.tsx/ScheduleSheetsManager.tsx）を置き換え。home/page.tsxのnowJst計算も統一。RecordFormの日付にmax属性＋送信時バリデーションを追加。DBマイグレーションなし。tsc/lint/build成功・**実機確認は未実施**。この後タスク2＋3（スプシ同期の方向固定・タイムライン表示）へ進む → 7793d5d
- 2026-07-04 / Claude Code (Sonnet 5) / タスク1（承認ゲート廃止）着手・完了。migration 20260704130000（approvedのDEFAULT TRUE化＋既存FALSE行バックフィル）を本番適用。layout.tsxのリダイレクト撤去、/pending route削除、ホーム承認バナー・members承認UI・PendingApprovals・getPendingProfiles・PendingProfile型を撤去。approved列・is_member()・set_member_approved RPCはDBに残置（指示通り）。tsc/lint/build成功。**実機確認は未実施**。この後タスク6-Q3(JST統一)とタスク7(未来日ブロック)へ進む → 1b6144e
- 2026-07-04 / Claude Code (Sonnet 5) / オーナー割り込み依頼（バックログ番号外）: ①メニューにcontentとは別枠の pace/supplement（ペース/補強、中長距離向け）追加 ②メニューのスプシ/CSV一括登録（予定と同じ体験。対象日の既存予定に紐付け必須、schedule-import.ts型を流用したmenu-import.ts新設、/api/menu-sheets/preview、MenuSheetImportManager.tsx、予定FABを3択化） ③予定の一括登録直後の取り消し（source_sheet_id+created_at基準の再取得→まとめて削除。削除候補リストもチェックボックスで実削除可能に）。migration 20260704120000 本番適用済み・Local/Remote一致確認済み。tsc/lint/build成功。**実機/ブラウザでの動作確認は未実施**（CSVインポートのプレビュー→反映、中長距離メニューのペース/補強入力、予定取込の取り消しボタン、削除候補チェックボックス削除の4点は次のエージェントか実機で要確認）。この後バックログ番号順（タスク1: 承認ゲート廃止）へ復帰 → 67256ee, 48111af, e4845b4
- 2026-07-04 / Claude Code (Sonnet 5) / タスク0（記録フォーム設定保存バグ）着手。Vercel本番デプロイが最新コミット反映済みを確認（調査手順0クリア）。RecordFieldsSetting/FormModalFooter/ReorderList/safe-updateを読み込み静的解析したが確定原因は特定できず。addField/saveの例外握り潰しの可能性を排除する防御的fix（try/catch＋メッセージ表示、crypto.randomUUID未対応時のフォールバック）を実装しpush。**実機（iOS PWA）での「追加→一覧に見えるか→保存→再度開いて残っているか」の再現確認が未完了。次のエージェントはまずオーナーに実機確認を依頼すること** → 5831ec2
- 2026-07-03 / Claude Code (Fable 5) / バックログ10〜12を追記（予定のワンタップ/自動取込・表形式一括入力、メニューの履歴複製・まとめて入力、⋯統一・コメント500字ほか）。@All確定も反映。コード変更なし → 7a3d881ほか(このcommit)
- 2026-07-03 / Claude Code (Fable 5) / 全体コードレビュー→オーナー確定の実装バックログを本ファイルに追記（コード変更なし）。承認ゲート廃止・同期の方向固定(record_source)・スプシ由来記録のタイムライン表示ほか。実装は番号順に別エージェントが担当 → (このcommit)
- 2026-06-29 / Codex / ロール権限にシステム管理を追加。通常管理者の権限昇格と最後の管理者解除をDBで防止し、システム管理者だけ自己投稿お知らせ通知を受信。migration 20260629130000本番適用、組込管理者への付与・通知設定ON・Push購読確認、tsc/対象Lint/build成功 → 完了
- 2026-06-29 / Codex / ホームをSuspenseでストリーミング表示、プロフィール取得重複排除、設定/D&D遅延ロード、主要DB索引追加。お知らせ通知先を全員/複数ロール対応、ベル赤丸をRealtime更新。migration 20260629120000本番適用、tsc/対象Lint/build成功 → 完了
- 2026-06-29 / Codex / 記録フォームをiPhoneホーム編集風の全画面エディタへ刷新、項目名完全一致時のみスプシ同期、全画面Pull-to-refresh、ホームの承認待ち導線、同期ボタン1タップ化。tsc/対象Lint/build成功 → 完了
- 2026-06-29 / Codex / 過去方針の残タスクを文書上でリセット（コード変更なし）。新規登録者の承認ゲートの現状と本番DB適用を確認 → `TASK-codex.md`をアーカイブ化、HANDOFFのアクティブバックログを「なし」に更新
- 2026-06-24 / Claude Code(Codex実装をレビュー適用) / セキュリティ修正を本番適用。RLSのSELECTが USING(TRUE)+public でanonキー単体で profiles(実メール)・記録・ノートが読めた脆弱性を是正。migration 110000(SELECTをTO authenticated＋REVOKE anon)・120000(profiles.approved 承認ゲート/is_member()でSELECT限定/approved直接UPDATE剥奪＋RPC set_member_approved=承認済みなら誰でも承認可/profilesは自分の行のみ例外)。回帰: anonで /rest/v1/profiles → 401 permission denied 確認。tsc/build OK → (このcommit)
- 2026-06-24 / Claude Code / アプリの記録コメントを旧TF式にスプシのリプライ列(感想列の右・列名なし列)へ書き込み。GAS sync-api に writeReply 追加(clasp で安定URL@3に更新)・/api/sheets/reply(作者のsheet_name+日付を引き「{コメント}　{投稿者名}」で書込・作者未連携ならskip)・CommentSectionから記録コメント時に呼ぶ(fire-and-forget) → (このcommit)
- 2026-06-24 / Claude Code / 練習記録同期を本番稼働。GASは手貼り沼を脱しclaspでCLIデプロイ(個人acct, gas/sync-clasp/, secretなし・公開web app)。proxy/middlewareで /api/sheets/sync をBearer用に素通り許可。Vercel: SHEET_SYNC_GAS_URL=新URL/SHEET_SYNC_ENABLED=true 設定。BearerでdryRun→本反映を検証(2件取込・再実行0件=冪等)。残: 毎時cron(pg_cron SQLをユーザーがSupabaseで実行) → (このcommit)
- 2026-06-24 / Claude Code / 表示分離＋カスタム項目並び替え＋GAS分離。①practice_records.from_sheet追加(migration 20260624100000)。シート由来(from_sheet=true)はタイムラインに出さず、アプリ投稿のみ表示(getFeed/getUserActivity)。マイページ集計(getUserRecords)は両方合算。②カスタム項目を上下で並び替え(ProfileEditForm)。③同期pullはfrom_sheet=trueで挿入(sheet-sync.ts)。④旧TFアプリ用GAS(TF/gas/Code.gs)は原状復帰、本アプリ用は別GAS(track-app/gas/sync-api.gs・secret付き)を別URLでデプロイする方針に → (このcommit)
- 2026-06-23 / Claude Code / スプシ同期を安全に作り直し。①応急(15c31ed): 表示を6/22以降・未来除外・空除外に+同期停止。②空/未来のゴミ55行をDB削除(REST,残59)。③再設計: カットオフ/未来/空スキップ・非破壊(空でアプリ/シートを潰さない)・複数記録/日はconflictスキップ・dryRun対応(lib/sheet-sync.ts)。④/api/sheets/sync は dryRun常時可/本番反映は env SHEET_SYNC_ENABLED=true 必須。⑤手動ボタンを「確認(ドライラン)→反映」式に。⑥カスタム項目は項目名=スプシ列名に統一 → (このcommit)
- 2026-06-23 / Claude Code / スプシ(TF構造)⇔練習記録の双方向同期を実装。GASブリッジ(fetchAllRaw/writeCells/listMembers＋日付ベースの汎用ヘッダー検出＋共有secret, TF/gas/Code.gs)・見出し名ベースのマッピング同期(lib/sheet-sync.ts, last-writer-wins)・/api/sheets/{sync,members}・プロフィールでシート選択＋記録フォームのカスタム項目(profiles.record_fields/practice_records.custom, 短距離の独自列対応)・RecordForm動的描画・管理者の手動同期ボタン・pg_cron手順(docs/SHEETS-SYNC-PLAN.md)。migration 20260623100000。menu同期はフェーズ2 → (このcommit)
- 2026-06-22 / Claude Code / PWA未起動の人にホーム画面追加を促すバナー追加(InstallPrompt: Androidはbeforeinstallpromptでワンタップ追加/iOSは手順案内/スタンドアロン時は非表示/閉じたら5日再表示しない) → (このcommit)
- 2026-06-22 / Claude Code / タイムラインのつぶやきカードに学年表示を追加(記録カードと同様/TweetCard) → (このcommit)
- 2026-06-22 / Claude Code / RLSを戻し作成者は自分のメニューを常時閲覧可に(migration 080000)・通知設定を端末通知ON/OFF一本化(ONでブラウザ許可ダイアログ/別建ての有効解除ボタン廃止/ON時のみ種類トグル)・メニュー並びを作成日時非依存の固定順(全体→個別/対象者名順)に → (このcommit)
- 2026-06-22 / Claude Code / 他ブロック閲覧オフでも作成者に他ブロック公開メニューが見える不具合修正(RLS:作成者常時可は下書きのみに/migration 070000)・予定メニューの並び順を作成順で固定(queries埋め込みorder)・「設定」をマイページに新設し通知設定+メニュー表示を展開式(details)で同居・メニュー表示設定を管理者限定から全員に開放(NotificationSettingsButton廃止) → (このcommit)
- 2026-06-22 / Claude Code / 対象者選択を別画面(全画面モーダル)に分離して本体フォームを短縮・メニューカードの⋯を右上絶対配置にして空白行を除去 → 1ab436d
- 2026-06-22 / Claude Code / 用語統一: メニュー種類を「ブロック全体/個人を指定」、ブロック見出しを「ブロック」に統一 → bbebae8
- 2026-06-22 / Claude Code / 他ブロック設定の表示が戻る不具合をrouter.refreshで修正・ブロック共通メニュー上の空白除去・メニュー対象者に検索とブロック絞り込みを追加 → 3a495ce
- 2026-06-22 / Claude Code / メニューをブロックごとにグループ表示(#5)・他ブロック閲覧の個人設定をマイページその他に追加(profiles.menu_view_all_blocks+RLS,既定off,#6)・重要お知らせを✗で消せないように・予定詳細の二重区切り線を解消 → e2857ff
- 2026-06-22 / Claude Code / プリセットをシート選択+⋯編集に刷新(Select不具合解消)・プリセットに本文/ブロックも保存・予定メニューを対象/自ブロックで色分けハイライト・モーダル外クリック誤判定でDialogが閉じる不具合修正 → 73637b2
- 2026-06-22 / Claude Code / メニュー追加ボタンを破線ボタンで明確化＋下書きカードに「公開する」ワンタップ公開ボタン追加 → 9723d7b
- 2026-06-22 / Claude Code / 個別メニューにブロック付与＋同ブロックは閲覧可(RLS更新)、予定カードで自分関連メニューを上位表示、メニュー種類のチラつき再対策 → 1b2609e
- 2026-06-22 / Claude Code / いいね長押しで「いいねした人」シート表示＋戻るボタンの確実化(router.back無反応時fallback)＋メニュー種類の初期表示チラつき解消(直近の種類で開く) → 87fed77
- 2026-06-22 / Claude Code / ノート: フォルダ作成をFABへ統一（/notesでFAB表示・NotesViewの旧ボタン撤去）＋記事一覧に編集(⋯)導線を追加（短い記事も編集可能に） → af734b3
- 2026-06-22 / Claude Code / お知らせ通知タップで該当お知らせへ（タブ切替+スクロール+展開）＋お知らせを折りたたみ表示（基本タイトル/タップで展開）に → e1d8105
- 2026-06-22 / Claude Code / お知らせに「メンバーに通知する」トグル追加（notices.notify_members＋トリガー条件分岐）＋ホームの重要/通常お知らせ混在時の枠被り修正（Linkをblock化） → d2397e8
- 2026-06-22 / Claude Code / キーボード表示時のフォーム揺れ解消: FullScreenContentのvisualViewport追従をReact再描画→ref直接更新+rAF間引きに（毎イベント再描画が原因のぐらつきを除去） → 55e5480
- 2026-06-22 / Claude Code / 通知設定をマイページのリスト（目標等の並び）へ移設＋入力欄の自動ズーム対策（Input/Textarea/スプシ入力を16px以上に＝iOSのガクつき解消） → b45771d
- 2026-06-22 / Claude Code / Web Push基盤を本番投入: VAPID secrets登録/Edge Function send-web-push デプロイ/Vercel env(公開鍵)/pg_netで通知INSERT→push のwebhookトリガー。本番DBへ全マイグレ適用済み（バグ修正含む＝コメント/お知らせ投稿の不具合解消） → 7d0f93a 他
- 2026-06-21 / Claude Code / 予定(schedule_update)通知を廃止（トリガー/関数/notify_schedule列/UI/型/Edge関数）。コメント＋お知らせのみに → a07843f
- 2026-06-21 / Claude Code / 通知トリガーの列名バグ修正（active→status / 予定列名 / notices.author_id）＋重複マイグレ削除 → ac94ce0
- 2026-06-21 / Antigravity / 通知機能（Phase 1-3）実装 → 列名バグ等を Claude が修正のうえ本番投入。以後 Antigravity の運用は終了
- 2026-06-21 / Claude Code / 通知機能の仕様書 `docs/NOTIFICATIONS-PLAN.md` 起案（実装はAntigravity担当）→ (このcommit)
- 2026-06-21 / Claude Code / AGENTS.md 運用ルール整理（署名・報告ルール・HANDOFF優先）→ ed4c8c4

## 着手前の確認：何が「未実装」かを誤認しない
- **未実装かどうかの判断は、最終的に現状のコードを正とする。** 文書は遅れることがある。
- 済んだ指示書を未着手と読み違える事故が続いたため、**2026-09-13に当時の指示書・課題台帳を削除した**（上の索引の注記）。
  残した文書も「いつ書かれたか」を見て、**古い記述を根拠に作り直さない**。
- `docs/UI-UNIFICATION.md` は現在も有効な規約だが、§3・§6 のように「当時こうだった」を
  明示して直した節がある。日付の付いた記述はその日時点の判断として読む。
- 「直す前」に対象機能の現状コードを確認し、すでに動いているなら微修正に留める。

## 実装の型（要点）
- 初期データは Server Component ＋ `src/lib/queries/` に集約（入口は `@/lib/queries`、画面に直接 supabase を書かない）。操作系は Client Component。
- 権限：UI は `permissionsOf(profile.roles)`、DB は `can_*()` / `is_admin()` / `is_staff()` で RLS。ロールは `profile_roles`×`roles`（複数ロールを OR で判定）。
- 学年表記は `B1/B2/B3/B4 ・ M1/M2 ・ D1/D2/D3`（`gradeShort` / `GRADE_OPTIONS`）。
- **update は RLS で弾かれても「エラー無し・0件」で無言失敗する**。重要な更新は `.select()` で件数確認し、0件なら `auth.refreshSession()`→再試行→明示エラー（実例 `src/components/post/ScheduleForm.tsx`）。

## 注意：README は人間向けセットアップ用
`README.md` は2026-09-23に現行構成・複数ロール方式・検証コマンドへ更新した。作業ルールは本ファイル、現在の運用は `ops/laptop/PC-PRODUCTION-HANDOFF.md`、文書の入口は `docs/README.md` を参照する。削除済みの `docs/CLAUDE-HANDOFF.md` への古い作業ログ内の参照はGit履歴用であり、現在の指示ではない。

## Claude handoff — 2026-07-12 tab lab D result and queued UX work

- Tab Lab D (real schedule <-> real home, inactive DOM unmounted) owner result: 100 switches, average 30ms, worst 64ms, max event-loop stop 24ms. No freeze was reported. DOM nodes by tab in display order: Home 237, Schedule 888, Timeline 47, Notes 47, My Page 47.
- Interpretation: cost rises with the real screen DOM, but the isolated client-owned switch path remains responsive for 100 switches. This supports replacing bottom-tab Next Router transitions with a client tab shell that unmounts inactive screen DOM/state. Codebase line count itself is not the cause; route retention/reactivation and the amount of mounted DOM/work are the relevant variables.
- Lab commits already pushed: `199d61c` (A/B/C) and `ba46bf7` (D). Do not treat the lab as production navigation yet.
- Owner explicitly asked Claude to take part of the remaining work because of Codex token limits.

### Next work requested by owner (not implemented yet)

1. Make additions, edits, and deletes of practice records and tweets (and similar mutations where practical) appear immediately in local UI, instead of waiting for `router.refresh()`/server re-render. Use an optimistic/shared client cache and rollback on failure. Ensure both HomeFeed and TimelineView stay coherent, including hidden/restored routes.
2. Home attendance counts must update immediately. Full `ScheduleCard` already does local state. The confirmed gap is the compact `upcomingSchedules` card in `src/app/(app)/home/page.tsx`: its `present`/`absent` counts are fixed server values and its direct `AttendanceToggle` has no `onChanged`. Extract/use a client card with local status/count state; AttendanceToggle already calls `onChanged` optimistically and again with the previous state on failure.
3. Splash: make the final blue TUAT scene transition to Home with the same white-flash/brightening feel used between the earlier scenes. Current `SplashIntro.tsx` removes at `FINISH_AFTER_MS=4380` after only 80ms; CSS `.flash` only runs `flash-between`. Add a final flash state/animation, then reveal Home near peak white without layout flash.
4. Implement and commit these as separate logical tasks, run `npx tsc --noEmit` and `npm run build`, update this log, then push.

### Worktree state at handoff

- Clean. Codex briefly created an untracked draft `UpcomingScheduleCard.tsx`, but removed it before this handoff because it was incomplete. No implementation from the queued work should be assumed present.
- 2026-07-13 / Codex / 季節別ブランド画像の夏版を実装。オーナー提供の夏TFアイコンを原本として、PWA 192/512、Apple 180、maskable 512（安全領域版）、favicon ICO/32、1024原本、OG/Twitter 1200x630を`public/branding/summer-*`へ生成。manifestとroot metadataを季節名付きURLへ接続し、OG/Twitter large image・apple-touch-icon・faviconを設定。SW事前キャッシュも夏アイコンへ変更。`npx tsc --noEmit`/`npm run build`成功。共有カードは夏絵の具背景＋左アイコン＋Archivo BlackのTUAT T&F。実機ホーム画面再追加およびSNSキャッシュ更新はオーナー確認待ち。
- 2026-07-13 / Codex / 夏版ブランド画像をオーナー提供の明色版TFへ差し替え。ホーム画面で旧版が濃く浮くため、白・淡水色主体＋鮮やかな青TFの原本から全サイズを再生成。原本の黒い角はstandard/faviconで透明化、Appleは淡水色で不透明補完、maskableは淡水色のフルブリード安全領域へ変更。OG/Twitterも明るい背景へ再調整。tsc/build成功・実機再追加確認待ち。
- 2026-09-08 / Codex / みんなの目標をアプリ共通の全体・中長距離・短距離タブへ統一。種目別/部員別モードと種目優先順の切替を撤去し、タイムライン/部員一覧と同じ所属ブロック判定で絞り込む。種目ごとの表、検索、種目絞り込み、自分のみ、三点編集削除、FAB追加は維持。取得時と保存直後の目標に所属情報を含め、種目順はシステムの設定順へ統一。共通SegmentedControlにtypeとaria-pressedを追加。全201テスト・tsc・対象eslint・PC専用build成功。PCアプリだけ再起動し、同じHTTPS URLで3タブ切替をブラウザ確認（目標0件のため実データ混在表示は未確認、共通判定は既存テスト確認）。URL・入口セッション・データは維持。本人限定試験のため専用ローカルブランチのみ、本番公開なし。 -> (this commit)
- 2026-09-08 / Codex / ホームを見出し→お知らせ→大会カウントダウン/目標カードの順へ変更。PC試験版だけ再ビルド/再起動し、同じHTTPS URLで表示順を確認。対象eslint・TypeScript付き専用build成功。Vercel課金済みとの申告を受け、Vercelにアプリを残しDB/AuthだけPCで代替する構成と残作業をops/laptop/README.mdへ追記。Vercel使用量は未取得、本番接続先・公開設定は未変更。 -> (this commit)
- 2026-09-08 / Codex / Vercelを残す本人限定PCバックエンド試験を実装。専用プロジェクトtuat-tf-pc-previewを作成し、サーバー限定ブリッジキー・本人セッション・本人JWT/RLSによる中継を追加。管理者DBキー/R2秘密キーはPCに残し、画像GETだけ既存PCアプリへ中継。全206テスト・ゲート2テスト・対象eslint・TypeScript付きPreview想定build成功、ローカル3010からHTTPSトンネル経由でログイン/4画面/目標CRUD/refresh/logout/外部連携拒否を確認し一時目標回収。相対LocationによるNext Proxyの500も修正。ブラウザbundleに実ブリッジキーがないことを確認。Vercelへのブリッジキー送信は自動承認レビューに拒否され、明示承認待ちで未デプロイ。最初の管理者DB/R2キー複製案も拒否され撤去済み。PCゲートだけ再起動し旧入口セッション失効、既存URL/データ保持。本番未変更。次の手順はops/laptop/VERCEL-TRIAL.md。 -> (this commit)
- 2026-09-09 / Codex / オーナーから安全ならPCをSupabaseの代わりに1週間だけ本番利用したいとの条件付き指示。11コンテナhealthy、3009/3108/8000のloopback待受、AC/DCとも2時間スリープ、復元用バックアップの存在を確認。一時URLはENOTFOUNDかつCloudflare DNS直接照会で名前なし、cloudflaredは制御接続失敗を反復。本人限定認証・常設API入口・PCデータの自動外部バックアップ・クラウドへの戻し検証が未完了のため、安全条件未達として本番切替は実施しない。期間は実際の切替完了から7日間とし、復帰時にPC期間中の変更を移す手順をops/laptop/ONE-WEEK-CUTOVER.mdへ記録。設定変更・データ変更なし。 -> (this commit)

- 2026-09-21 / Codex / 実測監査項目5: 所有者承認後、R2対象オブジェクトの非公開台帳とdry-runを保存。新旧2台の最新世代をダウンロードしSHA256・認証付き復号・PGDMP形式を検証後、古いバックアップ988件・2330841083 bytesを削除。直近24時間は全件、以前はJSTで1日1件を30日間保持し、旧PCの最後の世代も保存。DBと復旧材料を組で扱い、未知キーや画像は対象外。ETagとサイズを照合して条件付き削除。検証済みバックアップ後に1日1回実行する処理を追加し、失敗時もバックアップ成功を取り消さず再試行する。tsc・対象eslint・全323テスト・運用テスト8件・build成功。常駐処理は旧コードのままで、中継再起動の所有者承認待ち。実機確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 実測監査項目3の運用反映完了: 追加の明示承認を受け、Vault・Push秘密鍵・cron設定を含むAES-256-GCM暗号化バックアップを既存R2へ保存し、取得・認証付き復号・DBダンプとの結合を検証。19:37 JSTに専用中継を再起動し、全コンテナID不変・本番Auth 200・新常駐処理による復旧設定付きバックアップ作成を確認。実機での再購読・通知受信確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 実測監査項目5の運用反映完了: 追加の明示承認を受け、19:37 JSTに専用中継を再起動して、検証済みバックアップ後に1日1回実行するR2保持処理を常駐コードへ反映。削除済み988件・2330841083 bytes、保持対象82件の不変を確認した記録は維持。全コンテナID不変・本番Auth 200・新常駐バックアップ成功を確認。実機確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 実測監査項目7: 所有者が今の30分以内停止を承認。4Composeファイルを非公開退避し、dry-runで間隔以外の差分ゼロを確認後、10サービスを30秒へ変更（既存30秒のRealtimeを含め全11コンテナ）。適用は約3分以内で完了、全コンテナhealthy・本番Auth 200を確認。設定の定常呼出頻度は5秒のものが1/6、10秒のものが1/3になる。CPUの時間比較は未計測。実機確認は未実施。 → (this commit)

- 2026-09-21 / Codex / テスト通知の二重表示: 1要求から有効2購読への配信を確認。テスト送信先を操作中の本人購読1件へ限定し、同一通知IDの再配信をまとめる。スナップショットとrollbackで本人限定・他人/匿名拒否・冪等性を検証してRPC本番適用。関数配備済み。tsc・対象eslint・全328テスト・build成功。実機での解消確認は未実施。 → (this commit)

- 2026-09-21 / Codex / 大会プログラム・速報: 公式HTMLのスタートリスト／結果で列の意味が変わる点に対応し、プログラムと速報の切替、記録・順位・欠場・風・リレー、公式種目リンクを追加。開催前日〜終了後2日は5分ごと取得、当日の表示中は1分ごと再表示。失敗時保持とservice_role限定の原子的置換RPCをスナップショット・rollback・権限・二重適用で確認。最新57種目74出場を9/22・23の日程へ本番更新し全保存値一致。現在結果0件。tsc・対象eslint・全333テスト・build成功。実機確認は未実施。 → (this commit)

- 2026-09-21 / Codex / ホーム・プログラム上部に開始予定時刻と種目・出場者を示す「競技中」「次の競技」を共通表示。フィールドの並行開催と翌日の次種目に対応し、進行は予定時刻に基づく目安と明示。プログラム内に選手ごとの「結果：記録」を表示。フィールドの途中記録だけでは終了扱いにせず公式完了まで表示。tsc・対象eslint・全vitest・build確認。iOS実機未確認。 -> (this commit)
- 2026-09-21 / Codex / 実測監査項目4: 所有者指定メールへの外部監視を実装。Google Apps Scriptで5分ごとに確認、3回連続異常で1通・復旧で1通。Auth疎通に加え、Push設定・DB HTTP送信の連続失敗・記録同期の失敗/10分超中断/36時間超未更新・大会プログラム30分超未更新を専用Bearer認証APIで確認する。スキーマ退避とrollbackで監視RPCの権限・冪等性を検証して本番適用、監視秘密鍵を保護してVercelへ設定。現在、最新の記録同期は9/19 UTC開始のrunningのままで異常判定される。Google保存済み認証はinvalid_grantのため、専用GASへの配置・メール送信権限承認・初回installMonitor実行は未完了。メール監視はまだ稼働していない。tsc・対象eslint・全337テスト・GAS監視/送信再試行テスト3件・build成功。実機確認は未実施。 → (this commit)
